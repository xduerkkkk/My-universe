比如 Qwen2.5-VL 技术报告里就提到，它使用动态分辨率处理、Window Attention、原生动态分辨率 ViT，以支持图像、文档和长视频理解；它还强调了目标定位、文档解析、表格/图表/版面分析等能力。

这对你的 136 很关键：**文档理解需要高分辨率视觉能力，因为小字、表格线、公式、版面结构都很容易丢。
**现在主流多模态大模型通常是这个结构：

图片  
→ Vision Encoder，例如 ViT / ConvNeXt / Swin / 专用视觉编码器  
→ 视觉 token  
→ Adapter / Projector / Q-Former  
→ 对齐到 LLM 的 token 空间  
→ LLM 统一处理图像 token + 文本 token  
→ 输出回答

可以画成这样：


```
[图片] ──► [视觉编码器 ViT] ──► [视觉 token]  
                                  │  
                                  ▼  
[用户问题] ──► [文本 token] ──► [多模态 LLM] ──► [文字答案 / 坐标 / JSON / 工具调用]
```


# 核心解决问题

一页文档不是纯文本。  
它有标题、段落、表格、图片、公式、脚注、参考文献、阅读顺序。  
模型不仅要读出字，还要理解结构和语义。

识图的核心问题是：

图里有什么？  
它们在哪里？  
它们之间是什么关系？  
图片表达了什么含义？  
能不能基于图片回答问题？



Vision Transformer  
Multimodal Large Language Model  
Document Understanding  
OCR-free Document Parsing  
Layout Analysis  
Visual Question Answering  
Grounding  
RAG  
Agentic Document Understanding



复杂文档 PDF / 图片
→ 页面预处理
→ 版面检测模型：标题、正文、表格、图片、公式、页眉页脚
→ OCR / 表格识别 / 图像裁剪
→ VLM 理解局部区域和整页语义
→ LLM / Agent 整合结果
→ 输出 Markdown / JSON / 问答答案 / 证据定位
→ 网页可视化展示

### 系统输出

第一类：**版面可视化**

标题：红框  
正文：蓝框  
表格：绿框  
图片：紫框  
公式：黄框  
页眉页脚：灰框

第二类：**结构化结果**

{  
  "document_type": "academic_paper",  
  "pages": [  
    {  
      "page_id": 1,  
      "blocks": [  
        {  
          "type": "title",  
          "bbox": [120, 80, 900, 150],  
          "text": "A Survey on Vision-Language Models"  
        },  
        {  
          "type": "section_header",  
          "bbox": [100, 220, 500, 260],  
          "text": "1. Introduction"  
        },  
        {  
          "type": "table",  
          "bbox": [90, 600, 1000, 900],  
          "summary": "This table compares different VLM architectures."  
        }  
      ]  
    }  
  ]  
}

第三类：**Markdown 还原**

# A Survey on Vision-Language Models  
  
## 1. Introduction  
  
...  
  
| Model | Vision Encoder | LLM | Task |  
|---|---|---|---|  
| LLaVA | CLIP ViT | Vicuna | Visual QA |

第四类：**文档问答 + 证据定位**

用户问：

这篇文档主要讲了什么？  
实验结果在哪一页？  
表格 2 说明了什么？  
作者用了哪些模型做对比？

系统答：

文档主要讨论了视觉语言模型在复杂文档解析中的应用。  
证据来自第 2 页的 Introduction 区域和第 5 页的 Table 2。

并且高亮对应区域。

这就很有展示效果。



# 技术流程

1. 把 PDF 或图片变成可以处理的页面图。
   用现成的库
2. 版面检测
   DocLayNet 很适合用来做这个模块，它是人工标注的文档版面分析数据集，包含 80,863 页文档页面，并提供 11 类版面标签。
3. OCR 与结构化解析	```
	标题区域 → OCR → 标题文本  
	正文区域 → OCR → 段落文本  
	表格区域 → 表格识别 → HTML / Markdown 表格  
	图片区域 → VLM 描述 → 图片说明  
	公式区域 → OCR / VLM / LaTeX 识别，可选
	把文档从“图片”变成：带坐标的结构化文本
	 {  
	   "type": "paragraph",  
	   "text": "...",  
	   "bbox": [x1, y1, x2, y2],  
	   "page": 3  
	 }```
4. VLM 文档理解，Qwen2.5-VL
   请分析这页文档的版面结构，并输出标题、段落、表格、图片和阅读顺序。
   把检测出的表格、图片、图表区域裁剪出来，单独问 VLM：请解释这个表格表达了什么。  
	请总结这张图的主要趋势。  
	请判断这块区域是正文、公式还是图注。
	请根据页面视觉布局，对以下 OCR 文本块重新排序，并输出 Markdown。
5. Agent / 工具流


我建议你按这个方案做：

第一阶段：传统文档解析基线  
PDF → 图片 → PP-Structure → OCR → Markdown/JSON  
  
第二阶段：版面检测模型训练  
DocLayNet → YOLO/Detectron2 微调 → 文档区域检测 → 可视化框  
  
第三阶段：VLM 辅助理解  
页面/区域图片 → Qwen2.5-VL 或其他 VLM → 图表解释/结构校正/摘要  
  
第四阶段：Agent 工具流  
用户问题 → 判断需要 OCR、裁剪、表格解析还是 VLM → 调用对应工具 → 输出答案  
  
第五阶段：网页展示和实验评测  
Gradio/Streamlit → 上传文档 → 显示检测框、结构化结果、问答证据
   


