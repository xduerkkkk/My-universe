你现在要可视化的，不是“训练过程”。

而是：

模型在 test set 上的最终预测结果

也就是：

gold labels vs predicted labels

画回原始页面图像。

---

# 1. 你现在到底在可视化什么？

你训练结束后已经有：

text_only_test_predictions. json  
text_layout_test_predictions. json  
layoutlmv3_test_predictions. json

里面一般包含：

{  
  "id": "sample_001",  
  "tokens": [...],  
  "bboxes_norm": [...],  
  "gold_labels": [...],  
  "pred_labels": [...]  
}

这些就是：

模型预测结果

你现在要做的是：

把这些 token bbox 和标签重新画回原图

---

# 2. 为什么这个特别重要？

因为数字指标：

Entity F1 = 0.7479

老师其实很难直观看出：

模型到底学到了什么

但如果你展示：

|OCR-only|LayoutLMv3|
|---|---|
|漏掉很多字段|框得很完整|

这个效果会非常强。

这就是：

Document AI 的“可解释展示”

很多论文都会这样做。

---

# 3. 这一步和你原来的 Demo 怎么联系？

你原来的 Demo 已经有：

OCR bbox visualization

现在只是：

把 bbox 的颜色改成“预测类别”

所以你会发现：

你之前代码其实没白写

只是之前是：

OCR 可视化

现在变成：

Field Extraction 可视化

---

# 4. 这一步本质上是“模型解释”

因为你会展示：

## 正确案例

例如：

QUESTION: Name  
ANSWER: John Smith

LayoutLMv3：

正确框出 QUESTION 和 ANSWER

---

## 错误案例

例如：

gold: HEADER  
pred: O

图里会看到：

模型没识别标题

这个就是：

错误分析

老师会非常喜欢。

---

# 5. 接下来 Claude Code 要做什么？

我建议下一阶段让 Claude Code 做：

# 第一部分：单模型可视化

输入：

prediction json

输出：

画框后的 png

---

# 第二部分：三模型对比图

自动生成：

OCR-only  
Text+Layout  
LayoutLMv3

三列对比图。

---

# 第三部分：错误高亮

错误 token：

pred != gold

用红框。

正确 token：

按类别颜色

---

# 6. 你马上会学到什么知识？

这一阶段其实是：

# Document AI Visualization

你会学到：

## 1. token-level prediction visualization

这是 NLP + CV 的结合。

---

## 2. bbox coordinate mapping

你现在 bbox 是：

0~1000 normalized

要重新映射回：

真实图像坐标

你会真正理解：

为什么要做 bbox normalization

---

## 3. 错误分析可视化

这是：

真正像研究项目

而不是单纯训练模型。