- **A - Abstract (抽象与建模):**

1. class相比于C语言的struct，其最核心的进化是什么？
struct是随意修改数据  class中定义了private 是基础的数据 暴露给外部的接口只有public 外部只能按照我们设定的规则，进行对数据的调用、修改等


2. 描述一个对象诞生的“两阶段”过程。用“打地基”和“盖大楼”的比喻，解释“成员初始化列表”和“构造函数体”的执行时机和本质区别。
一个对象？什么是对象....  嗯... class是对象，还是class里定义的成员是对象。  
成员初始化列表是在构造函数体之前进行的  Classroom构造函数的{}**之前**，编译器会先为Classroom的**所有成员变量**（比如m_monitor）分配内存，并调用它们各自的构造函数进行**初始化**

3. this指针解决了什么核心问题？它在成员函数中，扮演了一个怎样的“隐藏角色”？
this指针，指到的变量，代表成员属性的变量，而不是接收参数的变量
4. 析构函数的“天职”是什么？它在什么情况下会被“自动”触发？什么情况下又需要我们“手动”触发（通过哪个关键字）？
用于回收内存。在生命域结束后自动回收，。手动》。。忘记了
- **栈对象：** 自动管理，作用域结束时，析构函数自动调用。(省心)
    
- **堆对象：** 手动管理，必须手动delete，delete时会触发析构函数调用。(灵活但易出错，是内存泄漏的根源)


- **R - Rebuild (重建与复现):**
    
    - **任务：** 打开一个空白的记事本，**徒手**写出我们Student类的.h文件的完整声明。不需要写实现，只需要写出所有private成员和public函数的声明。这个练习，是在检验您是否已经将class的“八股文”结构，刻入脑海。
        
- **T - Templatize (提炼与归档):**
    
    - **任务：** 为我们正在编写的Student类，制作一张“战术卡片”。这张卡片，应该包含：
        
        - **类名：** Student
            
        - **核心职责：** 描述一个学生的基本信息。
            
        - **成员变量(Private)：** m_id, m_score。
            
        - **公有接口(Public)：** 构造函数（多种）、析构函数、set/get方法、print方法。
            
        - **设计要点：** setScore中体现了数据验证；构造函数使用了初始化列表；析构函数（未来）需要负责资源清理（虽然这里没有需要清理的）。