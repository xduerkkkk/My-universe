
```cpp
void backtrack(参数...) {
    // 终止条件 (找到解 或 走不通)
    if (满足终止条件) {
        // ... 处理结果 ...
        return;
    }

    // 遍历当前层的所有“选择”
    for (选择 in 当前可选列表) {
        // 1. 做选择
        //    (e.g., path.push_back(选择))
        
        // 2. 递归到下一层
        backtrack(更新后的参数...);

        // 3. 撤销选择 (回溯)
        //    (e.g., path.pop_back())
    }
}

```
