- **A - Abstract (抽象与建模):**
    
    - **任务：**
        
1. 顺序表（数组）实现O(1)随机存取的底层“魔法”是什么？请描述那个简单的“地址计算公式”。
就是，额，地址的计算呢，是加法和乘法，所以一步到位。
加入说，一行有10个，我们现在要找下标11 其实是第二行第二个（因为下标从0开始）。
那我们有了下标0的地址，先11除以9 等于 1 余2  我们地址就 诶等等，直接0的地址加11？

2. MyVector内部，最重要的三个成员变量是什么？
首先 要有零下标的指针。然后就是，我们一共的容量，还有当前的成员数
3. 描述push_back操作中，“动态扩容”的详细步骤。
先检查一下，当前的成员数相对容量是否多了，如果多，进行扩容。
扩容的话我们不是在原来地方扩，而是直接搬新家，去一个新的更大的内存空间。申请连续的空间。   然后一个一个赋值过来，包括新成员。最后再把旧的零下标指针delete
            
- **R - Rebuild (重建与复现):**
    
    - **任务：** 暂时忘掉我们已经写好的MyVector.cpp。请您只看着MyVector.h的声明，尝试在纸上或记事本里，**徒手**写出push_back函数和~MyVector析构函数的伪代码。
        
- **T - Templatize (提炼与归档):**
    
    - **任务：** 制作一张“手写std::vector核心功能”的战术卡片。
        
        - **核心数据成员：** T* m_data, size_t m_size, size_t m_capacity。
            
        - **push_back算法流程：** if (size == capacity) -> 计算新容量 -> new新内存 -> 拷贝旧数据 -> delete旧内存 -> 更新指针和容量 -> 常规添加。
            
        - **构造/析构原则：** 构造函数初始化指针为nullptr，析构函数delete[]指针。