- **域（Domain）**：一组具有相同数据类型的值的集合。比如“年龄”的取值范围、“性别”的可能取值。
- **笛卡尔积**：把多个域中所有可能的取值组合都列出来，形成所有可能的 n 元组。

- **关系（Relation）**：不是笛卡尔积的全部结果，而是其中**有意义元组构成的有限子集**；从用户视角看，它表现成一张二维表。

关系是现实中有意义的，笛卡尔积表格的元组



- **关系模式 vs 关系实例**：
    
    - 关系模式：关系的结构定义，静态、稳定。
        
    - 关系实例：某一时刻实际存放的元组集合，动态、会变化