1. <font color="#ff0000">?</font>  
   存储保护的目的是**防止进程越权访问内存**
   分时系统中的内存分配是**动态的**。每个用户需要的内存大小不同，系统会根据实际需求和调度策略分配，不可能是死板的 $M/N$ 平均分配。
   虚拟地址空间的大小受两个因素限制：**外存（磁盘）的大小**。；**计算机的地址位数（最重要的硬限制）**。
   实现虚拟内存需要 **MMU（内存管理单元）**、页表寄存器、中断处理机制（处理缺页中断）等硬件支持。光靠软件（OS）是无法完成高效的地址转换的。
2. ?
   **内存管理不能改变硬件的物理存取速度**。物理速度由内存条的硬件性能（如 DDR5 的频率）决定。内存管理（软件+MMU）甚至会因为增加了地址转换的过程，而在宏观上稍微“拉慢”一点访问时间（虽然通过 TLB 快表补偿了）
3. B
   内存保护**必须要有硬件支持**。地址转换和权限检查（比如判断是否越界）是在 CPU 执行每一条访存指令时发生的。如果只靠软件检查，CPU 每读一个数都要跑一段 OS 代码来验证，电脑会慢到无法使用。必须由硬件（如界地址寄存器或 MMU）在指令执行的同时完成检查。
4. C
5. ?
   硬件地址变换机构与动态重定位绑定。
6. B
7. D
8. C
9. B
10. D
11. 哪错了
    在硬件层面，CPU 通常只设置**一个**重定位寄存器（也叫基址寄存器）,当进程切换时，操作系统会把 PCB（进程控制块）中保存的该进程起始地址装入这个寄存器。并不是给每个进程都发一个昂贵的硬件寄存器，而是通过**时分复用**来实现的。$$\text{物理地址} = \text{逻辑地址} + \text{重定位寄存器的值}$$
12. ?D
    **可重定位装入程序**是**静态重定位**才需要的
    **重定位寄存器**(用来存放进程在内存中的起始物理地址。) 和**地址变换机构**（即 MMU 等硬件逻辑，负责把逻辑地址和寄存器值“拼”或“加”在一起。）是动态重定位必须要的。
13. B
14. A,
15. D?
    在整个系统中只设置一个定位寄存器的原因是？
    - **可变分区** 产生了 **外部碎片**。
    
	- 为了消除碎片，需要进行 **紧凑（搬家）**。
	    
	- 为了让搬家后的程序能运行，必须采用 **动态重定位**。
	    
	- 动态重定位需要 **硬件地址变换机构（MMU）** 的实时支持。
	    
	- 出于成本和逻辑考虑，CPU 内部通常只维护 **一套** 物理寄存器，通过 **进程切换** 来复用。
16. A, 不是 A 和 B 都有吗
    可变分区管理中，目的是合并空闲区，合并分配区只是顺带的
17. B
18. B
19. B
20. C
21. A
22. A
23. C (这和大端小端有关吗)
24. 分配算法
    基于顺序搜索的，首次适应、循环适应、最佳/最坏适应
    基于索引搜索的，快速适应、伙伴适应、哈希算法
25. A
26. D?
    页表是由操作系统在程序装入内存时建立的
27. <font color="#ff0000">D</font>
    共享段表的作用是实现多个进程共享同一段代码或数据
28. C
29. <font color="#ff0000">D</font>
    页和段式，系统提供给用户的物理地址空间，为总空间大小减去页表或段表的长度
30. B
31. D
32. <font color="#ff0000">B</font>
    查找页表/段表都是硬件，MMU 实现的
33. <font color="#ff0000">A</font>
    程序如何分段是在用户编程时决定的
34. A
35. <font color="#ff0000">C</font>C
    当编译器（Compiler）处理你的源代码时，它并不知道你的程序将来会被加载到内存的哪个角落。

	- **起始为 0**：为了方便计算，编译器假设这个程序是从“零点”开始排列的。
	
	- **逻辑地址**：因为这个地址是“名义上”的，不是内存条上真实的物理位置，所以叫逻辑地址（也叫虚拟地址、相对地址）。
    IP是**动态运行**时的状态，不是目标文件里的静态编号。
36. ?
    ![[02王道题-1772030918835.jpeg]]
    可重入程序是执行期间不修改自身的纯代码。代码在内存和磁盘之间换来换去叫对换
37. A, 管理代价？
    就说固定分区管理代价最小。因为其他的要硬件的快存、地址加法器等
38. A
39. C
40. B
41. <font color="#ff0000">C?</font>
    ![[02王道题-1772606904820.jpeg]]
    选A..引入段式存储，为了满足方便编程、分段共享、分段保护、动态链接和增长。解释下。
42. <font color="#ff0000">C</font>
    对主存的访问以字节或字为单位，对主存的分配才有不同的方式
43. A
44. A
45. C
46. <font color="#ff0000">C</font>
    对于段页式，访存要 3 次。
47. <font color="#ff0000">C</font>
    段页式，内存是二维的。段号+（页号+页内地址）
48. C
49. A？
    每个进程都有自己的页表，不过系统只有一个页表寄存器。
50. A
51. D?
    分段方式对低级语言程序员和编译器可见
52. B
53. D
54. C
55. ？
    ![[02王道题-1772183766952.jpeg]]
    这道题没做出来的原因就是，相对地址，就是未装入内存前的逻辑地址？ 我没理解。同时，store r1 2500 的 2500 居然也是相对地址
56. D
57. D
58. 
    ![[02王道题-1772184166260.jpeg]]
    2^48 除以 2^12, 2^36 个页号，按理说?，有 2^36 个页表项，而一个页表项，用 8B 来表示块号。所以整个页表的大小是 2^39 B.算一下，就页表也要存到页框里嘛。要花费 2^9 个页框存页表。
    即，每一个页，能放 9 位页号。现在所有页号是 36 位，所以外面使用 4 个页，就能表示完所有页号。
59. C? A?
    ![[02王道题-1772184148631.jpeg]]
    什么是越界访问，进程间通信就不是越界访问吗
    “越界访问”是指访问**自己地址空间里未映射/无权限**的区域（或写只读页等）
60. A?
61. 
    ![[02王道题-1772198433365.jpeg]]我的理解是 2^32-2^8 啊
62. B
63. B
64. C?
    链接后形成的目标程序的地址就是逻辑地址
65. D
66. D
67. <font color="#ff0000">A</font>
    动态分区内存管理，回收内存后会根据算法排序！
68. B
69. A
70. <font color="#ff0000">B</font>
    各个动态分区分配算法的优缺
71. B
72. C
73. A?

# 虚拟存储
1. B，到底什么叫页面尺寸，就是页面大小吗
   对
2. B
3. B
4. B
5. A?
   虚拟内存的特征：多次性、对换性、离散性
6. C
7. B
8. C
9. <font color="#ff0000">B</font>（程序的链接方式）
   到底什么叫程序的链接，什么是运行时链接什么时装入时链接
10. A
11. <font color="#ff0000">B</font>1
12. 
    11、12 是同类题型。算算一个页框是几行数组
    ![[02王道题-1772454119126.jpeg]]
13. B
    又是访存次数不清晰。
14. D
15. C
16. <font color="#ff0000">A</font> (页帧数增加对不同算法的影响)
	像 **LRU（最近最少使用）** 和 **OPT（最佳置换）** 这样的优秀算法，属于**栈式算法（Stack Algorithm）**。
	- **包含性质（Inclusion Property）**：栈式算法有一个极其牛逼的数学特性。假设在同一时刻，你给系统分配了 $M$ 个页框，里面装了一堆页面；如果你给它分配 $M+1$ 个页框，那么**这 $M+1$ 个页框里，必定百分之百包含了那 $M$ 个页框里的所有内容。**
	    
	- **结果**：因为大内存永远完美兼容小内存里的数据，所以只要增加页框，命中率必然上升，缺页次数必然**单调递减（或不变）**。
	
	FIFO 会发生 Belady 现象，CLOCK 其实也属于改版 FIFO，也会有 belady 现象。
	
	
17. D?
    （上限由 XX 决定）
    实际虚拟内存容量 = $\min(\text{内存}+\text{外存容量}, 2^{n} \text{（地址上限）})$。
    **虚拟内存上限**：由 **CPU 的逻辑地址位数（即寄存器宽度/指令集架构）** 决定。写代码时的指针是 32 位的，那你的程序眼里最大就只有 4GB 的空间
    **物理内存上限**：由 **CPU 的“地址总线”引脚数量** 决定。看**硬件引脚数**
18. <font color="#ff0000">A</font>?
    - 问**页表项个数/页表行数** $\rightarrow$ 盯死**虚拟地址（逻辑地址）**。
    
	- 问**页表项大小/块号位数** $\rightarrow$ 盯死**物理地址（主存大小）**。
19. B
20. D.小页表反而让页面更大了
21. C
22. <font color="#ff0000">A</font>，LRU 需要什么特殊的硬件
    ![[02王道题-1772522623526.jpeg]]
    D 是 A 的原因。 LRU，必须随时记录每一个页面的“历史访问顺序”
23. A?
    有效位是，这页到底在不在物理内存里？页故障就是缺页中断。保护码 记录的是读、写、执行权限。如果你强行写入一个只读页面，触发的是**越权访问异常（段错误）**，而不是缺页故障。
24. <font color="#ff0000">A</font>
    所有页面调度策略都不可能完全避免抖动
25. C
26. B? 分页存储的特点，很多啊，选择到底怎么选
    便于动态链接 / 便于信息共享, 段比页更明显。“请求分页”，强调“请求”时，主要作用是扩充内存
27. C,
28. <font color="#ff0000"> D</font>
    只能说理想情况下，工作集应是驻留集的子集
29. B
30. D
    CPU 与内存交换的速度不影响页面换入换出
31. B
32. B, belady
33. B
34. C,
35. <font color="#ff0000">B</font>
    内存映射文件，能不能理解就把文件当作能被分页的进程一样，然后能进行读写操作
36. C
37. C
38. A
39. 什么叫 CPU 利用率
    **CPU 利用率**指的是：**CPU 在真正执行程序代码（干正事）的时间，占总时间的比例**
    CPU 利用率极低（比如 10%~20%），而磁盘交换区的利用率极高（比如 97% 以上）就是“抖动”，
40. B
41. A
42. D
43. A
44. <font color="#ff0000">C</font>
    **虚拟存储只能基于非连续分配技术**：这是虚拟内存的**根本大前提**。如果采用“连续分配”（也就是程序必须像一个完整的长条一样，连续地塞进内存），那怎么可能做到“只把当前需要用的几页装进内存，其他的留在磁盘”？
45. B
46. <font color="#ff0000">A</font>
    让页表常驻内存也可以加快虚实地址转换。可我之前根本没有这个概念，页表难道不都是在内存里吗
47. A
48. C
49. A
50. A
51. A
52. C
53. D
54. ?
    ![[02王道题-1772529174597.jpeg]]
55. A
56. D, 什么是页缓冲队列：将淘汰的页面缓存下来。显然，不影响缺页率。
57. D