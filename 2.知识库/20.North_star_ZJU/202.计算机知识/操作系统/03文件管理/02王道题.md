# 文件基础
1. D? 
   ![[02王道题-1773730773011.jpeg]]
   
   UNIX 将 I/O 设备分为**字符设备文件**（键盘、鼠标，不可寻址，按字符流读写）和**块设备文件**（硬盘、U盘，可寻址，按数据块读写）
   设备文件的 Inode 里存的是**主设备号**（找对应的设备驱动程序）和**次设备号**（区分同类型的具体哪个设备，比如是 1 号硬盘还是 2 号硬盘）。
2. A
3. <font color="#ff0000">C?</font>
   打开文件操作是把指定文件的目录项复制到内存指定的区域。
   具体来说是复制到系统文件打开表中，不过系统文件打开表的记录的数据项，还包括本身的索引（不对，实际上是隐含在数组）、打开计数器
4. B?
5. <font color="#ff0000">A？</font>!!
   关闭文件指讲文件当前控制信息从内存写回磁盘，而**不是将文件数据写回磁盘**。
   为啥？那数据的修改到底怎么实现？
   **数据的修改当然会写回磁盘，但它不是由 `close()` 这个动作“强制、同步”去做的。** 这里引入了一个操作系统的核心作弊神器——**内核缓冲区（Buffer/Page Cache）**。
   数据缓冲区里的“脏数据”写回磁盘，有以下几种触发时机：
	 - **异步后台线程（主要靠它）**：操作系统后台有一个守护线程（比如 Linux 的 `flusher` 线程），每隔一段时间（比如 30 秒）就会疯狂扫描内存，把所有标记为“脏”的数据块批量刷进磁盘。
    
	- **内存不够了**：当系统内存紧张，需要回收缓冲区时，会把脏数据强制刷回磁盘，然后清空内存。
	    
	- **用户手动强刷**：你在写代码时调用了 `fsync(fd)` 或 `sync()`，这是在命令系统：“别等了，立刻给我把数据埋进磁盘！”
	   
6. B
7. <font color="#ff0000">B?</font>
   ![[02王道题-1773732541401.jpeg]]
   Windows 里的“我的文档”文件夹，里面是不是既有 Word 文档（数据文件），又有新建文件夹（子目录）？这个目录文件存放的就是这些东西的清单（FCB 集合）。
   所以目录文件是 FCB 的集合，既可能有子目录又可能有数据文件。
8. A？
   内存索引节点是磁盘索引节点 copy 过来的，是读文件过来的，甚至不是打开文件过来的，是吗？然后只多一个 count，其他和磁盘的节点一样？
   
   `open()` 去磁盘上查目录，找到这个文件的 FCB / Inode，然后**把这个 FCB / Inode 从磁盘 copy 到内存里！** 这就叫建立“打开文件表”和“内存索引节点”。`open` **绝对不会**去把文件里的文章内容（数据）读到内存里。
   
   **`read()` 操作的核心任务**：看着内存里已经建好的打开文件表，顺着里面的物理块地址，去磁盘把真正的**文件内容（数据数据数据！）**读进内存缓冲区。
   
   内存 Inode 确实是从磁盘 Inode 复制来的。多加的几个动态属性：count、dirty bit、设备号
   
   count记录当前整个系统中有多少个进程/表项正在使用这个内存 Inode。进程 A 关了文件 $\rightarrow$ 调用 `close()` $\rightarrow$ `count` 减 1，变成 1。由于 `count > 0`，系统知道还有别人在用，**绝对不能把这个 Inode 从内存中销毁**。进程 B 也关了文件 $\rightarrow$ 调用 `close()` $\rightarrow$ `count` 变成 0。系统一看 `count == 0`，说明没人用了，这时才会放心把这个 Inode 从内存里回收掉。
   
   `dirty bit`，在 `count` 降为 0，系统准备把这个 Inode 踢出内存时检测，看是否磁盘 IO
   
   device number 标记**这个文件到底在哪块物理磁盘、哪个分区上**。

9. FAT32？
   文件目录项不包含自身的物理位置信息
10. C?
    inode 是为了减少查找时的 IO，其他环节的 IO 省不了！
11. A
12. D
13. <font color="#ff0000">B?</font>
    文件的访问由用户访问权限和文件属性里的控制信息共同限制，和用户优先级没有关系
14. <font color="#ff0000">C?</font>
    ![[02王道题-1773732868309.jpeg]]
    - **系统级管理（第一道大门）**：
    
    - **干什么的**：防贼进院子。控制谁能登录这台电脑/系统。
        
    - **手段**：注册、登录账号、校验密码（如题目所说的“进入系统时注册”）。
        
	- **用户级管理（第二道大门）**：
	    
	    - **干什么的**：院子里的不同人，能进哪些房间。
	        
	    - **手段**：给不同的用户分类（比如超级管理员 root、普通用户、访客），并通过“访问控制列表 (ACL)”规定张三能访问目录 A，李四不能。
	        
	- **目录级管理（第三道大门）**：
	    
	    - **干什么的**：保护整个房间（文件夹）不被乱搞。
	        
	    - **手段**：规定对某个目录的读、写、执行权限。防止别人把整个目录删了。
	        
	- **文件级管理（最后一道防线）**：
	    
	    - **干什么的**：保护具体的某个抽屉（文件）。
	        
	    - **手段**：设置文件的具体属性。比如哪怕你进了房间，这个文件我也设成了“只读”、“隐藏”、“系统文件”。
15. D?
    异步 IO 只是提高 CPU 利用率，对磁盘 IO 次数没有影响。
16. D
17. D
18. B
19. C
20. A
21. B
22. <font color="#ff0000">B</font>
    连续结构不利于文件长度动态增长，主要原因是连续结构要预留空间。链接分配不需要预留，随便增。
23. B
24. B
25. D
26. C 突然感觉，显式链接的 FAT，和索引表，和逻辑上的索引文件，都挺像的啊，辨析一下
    [[辨析--逻辑索引 vs 物理索引 vs FAT 表]]
27. B
28. <font color="#ff0000">D?</font>
    模糊点 ：设备的分配方式模糊地等同于了存储介质特性？
    逻辑文件存放到存储介质上，采用的组织形式与存储介质特性有关。
    设备的分配方式完全是 IO 章节的概念
    
29. <font color="#ff0000">B?</font>!！
    错误原因：觉得“增加一块”就是在末尾添加，题目没说，那完全可能是在中间添加的！此时再去分析效率等。
    在文件中加一块，一定是连续分配 IO 次数最多。因为有可能要迁移。其他分配方式无论如何都是改改指针、索引之类的
30. ？
    啥是记录成组分解技术？逻辑长度是 100B 盘块长度是 512B 不是一个盘块就把这个文件记录完了吗？为啥还有“第 22 个逻辑记录”
    ![[02王道题-1773737260824.jpeg]]
    不是逻辑长度，是逻辑记录的长度！是每行数据的大小。提到了“第 22 个逻辑记录”，说明这个文件**至少有 22 行数据**. 算一下总大小：$22 \times 100\text{ B} = 2200\text{ B}$。512B 的盘块显然装不下，它必须分开放到好几个不同的盘块里。
    一个·盘块装 5 个记录，修改第 22 个逻辑记录要访问 5 次拿到数据，然后修改数据，是把表项先存到缓冲区，无 IO，最后缓冲区再改回磁盘，一次 IO，共 6次。
31. B
32. <font color="#ff0000">A</font>
  
33. 
    卡点：簇号意味什么？
    有多少 FAT 表现？2^16，就代表簇号是 16 位！就代表了 FAT 表项占用的字节数
    $$\text{FAT表总大小} = \text{FAT表项的总个数} \times \text{每个FAT表项占用的字节数}$$
    这个系统的最小物理单位是 **簇 = 8KB**。也就是说，不管文件多小，哪怕只有 1 个字节，系统也会直接分给它 8KB 的物理空间。
    $$\text{簇的总个数} = \frac{512\text{MB}}{8\text{KB}} = \frac{512 \times 1024\text{KB}}{8\text{KB}} = 65536 \text{ 个}$$
    磁盘上一共有 $65536$ 个簇。因为 FAT 表是全局的，每个簇都要在 FAT 表里占一行，所以 **FAT 表项的总个数 = 65536 个**。
    $2^{16} = 65536$。所以，你需要用 **16 个 bit（也就是 2 个字节）**，才能存得下一个簇号。
    每个 FAT 表项需要占用 **2B (Bytes)** 的空间。- $\text{FAT表总大小} = 65536 \text{ (个表项)} \times 2\text{B (每项大小)} = 131072\text{B}$
	- 把它换算成 KB：

![[02王道题-1773738680631.jpeg]]
34. C
35. B?
    各类分配何时需要磁盘 IO？
    注意，隐式链接的指针（标记）**不在内存的 FCB 里**，而是藏在**每一个磁盘数据块的末尾**，内存中的文件头指针确实改一下就行，但是，块 $X$ 内部原本写着“下一个指向空”，现在必须改成“下一个指向原本的第一块”。因为块 $X$ 在磁盘上，你**必须把块 $X$ 读入内存，修改它尾部的 4 字节指针，再写回磁盘**。
    单级索引的话改标记完全能解决
36. A
    连续分配不是随机访问吗？为啥要读 45 次
    在计算机底层，“把磁盘上的块 A 挪到位置 B” 的真实物理动作是：
    启动磁盘 I/O，把块 A 的内容**读入**内存缓冲区。启动磁盘 I/O，把内存缓冲区里的内容**写入**到磁盘的 B 位置。
37. ？
    磁盘地址和本题有啥关系？
    文件块和磁盘块大小 4KB，共需 12 位，我的错误思路是，一个文件包含 10 个直接指针-->120 位，磁盘地址是 32 位，完全不够。
    可是磁盘地址，只是用于描述文件块地址的位数。需要 320+32=352,352/8B，即可，远小于 4KB。
    一共才用了 44 字节（$44\text{B} \ll 4\text{KB}$），它根本不需要占用一个独立的 4KB 盘块，它就缩在系统专门划给 Inode 存放的内核区域里。
    
    文件所占的磁盘块，10 个直接指针就是 10 个，1 个间接指针，是又指向了另一个索引块，该索引块 4KB，容纳 4KB/4B=1K 的地址，所以文件数据共占 10+1024
    ![[02王道题-1773748794152.jpeg]]
38. ？!!
    **注意，自己算出来是什么单位，是什么东西，答案问的是什么东西，不要光凭直接选答案！**。二级索引分配，一个盘块有 2^8 个地址，能算出一共有2^16 个“磁盘块”！但是要求文件长度，还需要乘上磁盘块的大小
39. ？
    和 38 一样，要乘磁盘块大小
40. ？
    用不用数组，是看有没有随机访问的需求。FAT 有随机访问的需求！回忆一下表的样子，要快速定位的！
41. A？
FCB 包含基本信息、存取控制信息、使用信息
42. B
43. C
    ![[02王道题-1773749724253.jpeg]]
    掌握了 363738，这题一样的
44. <font color="#ff0000">D?</font>
    open 要求文件路径与文件名，open 流程是什么？为何 read 只需要 open 返回的文件描述符，read 所需参数：fd、buf 首地址、传送字节数，怎么理解
    `open("/a/b/c.txt")`，操作系统先去磁盘找根目录 `/`，在里面按名字搜索，找到 `a` 目录的 Inode；去磁盘读 `a` 的数据块，在里面搜索 `b`；再去读 `b` 的数据块，最后终于在里面找到了 `c.txt` 的 Inode 编号。**（这里发生了大量的磁盘 I/O）**
    操作系统把 `c.txt` 的 Inode 从磁盘复制到内存的**“系统级打开文件表”**中。
    操作系统在当前进程的**“进程级打开文件表”**里找一个空闲的格子（比如第 3 号格子），让它指向系统表里的那个 Inode。
    操作系统把数字 `3` 返回给你。这个 `3` 就是**文件描述符（fd）**！
    当你调用 `read(fd, buf, size)`，也就是 `read(3, buf, 100)` 时：
    它直接去当前进程的表里找第 `3` 个格子。顺着指针瞬间找到内存里的 Inode。
    Inode 里不仅有文件的物理块地址，还有你现在的**“读写指针（当前读到第几个字节了）”**。
    
    数据不在内存，进程会发生什么？磁盘的速度太慢了（毫秒级），而 CPU 太快了（纳秒级）。为了不让 CPU 傻傻地在那儿原地打转，操作系统会立刻剥夺该进程的 CPU 使用权，把它从“运行态”**切换到**“阻塞态/睡眠等待状态
    
    内核在核心态下检查你的权限，去查**系统打开文件表**，然后指挥磁盘去读数据。数据读完后，内核把数据从“内核缓冲区”拷贝到你的“用户区缓冲区”，最后再切换回**用户态**，把控制权还给你的程序。因此，任何系统调用（`open`, `close`, `read`, `write`）都伴随着一次雷打不动的 **用户态 $\rightarrow$ 核心态** 的切换。
    
45. A
46. A??
    ![[02王道题-1773818517405.jpeg]]
    单个文件长度，是指假如文件有 n 个文件块，一个文件块的大小为 x，n x 就为长度
    索引节点（Inode）的总数，决定的是这块硬盘上最多能存多少个“文件”（文件数量的上限），而不是“某一个文件能有多大”。哪怕你硬盘上只有 2 个 Inode（只能存 2 个文件），只要你级数够多、块够大，这单个文件依然可以塞满整个硬盘！
47. <font color="#ff0000">D</font>
    要快速随机，选连续结构
48. B
49. ?
    ![[02王道题-1773749894129.jpeg]]
    本题我也忘了原来为啥会错。注意这个真题，考场是也有可能有点大计算的（虽然也只是算一算 256 乘 1024，但就是说，不要以为出现大计算了就思路错误了）
50. <font color="#ff0000">A?</font>
    ![[02王道题-1773817612552.jpeg]]
    ![[02王道题-1773817593632.jpeg]]
    这串二进制是存在**文件**的 FCB 里的！文件是一个没有感情的机器，它不可能只记住“某一种人”的“某一种权限”。它必须在自己的脑门上，把**所有人**的**所有权限**全部刻下来！随时等别人来查。
    权限是“同时并存”的，不是“单选”的，5 个权限，就要 5 位
    4 类用户的权限必须“同时存在”，在 FCB 的权限位串里，这 4 类用户是横向排开、各自占领独立地盘的：$$\text{总位数} = 4 \text{ 类用户} \times 5 \text{ 位/类} = 20 \text{ 位}$$
51. D
    我没搞懂题意，求磁盘空间大小？不就应该是文件大小吗？
    文件以簇为单位分配大小，物理空间的分配必须以“簇（或块）”为最小单位。哪怕你只写了 1 个字节，系统也必须批给你一整簇。所以考点是，刻意混淆你扇区跟文件有关吗（无关！扇区是硬件单位，多个扇区合并，定义为簇），然后计算要多少个簇，不一定精准分配完！有碎片是正常的
52. B
    创建文件数量上限=索引节点数量上限
53. B
54. A
55. B
56. A

# 目录
1. 
   ![[02王道题-1773819454005.jpeg]]
   散列（Hash）确实快（$O(1)$），但它有致命缺点：会有哈希冲突，且目录文件变大时动态扩容非常麻烦。更重要的是，散列**不支持模糊查找（比如 `*.txt`）**。所以现代操作系统并没有“都用它替代”，而是混合使用（比如少量文件用顺序检索，大型文件系统如 NTFS/Ext4 喜欢用 **B+ 树 / B 树**来检索
   要找 `/usr/bin/gcc`。如果系统在根目录里连 `usr` 这个文件夹都没找到，那后面的 `bin` 和 `gcc` 根本不用找了，直接报错“找不到路径”。这就叫“只要一个分量名未找到，就停止查找”。
   在 UNIX/Linux 中，目录检索完成后，你拿到的仅仅是**“索引节点号 (Inode Number)”**！你必须再拿着这个号去 Inode 表里查一次，才能拿到真正的“物理地址”。**
   在树形目录中，存在“当前目录”的定义，所以不一定每次从根目录按文件路径找
2. A
3. <font color="#ff0000">D</font>
   多级目录结构的目的，是解决命名冲突。难道没有缩短时间/减小开销吗？哦，好像**反而增加**了，多级目录和索引不同，是不能随机访问的，所以级数越多开销越大
   你要用绝对路径查 `/a/b/c/d/e.txt`，你必须发生 **5 次** 目录文件的磁盘 I/O！读 `/`，读 `a`，读 `b`，读 `c`，读 `d`。级数越深，**寻址的 I/O 代价呈线性暴增！**
4. A
5. D
   对文件的访问，由用户访问权限和文件属性共同约束
6. 符号链接、软链接、硬链接辨析一下
   建立 F1 的硬链接 F2 时
   引用计数（硬链接数）是死死刻在 Inode 里面的，跟文件名没有任何关系！所谓的“F1 和 F2 的引用计数”，本质上是**同一个计数器被读取了两次**。并不存在一个属于 F2 自己的、孤零零的计数器
   
   符号链接就是软连接，软链接是真正意义上的“自立门户、重新开户”创建软链接 F3- **对 F1 没有任何影响**：F1 对应的 88 号 Inode 的计数**不会增加**。
   **F3 自己的引用计数是 1**：因为它是一个独立的、刚出生的新文件。
7. <font color="#ff0000">D</font>!!
   F1 和 F2 的 Inode 是同一个，所以**文件本身**设定的读写权限绝对是一样的。
   **但访问权限是双向的！** 还要看**“是谁在访问”**！如果进程 P1 是超级管理员（root）运行的，而进程 P2 是一个无名小卒（guest）运行的。那 P1 可以随意读写，P2 可能连看的资格都没有。所以，不同进程的访问权限不一定相同。
   ![[02王道题-1773820772374.jpeg]]
8. D?
   只要文件被打开了，以后用户都不再使用文件名而使用文件描述符, 不管采用什么样式的目录
9. B?
   影响检索目录的平均次数为：目录项数量、大小、位置
10. D
11. B
12. C
13. <font color="#ff0000">A</font>
    光写了找到目录项所需 IO，但读入索引节点还需一次 IO!
14. B
15. C
16. B
17. <font color="#ff0000">D</font>
    删除文件不会导致快捷方式被删除


# 文件系统
1. <font color="#ff0000">D?</font>
   从用户角度看，文件系统的意义只是能够按名存取文件
   不然，就写存在硬盘第 3 张盘片的第 12 磁道第 8 扇区
   除此之外，硬件大一统（抽象化）、磁盘分配与回收、保护与共享、持久化与一致性也是其功能
2. <font color="#ff0000">A?</font>!!
   逻辑文件系统的功能：对文件按名存取，文件目录组织管理，文件名转换为文件描述符、存储保护，
3. A?
   利用磁盘阵列技术，一个文件系统可以将数据存放到多个磁盘上
4. <font color="#ff0000">D?</font>
   UNIX，文件的索引结构放在索引节点里
   ![[02王道题-1773824766921.jpeg]]
5. C
6. <font color="#ff0000">A</font>
   **提高存储空间的利用率（选 D）**。因为用户的空间是很宝贵的，操作系统要想尽办法（比如把文件切碎成块、用多级索引）把犄角旮旯的碎片都利用起来，绝对不能浪费。
   **提高换入换出速度（错选 B 的迷惑项）**。为了速度，对换区通常采用**连续分配**方式，宁可浪费一点空间，也绝对不允许磁头到处寻道乱跑。
   文件区要的是“省空间”（精打细算），对换区要的是“拼速度”（简单粗暴）。
   ![[02王道题-1773824926394.jpeg]]
7. B
8. <font color="#ff0000">A?</font>
   成组链接法将空闲盘块分了若干组，每组的第一个盘块记录下一组的空闲盘块总数和空闲盘块号，第一组的空闲盘块总数和空闲盘块号存放在内存的专用栈
9. B？!
   主引导扇区 / 主引导块指硬盘上的那**第 0 个扇区**本身。它是一个容器。叫 Master Boot Sector.
   主引导程序是 MBR 容器里装的一段**最初始的可执行代码**. 硬盘分区表样装在 MBR 容器里（占 64 字节）。它记录了这块硬盘被切成了几个分区，每个分区从哪个扇区开始、到哪个扇区结束。
   操作系统所在扇区，叫引导扇区。装在上面那个“分区引导扇区”里的一段代码。不同的操作系统（Windows、Linux）会在自己的分区大门口装不同的看门人。是分区的引导程序
   ![[02王道题-1773825105279.jpeg]]
10. B
11. <font color="#ff0000">B?</font>!!
    空闲表法非常适合**连续分配方式**。因为表格里明明白白写着哪块区域有多大连续空间。空闲链表法，若是空闲盘块链（以“块”为单位串联）**极难获得连续空间**
    ![[02王道题-1773825933439.jpeg]]
12. D?
13. A
14. <font color="#ff0000">B</font>
    从 0 开始编号！
    然后“盘块号”，在位示图的语境下就是第几块第几块，一个 bit 代表一块。盘块大小 1024B，就说明能代表 1024B 个块。要修改盘块号为 409612 的块，对应位 shi 图的哪？$$409612 \div 8 = 51201 \dots \dots 4$$$$51201 \div 1024 = 50 \dots \dots 1$$
15. <font color="#ff0000">C</font>
    你想想 FAT 表长什么样？它是一张覆盖全磁盘的“全局大网”。 如果物理块 88 是空闲的，FAT 表的第 88 行填的是什么？**填的是一个特殊的标记（比如 0，或者 FREE）**。 当操作系统需要找空闲块时，它怎么找？**它直接从头遍历内存里的 FAT 表，谁的格子里填的是 0，谁就是空闲块！** 所以，**FAT 表天然就兼职了“空闲表”的功能！** 有了 FAT，系统根本不需要再额外建什么空闲链表或位示图，一张 FAT 表把“文件链”和“空闲块”全包圆了
16. <font color="#ff0000">D</font>
    注意，计算出的是 2^22b，要化为 B
17. A