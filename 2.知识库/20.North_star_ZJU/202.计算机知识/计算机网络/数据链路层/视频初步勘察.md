SDU就是IP数据报？

帧？ 就是我们给IP数据报，包装的，首部尾部两个以太网帧嘛

不对，应该是整体，IP数据报+首尾以太网帧，叫做“帧”这个单位

然后数据链路层 主体功能，是包装，包装完，传输“帧”

传输的话，有可能“帧”内部有位错，有可能“帧”整体当成个体，放到海量的传输中，出错，比如俩个帧的顺序错了等

再就是流量控制
介质访问控制


那么，总流程就是这些，其中的知识点呢？

- 帧是怎么组成的？ 怎么解析的 。 <font color="#f79646">对应各种填充法</font>
- 帧内部组成，翻译到物理层是01，如果有意外让某个0突变1了，也就是出错了，即位错，怎么检查纠错？<font color="#f79646"> 对应两种检错编码一种纠错编码</font>
- 如果是帧错，即帧丢失、重复、失序，怎么处理<font color="#f79646">对应流量控制、可靠传输、滑动窗口机制。有点类似TCP传输机制那里。对应协议：停止等待、后退N帧、选择重传</font>
- 介质访问，是总线型，如何解决信号冲突? <font color="#f79646">对应信道划分、随机访问、轮询访问三个方法</font>
	- 对于信道划分，<font color="#f79646">分为时分复用和其Plus版，是分配时间；还有频分复用和兄弟波分复用；还有码分复用</font>
	- 对于随机访问，<font color="#f79646">分为超多的协议，每一个协议都是知识点，他们都描述的“以发送数据方视角，如何管理自己的发送”</font>
	- 对于轮询访问，是令牌传递协议  不是重点 


然后这个章节，又有一个，稍微独立点，与上面描述过程没有很大关联的知识：局域网

- 局域网的概念
- 以太网 IEEE 802.3
- VLAN 
- IEEE 802.11无线局域网 
- 以太网交换机


