# 网络层的功能
1. C
   **同构/异构”的判定标准是“协议体系（分层协议）”，而不是“物理几何形状（拓扑结构）”**
2. D?
   拥塞的根据是，随着通信子网负载增加，吞吐量反而降低
3.  <font color="#ff0000">B？</font>!!
   路由器向传输层及以上层隐藏下层的具体实现，物、数据链路、网络层协议可以不同。但网络层以上的高层必须相同
4. ？
   路由器的路由表，就是路由转发表吧。包含的是目的网络和到达该目的网络路径上的下一个路由器 IP 地址
5. C
6. <font color="#ff0000">C</font>
   A 路由器机制是存储转发机制
7. B
8. D
9. ,
   虚电路一旦建立，所有分组都沿同一路径走。如果路径中某个结点或链路出故障，整条虚电路都会受影响。

	在故障率很高的网络中，更适合数据报方式。因为数据报每个分组可以独立选路，某条路径坏了，可以绕路。
10. ,
    虚电路是面向连接的，数据报是无连接的。
    数据报每个分组独立选路，不一定沿同一条路径，也不保证按序到达。
    电路建立后，分组中携带的是虚电路标识，比如 VCI，而不是每次都携带完整目的地址用于重新选路。
    虚电路中分组通常沿同一路径传输，一般按序到达。
11. D
12. D
13. D
    数据报服务中，每个分组都必须携带源地址和目的地址
    虚电路服务有临时性的有非临时性的，不一定会话结束就取消
14. ？
- 虚电路服务本身通常**由网络层提供可靠交付**。在虚电路网络（如传统的 X.25、ATM 虚通道）中，网络层节点（交换机/路由器）之间具备确认和差错重传机制，直接由虚电路协议本身保证数据的可靠性与有序性，无需依赖上层（如传输层）协议。
- **对比数据报**：数据报网络（如 IP 网络）的网络层只提供尽最大努力交付（无连接、不可靠），因此**数据报才真正依赖上层协议（如传输层的 TCP）来实现差错控制与可靠传输**。

- 只有在**建立连接阶段**的呼叫请求分组中才需要携带完整的全局目的地址（用于寻路）。连接一旦建立完毕，后续传输的数据分组首部**不再包含全局目的地址，仅携带一个局部的“虚电路号（VCID / VCI）”**（开销极小）。中间节点查表时只需根据 <入端口, 入VCID> -> <出端口, 出VCID> 转发。
- **对比数据报**：数据报方式中，每个分组都是独立的，因此**每个数据报首部都必须包含完整的全局源 IP 和目的 IP 地址**。


1. **电路交换的思想**：通信前先建立逻辑连接，路径预先确定，所有分组沿固定路径传输，天然保证了**按序到达**，无乱序重组时延；
    
2. **分组交换的本质**：物理链路上依然采用统计时分复用（分组传输），不独占物理带宽，利用率高；
    
3. **适合场景**：虽然有建立连接的初期时延，但在长时间、连续传输时，中间节点无需为每个分组重新计算路由（只需匹配 VCID 快速转发），转发时延低且稳定，实时性更好。


**多站点同时使用一段物理链路实行虚电路交换会产生冲突，无法正常通信**

- **错误原因**：虚电路本质上仍然是**分组交换**，采用**统计时分复用（STDM）**。同一条物理链路上可以同时承载成百上千条不同的逻辑虚电路。不同站点的数据分组交替在链路上传输，依靠分组首部不同的**虚电路号（VCID）**相互区分，不会发生冲突。

15. C
16. C
17. D


# IPV4

1. C
2. B
3. C
4. ?
5. D
6. D
7. A
8. D
9. D
10. ?
    MTU
	- **全称**：Maximum Transmission Unit
	- **含义**：一条链路上能发送的**最大帧或数据报长度**（字节数）。
	- **用途**：如果 IP 数据报比链路 MTU 大，需要**分片**；每个分片的大小不超过 MTU。
	每个 IP 数据报都有唯一标识。当数据报被分片时，所有分片的“标识”都一样，方便目的主机重新组装。
	
	**DF (Don't Fragment，第 2 bit)**：禁止分片

	- DF = 1 → 不能分片，如果数据报超过链路 MTU，会丢弃并返回 ICMP 报文
	  
	  **MF (More Fragments，第 3 bit)**：还有更多分片
	  
	
11. C
12. ？
    注意最长匹配原则
13. ，
    IP 规定，C 类地址前 24 位网络位，后 8 位主机位 
14. A
15. ？
16. D
17. ？
    15-18 都是[特殊IP](卡片/特殊IP.md)
18. ?
19. ？
        255.255.240.0  
	= 11111111.11111111.11110000.00000000
	
	前面一共有：
	
	8 + 8 + 4 = 20 bit
	
	所以它是：
	
	/20
20. A
21. <font color="#ff0000">D</font>
    子网划分能增加网络数量，是结果而不是目的。 
22. 
![[02王道题-1777714373608.jpeg]]
23. ？
    主机可以有多个 IP, 但一定是不同类网络
24. A
25. D
26. B
27. DA
28. B
29. C
30. .。
31. <font color="#ff0000">D</font>
    目的主机重组分片
32. C
33. D
34. ?
35. <font color="#ff0000">D</font>
36. ?
37. A
    为传统 IPv4 子网里，通常有两个地址不能给主机用：
	
	主机号全 0：网络地址  
	主机号全 1：广播地址
	新标准允许：
	
	/31 的两个地址都可以作为接口地址使用
		
	
38. <font color="#ff0000">D</font>
39. D
40. ？
    ![[02王道题-1777880727389.jpeg]]
41. ？
    硬件地址只有本地意义
42. A
43. B
44. C
45. ？
46. C
47. D
48. A
49. C



# IPv6

1. D
2. D
   IPv6 的首部
3. C
4. D
5. D
6. A
7. ?

# 路由算法
1. B
2. B
3. A
4. B
5. <font color="#ff0000">?</font>
   采用分层路由后，每个路由器不知道如何去路由到其他区域
6. <font color="#ff0000">C?</font>
   采用分层划分区域的方法使交换信息的种类增多，OSPF 协议更加复杂。
7. D?
   路由表能知道下一条 IP，MAC 不知道，MAC 是由 ARP 实现的
8. B
9. A
10. D
11. C
12. A
13. C
14. A
15. <font color="#ff0000">B</font>
16. A
17. B
18. D
19. A
20. D
21. 需要记忆
22. B
23. ,
24. D
25. C
26. 
    ![[02王道题-1778051737763.jpeg]]
27. D

# 多播 IP
1. ？
2. ？
3. ？
4. ？


# 移动 IP
1. ？


# 网络层设备
1. <font color="#ff0000">D</font>
2. C
3. C
   路由器可分割广播域
4. C
5. ？
   路由器是网络层，就要处理网络层及以下的功能
6. C
7. D
8. C
9. C
10. D
11. D
12. D
13. D
14. C
    ![[02王道题-1778057754929.jpeg]]
15. C
16. D
17. A
18. C
19. ?
20. C
    路由器也有差错检验 
21. C