# 网络层的功能
1. C
   **同构/异构”的判定标准是“协议体系（分层协议）”，而不是“物理几何形状（拓扑结构）”**
2. D?
   拥塞的根据是，随着通信子网负载增加，吞吐量反而降低
3.  <font color="#ff0000">B？</font>!!
   路由器向传输层及以上层隐藏下层的具体实现，物、数据链路、网络层协议可以不同。但网络层以上的高层必须相同
4. ？
   路由器的路由表，就是路由转发表吧。包含的是目的网络和到达该目的网络路径上的下一个路由器 IP 地址
5. C
6. <font color="#ff0000">C</font>
   A 路由器机制是存储转发机制
7. B
8. D
9. ,
   虚电路一旦建立，所有分组都沿同一路径走。如果路径中某个结点或链路出故障，整条虚电路都会受影响。

	在故障率很高的网络中，更适合数据报方式。因为数据报每个分组可以独立选路，某条路径坏了，可以绕路。
10. ,
    虚电路是面向连接的，数据报是无连接的。
    数据报每个分组独立选路，不一定沿同一条路径，也不保证按序到达。
    电路建立后，分组中携带的是虚电路标识，比如 VCI，而不是每次都携带完整目的地址用于重新选路。
    虚电路中分组通常沿同一路径传输，一般按序到达。
11. D
12. D
13. D
    数据报服务中，每个分组都必须携带源地址和目的地址
    虚电路服务有临时性的有非临时性的，不一定会话结束就取消
14. ？
- 虚电路服务本身通常**由网络层提供可靠交付**。在虚电路网络（如传统的 X.25、ATM 虚通道）中，网络层节点（交换机/路由器）之间具备确认和差错重传机制，直接由虚电路协议本身保证数据的可靠性与有序性，无需依赖上层（如传输层）协议。
- **对比数据报**：数据报网络（如 IP 网络）的网络层只提供尽最大努力交付（无连接、不可靠），因此**数据报才真正依赖上层协议（如传输层的 TCP）来实现差错控制与可靠传输**。

- 只有在**建立连接阶段**的呼叫请求分组中才需要携带完整的全局目的地址（用于寻路）。连接一旦建立完毕，后续传输的数据分组首部**不再包含全局目的地址，仅携带一个局部的“虚电路号（VCID / VCI）”**（开销极小）。中间节点查表时只需根据 <入端口, 入VCID> -> <出端口, 出VCID> 转发。
- **对比数据报**：数据报方式中，每个分组都是独立的，因此**每个数据报首部都必须包含完整的全局源 IP 和目的 IP 地址**。


1. **电路交换的思想**：通信前先建立逻辑连接，路径预先确定，所有分组沿固定路径传输，天然保证了**按序到达**，无乱序重组时延；
    
2. **分组交换的本质**：物理链路上依然采用统计时分复用（分组传输），不独占物理带宽，利用率高；
    
3. **适合场景**：虽然有建立连接的初期时延，但在长时间、连续传输时，中间节点无需为每个分组重新计算路由（只需匹配 VCID 快速转发），转发时延低且稳定，实时性更好。


**多站点同时使用一段物理链路实行虚电路交换会产生冲突，无法正常通信**

- **错误原因**：虚电路本质上仍然是**分组交换**，采用**统计时分复用（STDM）**。同一条物理链路上可以同时承载成百上千条不同的逻辑虚电路。不同站点的数据分组交替在链路上传输，依靠分组首部不同的**虚电路号（VCID）**相互区分，不会发生冲突。

15. C
16. C
17. D


# IPV4

1. C
2. B

| 层次                | 首部中的关键字段          | 字段长度     | 指明上一层的谁？ | 常见值                       |
| ----------------- | ----------------- | -------- | -------- | ------------------------- |
| **链路层 (MAC 帧)**   | **类型 (Type)**     | **2 字节** | 网络层协议    | 0x0800 (IP), 0x0806 (ARP) |
| **网络层 (IP 数据报)**  | **协议 (Protocol)** | **1 字节** | 传输层协议    | 6 (TCP), 17 (UDP)         |
| **传输层 (TCP/UDP)** | **目的端口号**         | **2 字节** | 应用层进程    | 80 (HTTP), 53 (DNS)       |
3. C
4. ?
   > **C 选项表述**：“若网络层发现检验和错误，则丢弃该 IP 分组**并发送 ICMP 差错报文**。” 
>  **错误！**

错误原因剖析（408 经典逻辑陷阱）：

源 IP 地址已不可信**：

- 首部检验和**只校验 IP 首部**。
	
- 既然检验和算出来出错了，说明 **IP 首部的某一个或多个比特发生了损坏**。
	
- 你**无法确定损坏的是哪个字段**——很可能**源 IP 地址字段本身就已经损坏变质了**。
	
**引发灾难性后果**：

- 如果要发送 ICMP 报文，路由器必须将 ICMP 报文的目的地址填为该损坏分组的“源 IP 地址”。
	
- 如果源 IP 已损坏，路由器发出的 ICMP 报文就会发给一个**完全无辜的第三方主机**，或者发给一个不存在的地址，这会在网络中制造垃圾流量甚至引起广播风暴。
	
**协议硬性规定（408 结论）**：

- 当路由器或主机收到 **首部检验和出错** 的 IP 分组时，**唯一的动作是静默丢弃（Silently Drop）**，**绝不发送任何 ICMP 差错报告**。

5. D
6. D
7. A
8. D
9. D
10. ?
    MTU
	- **全称**：Maximum Transmission Unit
	- **含义**：一条链路上能发送的**最大帧或数据报长度**（字节数）。
	- **用途**：如果 IP 数据报比链路 MTU 大，需要**分片**；每个分片的大小不超过 MTU。
	每个 IP 数据报都有唯一标识。当数据报被分片时，所有分片的“标识”都一样，方便目的主机重新组装。
	
	**DF (Don't Fragment，第 2 bit)**：禁止分片

	- DF = 1 → 不能分片，如果数据报超过链路 MTU，会丢弃并返回 ICMP 报文
	  
	  **MF (More Fragments，第 3 bit)**：还有更多分片
当 DF = 1 且报文长度超过链路 MTU 时，路由器丢弃该分组后，**必须向源主机发送 ICMP 差错报文进行报告**
由器会生成一个 **ICMP 差错报告报文**，将其装入一个新的 IP 数据报中，目的 IP 填入原数据报的“源 IP”，发回给发送端。
11. C
12. ？
    注意最长匹配原则。
    以及，**拿数据报里的“目的 IP 地址”与路由表项里的“子网掩码”进行按位与，再拿得到的结果与该表项的“目的网络前缀”进行比对。**
13. ，
    IP 规定，C 类地址前 24 位网络位，后 8 位主机位 

|               |                       |                                                             |                     |            |            |
| ------------- | --------------------- | ----------------------------------------------------------- | ------------------- | ---------- | ---------- |
| IP 地址形式       | 实例（针对 192.168.1.0/24） | 准确名称与作用                                                     | 能否作为源 IP？           | 能否作为目的 IP？ | 能否配给主机/网关？ |
| **主机号全 0**    | 192.168.1.0           | **网络地址**（代表该网络本身）                                           | ❌ 不能                | ❌ 不能       | ❌ 不能       |
| **主机号全 1**    | 192.168.1.255         | **定向广播地址**（广播给该网段所有人）                                       | ❌ 不能                | **✔ 能**    | ❌ 不能       |
| **32 位全 0**   | 0.0.0.0               | 1. 源地址：**本网上的本主机**（DHCP 启动时用）<br>2. 路由项：**默认路由（0.0.0.0/0）** | **✔ 能**（仅限无 IP 时启动） | ❌ 不能       | ❌ 不能       |
| **32 位全 1**   | 255.255.255.255       | **受限广播地址**（仅限本地链路广播，路由器不转发）                                 | ❌ 不能                | **✔ 能**    | ❌ 不能       |
| **127.x.x.x** | 127.0.0.1             | **环回地址（Loopback）**（用于本机进程间通信和自测）                            | **✔ 能**             | **✔ 能**    | ❌ 不能用于外部通信 |
|               |                       |                                                             |                     |            |            |
- **默认网关（Default Gateway）**：
    
    - 它是连接当前局域网与外网的**路由器接口的真实 IP**。
        
    - 它必须是当前网段内的一个**合法主机 IP**（工程中习惯配成第 1 个或最后 1 个可用地址，如 192.168.1.1 或 192.168.1.254）。
        
- **默认路由（Default Route）**：
    
    - 它是路由表里的一条规则：当所有具体路由都匹配不上时，走这条兜底路由。
        
    - 它的**目的网络写为全 0**（即 0.0.0.0/0），而它的**下一跳填的就是默认网关的真实 IP**
一、语境 1：在真实发送的 IP 数据报首部中

在网络上跑的每一个真实 IP 数据报中，首部都有【源 IP 地址】和【目的 IP 地址】两个字段：

1. **作为源 IP（合法）**：
    
    - 当一台电脑刚开机，还没有分配到 IP 时（例如向 DHCP 服务器申请 IP），它不知道自己是谁，此时发送的 DHCP Discover 数据报中：
        
        - **源 IP 填 0.0.0.0**（含义：本网络上的本主机，目前暂无 IP）。
            
        - **目的 IP 填 255.255.255.255**（受限广播）。
            
2. **作为目的 IP（绝对非法）**：
    
    - 网络上**绝不可能**出现一个目的 IP 为 0.0.0.0 的数据报。你永远无法把数据发送给 0.0.0.0。
        
    - 所以表格中写的是：**在数据报首部中，0.0.0.0 只能出现在源 IP 字段，绝不能出现在目的 IP 字段。**
        

---

 二、 语境 2：在路由器内部的“路由表”中

路由表是路由器用来查路的字典，路由表中的每一行记录有三个主要字段：  
[ 目的网络前缀 | 子网掩码 | 下一跳 ]

在路由表里，**默认路由（Default Route）** 的记录长这样：

- **目的网络前缀**：0.0.0.0
    
- **子网掩码**：0.0.0.0（简写为 0.0.0.0/0）
    
- **下一跳**：192.168.1.1（默认网关 IP）
    

#### 这里的 0.0.0.0/0 是怎么工作的？

它不是指具体某台机器，而是一个**数学上的“万能通配符”**。

根据前面讲过的查表匹配公式：

```
目的 IP & 子网掩码目的网络前缀目的 IP & 子网掩码=?目的网络前缀
```

- 假设来了一个要去往百度的数据报，其真实目的 IP 是 202.108.22.5。
    
- 路由器拿这个目的 IP 与默认路由的掩码相与：  
    
    ```
    202.108.22.5 & 0.0.0.0=0.0.0.0202.108.22.5 & 0.0.0.0=0.0.0.0
    ```
    
- 计算结果是 0.0.0.0，与默认路由记录的目的网络前缀 0.0.0.0 **完全相等**！
    

**结论**：因为任何 32 位二进制数与全 0 相与，结果永远等于全 0。所以，**任何 IP 数据报只要前面的具体路由都没匹配上，都必定会 100% 匹配中这条默认路由**，被送给下一跳（默认网关）。

---



- **数据包首部**里的 0.0.0.0：只能当**源 IP**（表示我还没 IP）。
    
- **路由表条目**里的 0.0.0.0/0：代表**通配所有目的网络**（表示所有未明路径的兜底出口）。


14. A
15. ？!
    ![[_attachments/image/02王道题-1787126800996.jpeg]]
非 CIDR 地址块时，注意 D 类是多播，224--239
![[_attachments/image/02王道题-1787126960790.jpeg]]
16. D
17. ？
    15-18 都是[特殊IP](卡片/特殊IP.md)
18. ?
19. ？
        255.255.240.0  
	= 11111111.11111111.11110000.00000000
	
	前面一共有：
	
	8 + 8 + 4 = 20 bit
	
	所以它是：
	
	/20
20. A
21. <font color="#ff0000">D</font>
    子网划分能增加网络数量，是结果而不是目的。 
22. 
![[02王道题-1777714373608.jpeg]]
23. ？
    主机可以有多个 IP, 但一定是不同类网络
24. A
25. D
26. B
27. DA
28. B
29. C
30. .。
31. <font color="#ff0000">D</font>
    目的主机重组分片
32. C
33. D
34. ?
35. <font color="#ff0000">D</font>
36. ?
37. A
    为传统 IPv4 子网里，通常有两个地址不能给主机用：
	
	主机号全 0：网络地址  
	主机号全 1：广播地址
	新标准允许：
	
	/31 的两个地址都可以作为接口地址使用
		
	
38. <font color="#ff0000">D</font>
39. D
40. ？
    ![[02王道题-1777880727389.jpeg]]
41. ？
    硬件地址只有本地意义
42. A
43. B
44. C
45. ？
46. C
47. D
48. A
49. C



# IPv6

1. D
2. D
   IPv6 的首部
3. C
4. D
5. D
6. A
7. ?

# 路由算法
1. B
2. B
3. A
4. B
5. <font color="#ff0000">?</font>
   采用分层路由后，每个路由器不知道如何去路由到其他区域
6. <font color="#ff0000">C?</font>
   采用分层划分区域的方法使交换信息的种类增多，OSPF 协议更加复杂。
7. D?
   路由表能知道下一条 IP，MAC 不知道，MAC 是由 ARP 实现的
8. B
9. A
10. D
11. C
12. A
13. C
14. A
15. <font color="#ff0000">B</font>
16. A
17. B
18. D
19. A
20. D
21. 需要记忆
22. B
23. ,
24. D
25. C
26. 
    ![[02王道题-1778051737763.jpeg]]
27. D

# 多播 IP
1. ？
2. ？
3. ？
4. ？


# 移动 IP
1. ？


# 网络层设备
1. <font color="#ff0000">D</font>
2. C
3. C
   路由器可分割广播域
4. C
5. ？
   路由器是网络层，就要处理网络层及以下的功能
6. C
7. D
8. C
9. C
10. D
11. D
12. D
13. D
14. C
    ![[02王道题-1778057754929.jpeg]]
15. C
16. D
17. A
18. C
19. ?
20. C
    路由器也有差错检验 
21. C