

PC 相对寻址计算公式 (通用版)

Target = $$(PC_{\text{当前}} + \text{指令长度}) + \text{Offset}$$

重要变量解析：

1. 指令长度加多少？
	
	- 按字节编址：指令有几个字节，就加几。
	
	- 按字编址（本题情况）：指令有几个字，就加几。（本题 2 字节 = 1 字，所以 +1）。
2. Offset 是多少？

	- 注意看是补码还是原码（题目若是十六进制通常直接加）。

3. 最后还要加 1 吗？

	- **不加！** 算出来的结果就是最终 PC 的去向。