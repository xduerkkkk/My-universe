
PC自增规则：PC（程序计数器）按指令占用的存储单元数递增

pc存储指令地址，地址本身与指令大小无关，但"下一条指令地址"与指令大小有关   x020  x024    
地址的本质是 每一个地址都是一个存储指令内容的单位

pc是所有cpu都需要的

存放微指令的控制存储器隶属于cpu

在微程序控制中，把操作控制信号编成微指令。微程序控制比较慢但灵活，控制存储器用rom这断电仍保存的，微程序控制器upc与程序计数器pc并行

无条件跳转指令