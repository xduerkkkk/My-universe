无条件跳转指令的指令周期内 pc的值被修改次数是1
通用寄存器和pc对程序员可见
条件转移指令执行时 依据的条件来自程序状态字寄存器
条件转移指令都有哪些
译码器仅解析操作码。
仅内存操作数（`lw`/`sw`）需通过MDR，寄存器操作数直接由寄存器堆提供。

同一cpu周期内可并行执行的微操作为相容性微操作，不可并行执行的微操作为互斥性微操作

## 汇编程序员可见
可见的是通用寄存器（可直接改写）、pc（间接改写），不可见的是MAR、MDR、IR