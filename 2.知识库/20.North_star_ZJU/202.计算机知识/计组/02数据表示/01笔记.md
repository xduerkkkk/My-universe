# 定点数
n+1 位，有 1 位符号位
原码：$-(2^n-1)<=x<=2^n-1$ 
反码范围也是

但补码不是。
规定，+0 和-0 的补码都是 000000

100000（原来 -0 原码的样子），我们表示成-2^n

![[数值数据的表示-1774086016373.jpeg]]




![[数值数据的表示-1774248619128.jpeg]]

表示范围
![[数值数据的表示-1774248781747.jpeg]]







### IEEE
![[数值数据的表示-1748422458865.jpeg]]
负数补码表示

![[数值数据的表示-1748349383214.jpeg]]

IEEE还原呢？
展开二进制，看是不是正数（确定隐含最高位），第一位是0就是正数
接下来的8位，是阶码，不过要减去127才是真正阶码。剩下的23位，都是尾数，不包含隐含最高位的尾数！


## 浮点数表示范围


## 范围
原码表示范围 $-（2^n - 1） 到2^n -1$  11111  01111 共两倍的$2^n - 1$
原码的10000 是 -0
补码：  补码的 10000 是 $-2^n$ 所以就比原码多一个

小数：

![[数值数据的表示-1750261828452.jpeg]]

模值：最大减去最小 + 1

![[数值数据的表示-1750262484650.jpeg]]


# 加法器

- 输出=两个本位与一个进位，连续异或
- 进位：两个本位为 1，或者，两个本位中有一个是一，且进位是 1
- ![[笔记-1774334875561.jpeg]]
- 标志位：<span style="background:#b1ffff">OF、SF、</span> （有符号数）  ZF、**CF** （可判断无符号数是否溢出）





ALU 作用：
- 算数运算
- 逻辑运算

![[笔记-1774335165724.jpeg]]




无论算术右移还是逻辑右移，右移丢弃的位=1 的话，就会丢失精度



补码运算判断溢出：
法 1
![[笔记-1774337166352.jpeg]]
法 2，看两次进位
![[笔记-1774341577537.jpeg]]
法 3，双符号位


![[笔记-1774341993250.jpeg]]

# 乘除法

![[笔记-1774354731359.jpeg]]
无符号乘法：
乘积寄存器和乘数寄存器相接，被乘数寄存器为 ALU 的一端，
乘数寄存器的最右端决定了是加乘数还是啥也不干。 
然后个操作（加或不干）完成后，乘积+乘数寄存器整体逻辑右移一位，
一箭双雕，既实现了判断下一个操作（从右往左看乘数的下一个数），又实现了“累加”在乘积要往左移一位，

![[_attachments/image/笔记-1782721698479.jpeg]]
关于**有符号乘法（Booth 算法）**，它依然沿用你总结的“操作后整体右移一箭双雕”的框架，但判断规则的背后原理是：**把乘数看作多个连续的 1 组成的区块，利用“一段连续的 1 可以转换为一次减法和一次加法”的数学规律来计算。**

在二进制中，一段连续的 1，例如二进制的 `001110`（十进制 14），可以等价表示为 `010000 - 000010`（即 $16 - 2 = 14$）。
这意味着，当我们从右向左依次检查乘数时，不需要遇到 1 就加被乘数，而是可以**在连续 1 的开头减去被乘数，在连续 1 的结尾加上被乘数。**

图中引入一个**辅助位（$Y_{-1}$，初始值为 0）**，与乘数最低位（$Y_0$）组合起来，就是为了在从右向左移动的过程中，**探测“连续 1”的边界**。

原理具体拆解如下（设从右向左扫描，当前位为 $Y_0$，右侧移出的上一位为辅助位 $Y_{-1}$）：

*   **当 $Y_0 Y_{-1} = 10$ 时（遇到 1，上一位是 0）**：
    说明从右往左看，你**刚进入**一段连续的 1 的最右端（例如 `0111[1]0` 的括号处）。
    根据原理，在连续 1 的低位端应该执行减法。所以操作是：**乘积 P 减去被乘数 X（即 P + [-x]补）**。
*   **当 $Y_0 Y_{-1} = 11$ 时（遇到 1，上一位也是 1）**：
    说明你正处于一段连续的 1 的**内部**。
    由于刚进入这段 1 的时候已经做过减法了，内部的 1 不需要额外操作。所以操作是：**+0**。
*   **当 $Y_0 Y_{-1} = 01$ 时（遇到 0，上一位是 1）**：
    说明从右往左看，你**刚离开**一段连续的 1，进入了 0 的区域。
    根据原理，连续 1 结束的高位端应该执行加法（对应前面提到的 `+ 010000`）。所以操作是：**乘积 P 加上被乘数 X（即 P + [x]补）**。
*   **当 $Y_0 Y_{-1} = 00$ 时（遇到 0，上一位也是 0）**：
    说明你正处于一段连续的 0 的内部，没有遇到 1 的边界。所以操作是：**+0**。

**这就是图片左下角记忆技巧 `辅助位 - Y最低位` 的底层逻辑**：
$Y_{-1} - Y_0 = -1$（即 0 - 1），代表从 0 变 1，进入连续 1 区间，减 X。
$Y_{-1} - Y_0 = 1$（即 1 - 0），代表从 1 变 0，离开连续 1 区间，加 X。
$Y_{-1} - Y_0 = 0$（即 0-0 或 1-1），代表区间内部状态无变化，加 0。

**有符号乘法操作上的关键不同点（必须注意的前提）：**
在完成上述加减操作后，乘积寄存器 P 和乘数寄存器 Y 及辅助位整体向右移一位时，必须使用**算术右移**。因为当前寄存器 P 里存储的是带符号的部分积补码，右移时最高位必须复制自身的符号位（如果是负数最高位必须补 1），不能像无符号乘法那样无脑补 0，否则正负号就乱了。



![[笔记-1774422830017.jpeg]]

- **除数寄存器 B**：4bit，存放除数。
    
- **余数寄存器 R**：4bit，初始全为 0000。
    
- **商寄存器 Q**：4bit，初始存放**被除数**（例如 1011）。
    
- **拼接移位**：硬件上将 R 和 Q 拼接成一个 8bit 的整体 [R, Q]，并且只支持**逻辑左移**。
    

除法运算的“一箭双雕”，就是 **[R, Q] 整体向左移一位** 时同时完成的两件事：

1. **作用一（往前送数据）：将“被除数”的最高位，送入“余数寄存器 R”参与计算。**  
    当 [R, Q] 左移时，Q 的最高位会被挤入 R 的最低位。这在底层等价于你在纸上手算长除法时，**“把被除数的下一位数字拉下来”**。此时 R 里的数据就是当前需要和除数 B 进行大小比较的被除数部分。
    
2. **作用二（往后腾位置）：为新算出来的“商”，腾出存放空间。**  
    由于 Q 整体左移了一位，Q 的最低位必然空了出来。这个空位，**正好被用来直接写入当前这一步算出的商值（1 或 0）**。
    

之所以你会觉得除法“绕”，是因为乘法是“先算再移”，而**除法必须是“先移再算”**。

下面以**恢复余数法**为例，拆解一次完整的循环（4bit 除法严格执行 4 轮）：

**第 1 步：先移位（一箭双雕发力）**  
[R, Q] 整体逻辑左移一位。此时待计算的位进入了 R，同时 Q 的最低位空出（暂时为 0）。

**第 2 步：ALU 试探性减法**  
将余数寄存器 R 减去除数 B（底层是 R + [-B]补），结果写回 R。

**第 3 步：根据 ALU 结果的符号位，上商并决定是否恢复**

- **如果 R 结果的最高位（符号位）是 0**：  
    说明得到的余数是正数，**够减**。  
    硬件直接把刚才 Q 左移空出来的最低位改为 1（这就是“上商 1”）。
    
- **如果 R 结果的最高位（符号位）是 1**：  
    说明得到的余数是负数，**不够减**。  
    硬件把 Q 的最低位保持为 0（这就是“上商 0”）。  
    由于刚才把 R 减成了负数（破坏了原来的数据），必须立刻执行一次**恢复操作**：将 R 加上除数 B（即 R + B），把 R 变回减法之前的值。
    

**为什么这个机制极度精妙？**  
这是一个完美的**空间置换**过程。初始时，Q 里面塞满了 4bit 的被除数，没有地方放商。但在 4 轮循环中，被除数的数据一位一位地向左送进了 R 被消耗掉；同时，Q 的右侧一位一位地空出来，正好吃进了 4 轮产生出的 4bit 商。  
执行 4 轮结束后：Q 里原本的被除数全部消失，完全被新产生的“商”取代；而最后剩下的无法再减的数值，就稳稳地停在 R 里，成为最终的“余数”。
![[_attachments/image/笔记-1782722827350.jpeg]]
# 浮点数

$符号，尾数×基数^{阶码}$

![[笔记-1774509484952.jpeg]]
阶码加偏置，以及阶码后拼尾数，都是为了方便比较。



当发现存储的 8bit 阶码中，全是 1 或全是 0 时，是特殊的浮点数
![[笔记-1774513731735.jpeg]]
这张表看起来内容很多，但你**绝对不能横向一行一行去死记硬背**。

计算机底层的硬件根本没有这张表，硬件只有一套**“由粗到细的分类电路”**。你只需要按照硬件的判断逻辑，在脑子里建一棵**决策树**，这 6 种情况瞬间就能推导出来。

判断规则永远是两步走：**第一步看阶码（定大类），第二步看尾数（定明细）。**

---

### 第一大类：阶码 E = 全 1（255 或 2047）—— “超载异常区”
当阶码达到全 1 时，硬件的含义是：**“这个数太大了，或者发生了非法的数学运算，我已经无法用正常的浮点数表示了。”**

进入这个大类后，看尾数 `f` 究竟干不干净：

1.  **尾数 `f = 0`（干干净净）：无穷大（$\infty$）**
    *   **原理**：既然数字太大超出了上限，但尾数全 0 说明它是一个**“纯粹的超出极限”**。比如你在程序里算 `1.0 / 0.0`，数学上极限趋向于无穷。
    *   **区分正负**：看最高位的符号位。符号位是 0 就是正无穷（$+\infty$），是 1 就是负无穷（$-\infty$）。

2.  **尾数 `f \neq 0`（带有杂质）：非数（NaN, Not a Number）**
    *   **原理**：如果发生了完全无法定义数学意义的操作，比如 `0.0 / 0.0` 或者对负数开平方，结果根本就不是一个数。硬件为了标记这种**“乱码”**状态，就会把尾数 `f` 填上非 0 的乱七八糟的值。只要看到尾数不全为 0，统统判断为运算错误产生的 NaN。

---

### 第二大类：阶码 E = 全 0 —— “极小/归零区”
当阶码全是 0 时，硬件的含义是：**“这个数已经小到了极限，无法再用规格化数（隐藏的 1. xxx）来表示了。”**

同样，看尾数 `f`：

1.  **尾数 `f = 0`：真正的零（0）**
    *   **原理**：阶码全 0 且尾数全 0，说明什么都没有了，这就是 0。
    *   **注意细节**：IEEE 754 是保留了正负 0 的（$+0$ 和 $-0$），这完全取决于最高位的符号位。在底层的浮点比较器中，$+0$ 和 $-0$ 在数值上被判定为相等，但在某些高级求极限的除法中符号位会影响正负无穷的产生。

2.  **尾数 `f \neq 0`：非规格化数（核心难点，历年必考）**
    *   **原理**：正常的规格化浮点数，尾数前面总是隐藏着一个 `1.`，最小的正常数是 $1.0 \times 2^{-126}$。如果一个数比这个极限还要小怎么办？直接变成 0 就太粗暴了，会产生极大的精度断层。
    *   **机制转换**：当检测到 `E=0` 且 `f \neq 0` 时，硬件会自动**修改解释规则**：
        1. 默认隐藏的 `1.` 被强行改成了 **`0.`**。
        2. 指数被强行**固定为 -126**（单精度）或 -1022（双精度）。它不再使用“阶码 - 127”的公式去算（如果按公式算应该是 0 - 127 = -127，但绝对不能这么算）。
    *   **为什么指数固定在 -126？** 
        这是为了和平滑衔接。最小的正常数是 $1.000...0 \times 2^{-126}$。最大的非规格化数是 $0.111...1 \times 2^{-126}$。你看，它们的指数被设计成完全一样，这样数值大小就能顺滑地从规格化数过渡到非规格化数，直到逼近于 0。

---

### 给 408 考生的极简记忆树（考场直接默写）

遇到 IEEE 754 极值，按以下顺序提问：
1. **阶码全 1 吗？** -> 是异常情况。
   - 尾数全 0 吗？ -> 是，**无穷大**。
   - 尾数非 0 吗？ -> 是，**NaN（算错了）**。
2. **阶码全 0 吗？** -> 是极小情况。
   - 尾数全 0 吗？ -> 是，**这就是 0**。
   - 尾数非 0 吗？ -> 是，**非规格化数（强行变成 `0.f`，指数锁死 `-126`）**。

把这张图在脑海里转化成这个简单的两级 `if-else` 分支，你就彻底掌握了它的底层逻辑，再也不会记混。
## 取值范围
![[笔记-1774686796240.jpeg]]




![[笔记-1774514714503.jpeg]]



舍入，1 入 0 舍。
特殊情况是要丢弃的为 100，
若前一位是 1，则入 1. 是 0，则全舍


![[笔记-1774599703024.jpeg]]