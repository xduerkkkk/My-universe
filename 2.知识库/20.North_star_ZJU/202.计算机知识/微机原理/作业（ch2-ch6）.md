# ch2
![[作业-1766065808018.jpeg]]
### **如何把“运算器/控制器”映射到 8086 的 EU/BIU？**

408 计组讲的是**冯·诺依曼架构的逻辑功能划分**，而 8086 是**因特尔工程师的具体物理实现**。它们的对应关系如下：

1. **运算器 (ALU)**
    
    - **在 8086 里在哪？** 全都在 **EU (执行单元)** 里。
        
    - **原因**：EU 负责干活，算术逻辑运算单元 (ALU) 自然要在车间（EU）里。
        
2. **控制器 (CU)**
    
    - **在 8086 里在哪？** 被拆分了！
        
    - **指令译码逻辑**：在 **EU** 里。（负责看懂指令要干嘛）
        
    - **总线控制逻辑**：在 **BIU** 里。（负责控制什么时候去内存拿东西）
        
    - **原因**：8086 为了实现流水线（一边执行一边取指），把控制功能分成了“脑子”（EU译码）和“腿”（BIU取指）。
        
3. **寄存器**
    
    - **在 8086 里在哪？** 也被拆分了！
        
    - **干活用的寄存器** (AX, BX, CX, DX, SP, BP, SI, DI, FLAGS)：在 **EU** 里。（工人干活的工具包）。
        
    - **指路用的寄存器** (CS, DS, SS, ES, IP)：在 **BIU** 里。（物流部门的地图和导航仪）。
        
    - **原因**：BIU 需要计算物理地址（段地址+偏移），所以它必须拿着段寄存器和 IP。


### **第 3 题：2.6 寄存器分类与功能**

**题目**：8086/8088 微处理器内部有哪些寄存器？其主要作用是什么？

**【解析】**  
8086 共有 14 个 16位寄存器，分为三类。这道题是简答题的常客。

1. **通用寄存器 (8个)** —— EU 里的工具
    
    - **数据寄存器** (AX, BX, CX, DX)：
        
        - **AX** (Accumulator)：累加器，运算首选，I/O 专用。
            
        - **BX** (Base)：基址，**唯一能做指针的通用数据寄存器**。
            
        - **CX** (Count)：计数，串操作和 Loop 循环专用。
            
        - **DX** (Data)：数据，乘除法辅助，I/O 端口号专用。
            
    - **指针与变址寄存器** (SP, BP, SI, DI)：
        
        - **SP** (Stack Pointer)：栈顶指针，永远指向堆栈顶部。
            
        - **BP** (Base Pointer)：基址指针，常用于访问堆栈里的变量。
            
        - **SI** (Source Index)：源变址，串操作指源串。
            
        - **DI** (Destination Index)：目的变址，串操作指目的串。
            
2. **段寄存器 (4个)** —— BIU 里的地基
    
    - **CS** (Code)：代码段基址。
        
    - **DS** (Data)：数据段基址。
        
    - **SS** (Stack)：堆栈段基址。
        
    - **ES** (Extra)：附加段基址（串操作的目的地）。
        
3. **控制寄存器 (2个)**
    
    - **IP** (Instruction Pointer)：指令指针（计组里叫 PC），指向下一条要执行的指令偏移地址。
        
    - **FLAGS/PSW** (Program Status Word)：标志寄存器，存红绿灯（CF, ZF 等）和控制开关（IF, DF）。
        

---

### **第 4 题：2.7 寻址专用寄存器（考研重点）**

**题目**：IBM PC 有哪些寄存器用来指示存储器的地址？其中哪些可供在汇编语言程序中进行存储器寻址使用？

**【解析】**  
这道题问得有点绕，其实就是问：**“谁能存偏移地址？”** 和 **“谁能写在 [] 里？”**

**第一问：哪些寄存器指示存储器地址（存偏移地址）？**

- **IP**：指示**指令**的地址（配合 CS）。
    
- **SP**：指示**堆栈顶**的地址（配合 SS）。
    
- **BX, BP, SI, DI**：指示**数据**的地址（配合 DS, SS, ES）。
    

**第二问（核心）：哪些可在汇编中进行存储器寻址（能写在 [] 里）？**  
这就是我们之前做改错题时反复强调的**“四大金刚”**：

- **BX** (Base)
    
- **BP** (Base Pointer)
    
- **SI** (Source Index)
    
- **DI** (Destination Index)
    

**避坑指南（408 易错点）：**

- **AX, CX, DX** 虽然也是 16 位，但**不能**用来存地址（在 16 位汇编中），写 MOV AX, [CX] 是错的！
    
- **IP** 程序员**不能直接修改**（不能 MOV IP, ...），只能通过 JMP、CALL 间接改变。


---
### *回答你的疑惑：什么叫“配合 CS/SS”？**

在 8086 系统中，计算物理地址的公式你已经背过了：

> **物理地址 = 段地址 × 16 + 偏移地址**

但是，CPU 里有 4 个段寄存器（CS, DS, SS, ES），当我们只给出一个偏移地址（比如 MOV AX, [BX]，或者指令指针 IP）时，**CPU 怎么知道该乘哪一个段地址呢？**

这就有了**“默认配合”**（也叫默认段约定）：

1. **CS 配合 IP（代码区）**
    
    - **含义**：IP 指向的是**指令**。指令一定放在**代码段**（Code Segment）里。
        
    - **规则**：CPU 只要看到 IP，就自动去乘 CS。
        
    - 情景：去学校（CS）找教室（IP）。
        
2. **SS 配合 SP 和 BP（堆栈区）**
    
    - **含义**：SP（栈顶）和 BP（基址指针）指向的是**堆栈数据**。堆栈一定放在**堆栈段**（Stack Segment）里。
        
    - **规则**：只要操作数涉及 SP 或 BP（比如 PUSH, POP, MOV AX, [BP]），CPU 自动去乘 SS。
        
    - 情景：去食堂（SS）找盘子（SP）。
        
3. **DS 配合 BX, SI, DI（数据区）**
    
    - **含义**：这三个通常用来指向**变量**。变量默认放在**数据段**（Data Segment）里。
        
    - **规则**：只要用 [BX], [SI], [DI] 访问内存，CPU 默认去乘 DS。
        
    - 情景：去宿舍（DS）找人（BX）。
        
4. **ES 配合 DI（串操作专用）**
    
    - **含义**：只有在做串操作（如 MOVSB）时，目标地址 DI 才会强制配合 ES。
        

**总结表格（背这个就行）：**

|   |   |   |
|---|---|---|
|偏移地址由谁提供？|**默认配合的段寄存器**|也就是去哪里找？|
|**IP** (取指令)|**CS**|代码段|
|**SP** 或 **BP**|**SS**|堆栈段|
|**BX**, **SI**, **DI**|**DS**|数据段|



### **第 5 题：2.11 标志位计算（重点！）**

**题目**：将十六进制数 **62A0H** 与下列各数相加，求结果及标志位 CF, AF, SF, ZF, OF, PF。  
我们挑打勾的 **(1)** 和 **(6)** 来讲，这两个最典型。

**【预备知识：标志位怎么看？】**

- **CF (Carry)**：最高位进位（无符号数溢出）。
    
- **OF (Overflow)**：**有符号数**溢出（正+正=负，负+负=正）。
    
- **SF (Sign)**：结果的最高位是啥，它就是啥（0正1负）。
    
- **ZF (Zero)**：结果是0吗？是0则为1。
    
- **PF (Parity)**：结果的**低8位**里，1的个数是偶数吗？是偶数则为1。
    
- **AF (Auxiliary)**：第3位向第4位有进位吗？（低半字节进位）。
    

---

#### **(1) 62A0H + 1234H**

**第一步：列竖式计算**

codeText

```
6 2 A 0
+   1 2 3 4
-----------
    7 4 D 4
```

**结果**：**74D4H**

**第二步：分析标志位**

1. **CF (进位)**：最高位 6+1=7，没进位。 -> **CF = 0**
    
2. **SF (符号)**：结果最高位是 7 (0111)，首位是 0 (正数)。 -> **SF = 0**
    
3. **ZF (零)**：结果不是0。 -> **ZF = 0**
    
4. **OF (溢出)**：
    
    - 被加数 62A0 (正)，加数 1234 (正)。
        
    - 结果 74D4 (正)。
        
    - 正+正=正，逻辑正常，没溢出。 -> **OF = 0**
        
5. **PF (奇偶)**：看低8位 **D4H**。
    
    - D4H = 1101 0100。
        
    - 有 4 个 1（偶数）。 -> **PF = 1**
        
6. **AF (辅助)**：看最低一位 0+4=4，没进位。 -> **AF = 0**
    

---

#### **(6) 62A0H + F021H**

**第一步：列竖式计算**

codeText

```
6 2 A 0
+   F 0 2 1
-----------
  1 5 2 C 1
```

**结果**：**52C1H** （进位丢给CF）

**第二步：分析标志位**

1. **CF**：最高位 6+F = 21 (15+6=21=16+5)，产生了进位！ -> **CF = 1**
    
2. **SF**：结果最高位 5 (0101)，首位 0。 -> **SF = 0**
    
3. **ZF**：结果非0。 -> **ZF = 0**
    
4. **OF (最易错！)**：
    
    - 62A0 (正)。
        
    - F021 (首位F=1111，负)。
        
    - **正 + 负，永远不会溢出！** -> **OF = 0**
        
5. **PF**：看低8位 **C1H**。
    
    - C1H = 1100 0001。
        
    - 有 3 个 1（奇数）。 -> **PF = 0**
        

---

### **第 6 题：2.14 物理地址计算**

**题目**：写出下列存储器地址的段地址、偏移地址和物理地址。格式 AAAA:BBBB。

**【公式】**：**物理地址 = AAAA × 10H + BBBB** （十六进制乘10H就是左移一位）

我们看打勾的 **(2) 1FA0:0A1F**

1. **段地址**：冒号前面那个 -> **1FA0H**
    
2. **偏移地址**：冒号后面那个 -> **0A1FH**
    
3. **物理地址计算**：
    
    - 段地址左移一位：1FA0 -> **1FA00**
        
    - 加上偏移地址：
        
        codeText
        
        ```
        1 F A 0 0
        +   0 A 1 F
        -----------
          2 0 4 1 F
        ```
        
    - (A+A = 10+10=20=16+4 -> 进1写4)
        
    - (F+0+1 = 15+1=16 -> 进1写0)
        
    - (1+1 = 2)
        
    - **结果**：**2041FH**
        

---

### **总结技巧**

1. **配合关系**：
    
    - 取指令：找 CS。
        
    - 取堆栈：找 SS。
        
    - 取数据：找 DS。
        
2. **标志位**：
    
    - **OF**：只看有符号数逻辑（正+正=负？负+负=正？）。
        
    - **CF**：看最高位有没有进位。
        
3. **地址计算**：段地址末尾补个0，加上偏移地址，搞定。

# ch3
### **第 2 题：3.2 存储分配图（必考大题！）**

这道题是让你画出内存里到底存了啥。这种题 99% 会考填空或画图。

**已知**：段地址 0100H，偏移地址 0000H。  
**代码**：

codeAssembly

```
var1 DB 12, -12, 20/6, 4 DUP(0, 55H)
var2 DB 'Assemble'
var3 DW 'AB', 'cd', 'E'
var4 DW var2
var5 DD var2
```

**【逐行解析 & 内存图】**

1. **var1 DB 12, -12, 20/6, 4 DUP(0, 55H)**
    
    - 起始偏移：0000H
        
    - 12 -> 0CH
        
    - -12 -> F4H (补码)
        
    - 20/6 -> 3 -> 03H
        
    - 4 DUP(0, 55H) -> 重复4次 0, 55H -> 00, 55, 00, 55, 00, 55, 00, 55
        
    - **占用字节**：1 + 1 + 1 + 8 = 11 字节。
        
    - **下一个偏移**：000BH。
        
2. **var2 DB 'Assemble'**
    
    - 起始偏移：000BH
        
    - 存放 ASCII 码：41H('A'), 73H('s'), 73H('s')...
        
    - **占用字节**：8 字节。
        
    - **下一个偏移**：000BH + 8 = 0013H。
        
3. **var3 DW 'AB', 'cd', 'E' (高能预警！)**
    
    - 起始偏移：0013H
        
    - **DW 'AB'**：这是定义一个**字**。x86 是**小端存储（Little Endian）**，即“低位在前，高位在后”。
        
        - 'B' (42H) 是低位 -> 存低地址。
            
        - 'A' (41H) 是高位 -> 存高地址。
            
        - 内存里是：**42H, 41H** (B, A)。
            
    - **'cd'** -> 内存：**64H, 63H** (d, c)。
        
    - **'E'** -> 是 'E' 和 0 (0045H) -> 内存：**45H, 00H**。
        
    - **占用字节**：2 + 2 + 2 = 6 字节。
        
    - **下一个偏移**：0013H + 6 = 0019H。
        
4. **var4 DW var2**
    
    - 起始偏移：0019H
        
    - 含义：存 var2 的**偏移地址**。
        
    - var2 的偏移是 000BH。
        
    - 内存：**0BH, 00H** (小端模式)。
        
    - **占用**：2 字节。
        
5. **var5 DD var2**
    
    - 起始偏移：001BH
        
    - 含义：存 var2 的**远地址** (Double Word)，即 **偏移地址 + 段地址**。
        
    - 先存偏移 000BH，再存段地址 0100H。
        
    - 内存：**0BH, 00H** (偏移), **00H, 01H** (段)。
        
    - **占用**：4 字节。
        

---

### **做题技巧总结**

1. **DB/DW/DD**：分别对应 1、2、4 字节。
    
2. **DUP**：重复。3 DUP(1) 就是 1, 1, 1。
    
3. **小端模式（死记！）**：
    
    - **字数据**（DW）：低字节在低地址，高字节在高地址。
        
    - **字符串**（DB）：顺序存储，写啥存啥。
        
    - **DW 'AB'**：这是字，所以反着存（BA）。
        
4. **地址存储**：
    
    - DW label：存偏移。
        
    - DD label：先偏移，后段地址。

### **、 解开“小端存储”的疑惑**

#### **1. 数据的本质：是“串”还是“数”？**

- **DB 'Assemble'**：
    
    - 这里定义的是 **“字符串” (String)**，也就是一个**字节数组**。
        
    - 就像排队报数一样，'A' 是第1个，'s' 是第2个...
        
    - 内存是线性的，所以**第1个就在前面，第2个在后面**。这是天经地义的，和端模式无关。
        
    - 内存：'A', 's', 's', 'e' ...
        
- **DW 'AB'**：
    
    - 这里用了 DW，汇编器就认为你定义的是一个 **“字数值” (Word Value/Number)**。
        
    - 它把 'AB' 看作了一个 **16位的整数**。
        
    - 'A' (41H) 在高位，'B' (42H) 在低位。于是这个数就是 **4142H**。
        
    - **关键来了**：因为 x86 是**小端模式**（低位在低地址），存这个数的时候，**低位 42H ('B') 要先存**。
        
    - 内存：'B', 'A'。
        

> **一句话总结**：
> 
> - DB 把字符当**排队的兵**（数组），谁先来谁在前。
>     
> - DW 把字符当**一个大整数**（数值），既然是数，就要遵循“小端倒着存”的规矩。
>     

#### **2. 为什么要设计成“小端”这种反直觉的东西？**

虽然对人类读图不友好，但对 **CPU** 极其友好：

1. **加法计算快**：你想想列竖式加法，是不是从**个位**（最低位）开始算的？CPU 也是。把低位放在第一个地址，CPU 拿到地址就能立刻开始算加法，不用先跳到最后去。
    
2. **类型转换快**：
    
    - 有一个 32 位整数 0000 0005。
        
    - **小端存**：05 00 00 00。
        
    - 如果你想把它当 8 位读（强转 char），只需**读第一个字节 05**，地址都不用变！
        
    - 如果是大端（00 00 00 05），你想读 8 位，还得先算出那个 05 的地址在哪，很麻烦。


# ch4
## 1指令
1. **[] 里只能有 BX, BP, SI, DI**。
    
2. **两边位数要一样** (AX对BX，AL对BL)。
    
3. **两个内存不能对传**。
    
4. **段寄存器 (DS, ES) 不能直接吃数字**。
    
5. **不写 PTR，CPU不知道操作多大内存

6. **MOVS 这种串指令**，后面不要加操作数，它是靠 SI/DI 吃饭的。
    
7. **PUSH/POP 在 8086 里很挑剔**：PUSH 不能压数字，POP 不能弹 CS。
    
8. **段寄存器 (DS, ES...) 是“残疾人”**：不能 MOV 立即数，不能段对段 MOV，不能 XCHG。
    
9. **DIV / MUL 很霸道**：只能写一个操作数（除数/乘数），而且不能是数字，必须是寄存器或内存。
10. **移位 SHL/SHR**：次数超过1，必须用 **CL**。
    
11. **端口 IN/OUT**：端口号要么直接写数字（<256），要么放 **DX**，别的人不行。
    
12. **地址 OFFSET**：地址永远是 **16位** 的，别想放进 AL/BL 里。
    
13. **堆栈 PUSH**：只收 **16位** 寄存器。
恭喜你！这40道题看下来，你已经掌握了 8086 汇编语法的 **80%** 考点了。

以后做这种题，**像安检员一样扫描这几个点**：

1. **扫描 []**：里面必须是 BX, BP, SI, DI 的组合。BX和BP不能共存，SI和DI不能共存。
    
2. **扫描 MOV**：两边位数必须一样（8对8，16对16）；不能两边都是内存；不能两边都是段寄存器。
    
3. **扫描 段寄存器 (DS/ES)**：不能直接给它塞数字（立即数）。
    
4. **扫描 PTR**：如果你只看到 [内存] 和 立即数，没看到寄存器，那就是错的（大小不明）。
    
5. **扫描 特殊指令**：
    
    - **移位**：大于1次用 CL。
        
    - **端口**：大于255用 DX。
        
    - **堆栈**：只能压 16位。

**小结一下指令对的区别套路**：

1. **串指令 (MOVS, STOS, SCAS...)** 最大的特点就是**自带指针移动** (SI/DI ++/--)。
    
2. **LEA 是算地址，MOV 是取值**。
    
3. **SUB 是真减，CMP 是假减（只变标志位）**。
    
4. **BP 默认找 SS，其他通用寄存器默认找 DS**


# 2 寻址
### **（补充）根据条件推测的后续题目**

(题目给出了 20100H 等数据，肯定有对应的指令，以下是标准考法)

#### **(4) 推测指令：MOV AX, [BX]**

- **类型**：**寄存器间接寻址**
    
- **分析**：
    
    - **偏移地址** = BX = 0100H
        
    - **物理地址** = 20000H + 0100H = **20100H**
        
    - 查表得：3412H
        
- **答案**：**AX = 3412H**
    

#### **(5) 推测指令：MOV AX, [BX][SI]**

- **类型**：**基址变址寻址**
    
- **分析**：
    
    - **偏移地址** = BX + SI = 0100H + 0002H = 0102H
        
    - **物理地址** = 20000H + 0102H = **20102H**
        
    - 查表得：7856H
        
- **答案**：**AX = 7856H**
    

#### **(6) 推测指令：MOV AX, 1100H[BX] (或 [BX+1100H])**

- **类型**：**寄存器相对寻址**
    
- **分析**：
    
    - **偏移地址** = BX + 1100H = 0100H + 1100H = 1200H
        
    - **物理地址** = 20000H + 1200H = **21200H**
        
    - 查表得：4C2AH
        
- **答案**：**AX = 4C2AH**
    

#### **(7) 推测指令：MOV AX, 1100H[BX][SI]**

- **类型**：**相对基址变址寻址**（全家桶）
    
- **分析**：
    
    - **偏移地址** = BX + SI + 1100H = 0100H + 0002H + 1100H = 1202H
        
    - **物理地址** = 20000H + 1202H = **21202H**
        
    - 查表得：65B7H
        
- **答案**：**AX = 65B7H**
    

---

### **做题心得总结**

这道题的核心就一个公式：  
**物理地址 = 20000H (DS基址) + []里算出来的数**


# 3循环
1. **搜索循环模版**（必背）：
    
    - MOV CX, 长度
        
    - MOV SI, -1 (或者0，看逻辑)
        
    - INC SI
        
    - CMP ...
        
    - LOOPNZ NEXT (没找到继续找)
        
    - JNZ NOT_FOUND (循环结束如果还是不相等，就是没找到)
        
    - FOUND: (找到了)
        
2. **逻辑运算口诀**：
    
    - **AND (与)**：用来**清零**（想清零哪一位，就跟0与）。
        
    - **OR (或)**：用来**置1**（想把哪一位置1，就跟1或）。
        
    - **XOR (异或)**：用来**取反**（想把哪一位取反，就跟1异或；跟0异或保持不变）。**自己异或自己 = 0**（清零最快方法）。

# 4代码
在 8086（16位机）中，寄存器最大只有 16 位。要处理 32 位数，必须把它拆成 **高16位（DX）** 和 **低16位（AX）** 两个部分来协同工作。

### **核心思路**

1. **保存“尾巴”**：  
    题目要求把“移出的低 3 位”保存在 CL 中。这 3 位原本在 AX 的最右边。移位之前，先把它们备份出来。
    
2. **32位右移怎么做？（接力跑）**
    
    - 想象 DX 和 AX 是两根首尾相接的水管：[ DX ] -> [ AX ] -> 悬崖。
        
    - **第一步**：**高位（DX）** 向右移 1 位。最右边的那一位会被挤出来，掉进 **CF（进位标志位）** 里。
        
        - 指令：SHR DX, 1 （逻辑右移，高位补0）。
            
    - **第二步**：**低位（AX）** 向右移 1 位。重点来了，**它必须去接住刚才从 DX 掉进 CF 里的那一位**，把它放到自己的最高位。
        
        - 指令：RCR AX, 1 （带进位循环右移）。
            
    - **重复 3 次**，就完成了 32 位数的右移。
        

---

### **代码实现**

codeAssembly

```
; 1. 先保存要被移走的低3位
MOV CL, AL      ; 把 AL (AX的低8位) 复制给 CL
AND CL, 07H     ; 07H = 0000 0111B，做“与”运算，只保留最后3位，其他清零。
                ; 现在 CL 里存的就是未来的“移出位”了。

; 2. 开始 32位 移位操作
; 我们需要循环 3 次。
; 注意：不能用 LOOP 指令或 CX 计数，因为 CL 已经被占用了！
; 我们用 SI 或 BL 当计数器，这里用 BL。

MOV BL, 3       ; 设置循环次数为 3

L1:             ; 循环标号
    SHR DX, 1   ; 【关键】高位逻辑右移1位。最低位进入 CF，最高位补0。
    RCR AX, 1   ; 【关键】低位带进位右移1位。CF(原DX最低位)进入AX最高位。
    
    DEC BL      ; 计数器减 1
    JNZ L1      ; 如果 BL 不为 0，跳回 L1 继续移位
```

---

### **避坑指南（考试重点）**

1. **为什么不用 LOOP 指令？**
    
    - LOOP 指令强制使用 CX 寄存器做减法计数。
        
    - 但是题目要求结果存 CL。CX = CH:CL。如果你用 LOOP，它会自动修改 CX 的值，这就把你保存在 CL 里的结果给**破坏**了。
        
    - **对策**：用其他寄存器（如 BL, SI, DI）手动计数 (DEC + JNZ)。
        
2. **为什么要用 RCR？**
    
    - SHR 是补 0，RCR 是补 CF。
        
    - AX 的最高位，必须是 DX 移出来的那个位（也就是 CF），所以必须用 **带进位** 的移位指令。
        
3. **DEC 指令不影响 CF**
    
    - 在循环中，DEC BL 会改变 ZF 标志位（用于判断是否结束），但**不会改变 CF**。
        
    - 所以，SHR 产生的 CF 可以安全地跨过 DEC 指令，保留给下一轮的 RCR 使用。这是 8086 指令集设计的精妙之处。


# 5 程序分析
1. **SHL 是什么？**
    
    - **SHL (Shift Logical Left)**：逻辑左移。
        
    - **动作**：把二进制位全部向左推，**右边（低位）补 0**，左边（高位）挤出去的位掉进 CF 标志位。
        
    - **算术还是逻辑？** 对于**左移**来说，算术左移（SAL）和逻辑左移（SHL）**完全一样**，都是右边补0。只有右移（SHR/SAR）才区分补0还是补符号位。
        
    - **指令意思**：SHL DX, CL 的意思是“把 DX 里的东西向左移 CL (4) 位”，**不是**把 8 存进去。
        
2. **04 是什么？**
    
    - MOV CL, 04：就是把数字 4 存入 CL。
        
    - 后面的 SHL ..., CL：意思就是“移 4 位”。这个 4 是**次数**，不是数据。

# ch5
### **第一组：总线基础与引脚复用 (5.1 - 5.8)**

#### **5.1 ~ 5.3 总线基础概念**

- **5.1 填空**：外部结构表现为 **【引脚 (Pin)】**，它们构成了微处理器的 **【级】** 总线。
    
- **5.3 简答（总线分类 - 408必考）**：
    
    - **数据总线 (DB)**：双向，传数据/指令。8086是16位。
        
    - **地址总线 (AB)**：单向（CPU发），传地址。8086是20位。
        
    - **控制总线 (CB)**：双向/单向，传读写命令/状态信号。
        

#### **5.4 ~ 5.5 启动与复位 (RESET) —— 必考填空！**

- **5.4 启动地址**：
    
    - CPU 复位后，CS=FFFFH, IP=0000H。
        
    - 物理地址 = **0FFFF0H**。
        
- **5.5 RESET 信号**：
    
    - 有效形式：**【高电平】** 有效。
        
    - 持续时间：至少保持 **【4个】** 时钟周期。
        

#### **5.6 ~ 5.7 引脚复用 (Multiplexing) —— 第五章最核心考点！**

- **背景**：8086 只有 40 个引脚，为了省地方，**地址线**和**数据线**共用了一部分引脚。
    
- **5.6 复用引脚有哪些？（共21根）**
    
    - **AD15 ~ AD0**：地址/数据复用（16根）。
        
    - **A19/S6 ~ A16/S3**：地址/状态复用（4根）。
        
    - **BHE/S7**：高8位允许/状态复用（1根）。
        
- **5.7 ALE 信号的作用（灵魂拷问）**：
    
    - **问题**：同一根线，一会传地址，一会传数据，怎么分？
        
    - **答案**：靠 **ALE (Address Latch Enable 地址锁存允许)** 信号。
        
    - **何时有效**：**【高电平】** 有效。
        
    - **含义**：当 ALE=1 时，表示引脚上跑的是 **【地址】**。此时必须用**锁存器 (74LS373)** 把地址存下来，防止后面传数据时把地址覆盖了。
        

#### **5.8 高阻态 (High-Z)**

- **含义**：意味着引脚 **【与总线断开】**（电气上断路）。
    
- **作用**：把总线控制权让给别的设备（如 DMA 控制器）。
    

---

### **本组重点总结 (人话版)**

1. **启动地址**：死记 **0FFFF0H**。
    
2. **分时复用**：AD0-AD15 既传地址也传数据。
    
    - **T1 时刻**（周期刚开始）：传地址，**ALE=1**，告诉外面“快把地址记下来！”。
        
    - **T2-T4 时刻**：传数据，ALE=0。
        
3. **ALE**：是**区分地址和数据**的裁判员。
    

---
#### **5.9 输入控制信号 (RESET, READY, TEST)**

这三个信号是别人发给 CPU 的命令。

- **RESET (复位)**：
    
    - **作用**：让 CPU 重新投胎，回到初始状态（CS=FFFFH, IP=0000H）。
        
    - **有效电平**：**高电平**。
        
- **READY (准备好)**：
    
    - **作用**：**配合慢速设备**。如果内存或接口太慢，它们就把 READY 拉低，CPU 就在那里**空转等待 (插入等待周期** 
        
        **`TwTw​`**
        
        **)**，直到 READY 变高才继续干活。
        
    - **有效电平**：**高电平**表示“我准备好了，你可以读写了”。
        
- **TEST (测试)**：
    
    - **作用**：配合 **WAIT 指令** 使用，通常用于和协处理器（8087）同步。
        
    - **有效电平**：**低电平**有效（CPU 检测到引脚为低时，结束等待）。
        

#### **5.10 中断与总线请求 (HOLD, NMI, INTR...) —— 必考！**

- **DMA 相关**（让出总线权）：
    
    - **HOLD (输入)**：外设（如 DMA 控制器）想借用总线，向 CPU 发出请求（高电平）。
        
    - **HLDA (输出)**：Hold Acknowledge。CPU 说“好的，我松手了”，发出应答（高电平），然后 CPU 进入**高阻态**。
        
- **中断相关**（打断 CPU）：
    
    - **NMI (非屏蔽中断)**：天塌下来的大事（如掉电）。**不可屏蔽**，优先级极高。
        
    - **INTR (可屏蔽中断)**：普通琐事（如键盘输入）。可以通过 CLI 指令屏蔽（关中断）。
        
    - **INTA (中断响应)**：Interrupt Acknowledge。CPU 说“收到中断”，给外设发出的应答信号（**低电平有效**）。
        

#### **5.11 8086 与 8088 的区别 (常识题)**

记住这三点最核心的区别：

1. **数据总线宽度**：8086 是 **16位** (AD0-AD15)，8088 是 **8位** (AD0-AD7)。
    
2. **指令队列**：8086 是 **6字节**，8088 是 **4字节**。
    
3. **引脚定义**：34号引脚，8086 是 BHE (高8位允许)，8088 是 SS0 (状态位)。
    

#### **5.12 最大/最小模式 (RQ/GT)**

- **最小模式 (MN/MX 接 +5V)**：单 CPU 系统，控制信号由 CPU 直接发出（HOLD/HLDA）。
    
- **最大模式 (MN/MX 接地)**：多处理器系统（有协处理器 8087），控制信号由**总线控制器 (8288)** 发出。
    
    - **RQ/GT (Request/Grant)**：用于代替 HOLD/HLDA，实现**双向**的总线请求与总线授权（一根线既发请求也发授权）。
        

#### **5.13 M/IO 信号 (死记硬背！)**

这是独立编址的关键信号，告诉外界现在地址总线上跑的是内存地址还是端口号。

- 访问 **存储器 (Memory)** 时：**高电平 (1)**。
    
- 访问 **I/O 端口** 时：**低电平 (0)**。
    
- (注：8088 CPU 这个引脚叫 IO/M，电平逻辑正好相反，不要记混，考试一般考 8086)
    

#### **5.14 为什么要有地址锁存器 (74LS373)？**

这道题是 **5.7 ALE 信号** 的后续。

- **原因**：因为 8086 的地址线和数据线是 **分时复用** 的（AD0-AD15）。
    
- **后果**：在总线周期的 T1 时刻，引脚上是地址；到了 T2 时刻，引脚上就变成数据了，**地址就消失了！**
    
- **解决**：必须在 T1 时刻，趁着 **ALE 有效**，赶紧用锁存器把地址**“锁”**（保存）在外部芯片里，保证整个周期内地址一直稳定有效。
    

---

### **本组必背考点**

1. **READY**：解决 CPU 和慢速设备的速度差异（插入 
    
    ```
    TwTw​
    ```
    
    ）。
    
2. **HOLD/HLDA**：是 **DMA** 用来借总线的。
    
3. **M/IO**：**高存低口**（高电平访问内存，低电平访问 I/O）。
    
4. **地址锁存器**：因为 **地址/数据线复用**，不锁地址就没了。


### **第 1 小题：5.15 & 5.16 基础概念**

- **5.15 总线分类**
    
    - 根据传送信息种类不同，分为：**【数据总线】**、**【地址总线】** 和 **【控制总线】**。
        
    - (老生常谈了，必得分)
        
- **5.16 三态门 (Tristate)**
    
    - 三态逻辑电路输出的三个状态是：**【高电平 (1)】**、**【低电平 (0)】** 和 **【高阻态 (High-Z)】**。
        
    - 注：高阻态就像是“断路”或者“悬空”，在总线控制权移交（如 DMA）时非常重要。
        

---

### **第 2 小题：5.17 总线读周期时序（⭐超级重点）**

这道题把 **8086 一个总线周期（4个时钟周期：T1, T2, T3, T4）** 的全过程都考了一遍。我们需要像看电影一样回放这个过程：

**【电影回放：CPU 去内存读数据的过程】**

1. **T1 时刻（地址阶段）**：
    
    - CPU 大喊：“我要读这个地址！”
        
    - **动作**：CPU 往 AD 总线上输出地址。
        
    - **关键信号**：**ALE** (地址锁存允许) 变 **【高】** 电平。告诉外面的锁存器：“快把现在的地址记下来！”
        
    - **引脚状态**：AD15~AD0 输出的是 **【地址】** 信息。
        
2. **T2 时刻（转换/准备阶段）**：
    
    - 地址已经锁存好了，AD 总线空闲下来，准备传输数据。
        
    - **关键信号**：
        
        - CPU 发出 **RD** (读) 信号，开始输出 **【低】** 电平（有效）。
            
        - **DEN** (数据允许) 变为 **【低】** 电平（有效），打开数据收发器的大门。
            
        - **DT/R** (数据发送/接收)：因为是“读”周期（Receive），所以为 **【低】** 电平。（如果是写，就是高）。
            
3. **T3 ~ T4 时刻（数据传输阶段）**：
    
    - 数据像水一样流进来。
        
    - **动作**：在 **【T3 ~ T4】** 状态期间，数据在总线上稳定传输，CPU 完成数据的读入。
        
    - (注：如果有等待状态 Tw，会插在 T3 和 T4 之间)
        

**【填空答案速查】**

- ALE 信号在 **【T1】** 状态有效（高电平）。
    
- 在 **【T1】** 状态期间给出地址信息。
    
- 在 **【T2】** 状态开始输出低电平的 RD 信号。
    
- DEN 为 **【低】** 电平（低电平有效）。
    
- DT/R 为 **【低】** 电平（R代表Receive，低电平）。
    
- 在 **【T3、T4】** 状态完成数据的读入。
    

---

### **第 3 小题：5.18 & 5.19 最小方式总线形成**

- **5.18 两个芯片的作用（画图题常考芯片名）**
    
    - **74LS373**：**【地址锁存器】**。
        
        - **为什么用它？** 配合 ALE 信号，把 T1 时刻的地址“锁”住，解决地址/数据复用问题。
            
        - 它构成了 **8086 系统地址总线**。
            
    - **74LS245**：**【总线收发器】**（也叫数据缓冲器）。
        
        - **为什么用它？** 增强数据总线的驱动能力（力气大，推得动负载），并控制数据传输方向。
            
        - 它构成了 **数据总线**。
            
- **5.19 控制总线提供者**
    
    - 微机中的控制总线提供：通常填 **【CPU】**（在最小模式下）或 **【总线控制器 (8288)】**（在最大模式下）。根据前文语境，这里填 **CPU** 即可。
        

---

### **总结：第五章还要看什么？**

看到这里，第五章的作业题你也基本扫完了。  
对于 25 号的模考，第五章你只需要记住这张图的逻辑：

1. **T1**：发地址，**ALE** 变高。
    
2. **T2**：发读写命令 (**RD/WR**)，准备传数据。
    
3. **DT/R**：**读是低**，写是高。
    
4. **复用**：需要 **373** 锁存地址，**245** 缓冲数据。


### **第四组 (5.21 - 5.25)**

#### **5.21 最小模式下 4 种基本的读写操作是什么？**

- **答案**：
    
    1. **存储器读 (MEMR)**
        
    2. **存储器写 (MEMW)**
        
    3. **I/O 读 (IOR)**
        
    4. **I/O 写 (IOW)**
        
- **解析**：这就是 8086 的核心业务：要么跟内存打交道，要么跟接口打交道；要么读，要么写。
    

#### **5.22 控制信号是谁？**

- **答案**：**M/IO**（区分内存/接口）、**RD**（读）、**WR**（写）。
    
- **解析**：这三个信号的电平组合，决定了上面那 4 种操作。比如 M/IO=1, RD=0 就是“存储器读”。
    

#### **5.23 8086 执行 MOV AX, [SI] 产生什么操作？**

- **答案**：**【存储器读 (MEMR)】** 操作。
    
- **解析**：[SI] 是内存地址，MOV 把数据取回来，所以是读内存。
    

#### **5.24 8086 执行 OUT DX, AL 产生什么操作？**

- **答案**：**【I/O 写 (IOW)】** 操作。
    
- **解析**：OUT 指令是把数据发给端口，所以是写 I/O。
    

#### **5.25 RESET 信号后的启动地址？**

- **答案**：IP 为 **【0000H】**，从 **【FFFF0H】** 地址开始执行。
    
- **解析**：老生常谈的考点，RESET 后 CS=FFFFH, IP=0000H。
    

---

### **第五组 (5.26 - 5.28) —— 硬件连接细节（填空常客）**

#### **5.26 最小系统芯片连接（背诵题）**

- **地址锁存器 74LS373**：
    
    - 选通信号 G 应接 CPU 的 **【ALE】** 信号。（ALE=1 时锁存）
        
    - 输出允许端 OE 应接 **【地 (GND)】**。（让它一直工作）
        
- **数据收发器 74LS245**：
    
    - 方向控制端 DIR 应接 **【DT/R】** 信号。（控制是发还是收）
        
    - 输出允许端 E 应接 **【DEN】** 信号。（低电平有效，允许传输）
        

#### **5.27 8086 读写字节 vs 字**

- **写一个字节**：只需要使用 16 条数据线中的 **【8】** 条。
    
- **写一个字**：自然要用到 16 条数据线。
    
- **不对准（Unaligned）**：当存储为未对准时，则要在 **【2】** 个总线周期内完成。
    
    - (注：所谓“未对准”，就是比如你想读一个字，但起始地址是奇数（比如 1001H）。CPU 必须先读 1001H（高8位），再读 1002H（低8位），分两次干活。这是效率大坑。)
        

#### **5.28 READY 信号检测**

- **CPU 在哪检测？**：在 **【T3】** 状态开始检查 READY 信号。
    
- **高电平**：说明存储器或 I/O 端口 **【准备就绪】**。
    
- **低电平**：说明还没好，CPU 自动插入一个或几个 **【Tw (等待状态)】**，以延长总线周期。
    

---

### **第六组 (5.29 - 5.30) —— 最大模式（了解即可）**

#### **5.29 8086 最大系统 vs 最小系统**

- **最大系统结构较最小系统结构多一个芯片**：**【8288 (总线控制器)】**。
    
- **解析**：最大模式下，CPU 把控制信号的生成任务外包给了 8288。
    

#### **5.30 8086 最大系统总线命令**

- 这题考的是 8288 发出的信号名，填空稍微冷门一点：
    
    - A. MRDC (Memory Read Command)
        
    - B. MWTC (Memory Write Command)
        
    - C. IORC (I/O Read Command)
        
    - D. IOWC (I/O Write Command)
        
- 注：这一题如果考得不深，可以直接略过，知道最大模式用 8288 就行。
    

---

### **总结：第五章的通关密码**

1. **373** 接 **ALE**（锁地址）。
    
2. **245** 接 **DEN** 和 **DT/R**（控数据）。
    
3. **T1** 发地址，**T2** 发命令，**T3** 查 READY，**T4** 传完收工。
    
4. **奇地址读字**：要 **2** 个周期（慢）。


### **1. 关于 5.18 画图题**

- **你的问题**：是不是就是我给的这张图？
    
- **回答**：**完全正确！就是这张图！**
    
- **考试拿分要点**：  
    如果你在考试中需要画这个图，不需要画得像 PPT 这么花哨，但必须画出**三根核心连线**：
    
    1. **地址锁存**：CPU 的 ALE 引脚 
        
        ```
        →→
        ```
        
         74LS373 的 G (或 STB) 引脚。
        
    2. **数据方向**：CPU 的 DT/R 引脚 
        
        ```
        →→
        ```
        
         74LS245 的 DIR 引脚。
        
    3. **数据开关**：CPU 的 DEN 引脚 
        
        ```
        →→
        ```
        
         74LS245 的 OE (或 G) 引脚。
        
    
    - 只要这三根线连对了，分就拿到了。
        

---

### **2. 关于 5.19 控制总线提供什么？**

- **题目**：微机中的控制总线提供：
    
    - A. 数据信号流
        
    - B. 存储器和 I/O 设备的地址码
        
    - C. ... D. ...
        
- **解析**：
    
    - A 是数据总线干的事。
        
    - B 是地址总线干的事。
        
    - **控制总线 (CB)** 提供的是：**存储器和 I/O 设备的【控制信号】**（如读 RD、写 WR、片选 CS 等）以及**【时序信号】**。
        
- **答案**：如果选项 C 或 D 中有类似“控制信号”或“读写命令”的描述，选那个。
    

---

### **3. 5.24 最大模式引脚 (MN/MX)**

- **题目**：8086 CPU 工作在最大方式，引脚 MN/MX 应接？
    
- **答案**：**【地 (GND) / 低电平】**
    
- **记忆技巧**：
    
    - MN (Minimum) 上面没有横线，表示高电平有效 
        
        ```
        →→
        ```
        
         接 +5V 是最小模式。
        
    - MX (Maximum) 上面有横线，表示低电平有效 
        
        ```
        →→
        ```
        
         接 **接地 (GND)** 是最大模式。
        

---

### **4. 5.25 RESET 信号的细节 (为什么是 4 个周期？)**

- **题目**：RESET 信号在至少保持 4 个时钟周期的 **【高】** 电平时才有效。
    
- **深度解析 (Why?)**：  
    这不仅仅是一个电气规定，而是 CPU 内部逻辑复位的物理需求。
    
    1. **内部同步**：CPU 内部极其复杂，有微指令序列、流水线、各种寄存器。上电瞬间，这些部件的状态是随机的（混乱的）。
        
    2. **清空队列**：CPU 需要时间把指令队列里残留的垃圾指令清空。
        
    3. **初始化寄存器**：CPU 需要几个时钟脉冲来一步步把 CS 强制置为 FFFFH，把 IP 置为 0000H，把标志位清零。
        
    
    - **结论**：**4 个时钟周期**是 Intel 设计时计算出的完成这一系列“大扫除”动作所需的最短时间。如果 RESET 时间太短，CPU 可能还没“醒透”，就会跑飞。
        

---

### **5. 5.27 存储器对准 (Aligned) vs 未对准 (Unaligned) —— 难点详解！**

这道题非常重要，涉及 8086 **16位数据总线** 的物理特性。

- **填空答案**：
    
    - 读写字节：**【1】** 个总线周期。
        
    - 字对准：**【1】** 个总线周期。
        
    - 字未对准：**【2】** 个总线周期。
        
- **详细解读 (必看)**：
    
    **场景设定**：  
    8086 的内存就像**两列火车**并排停放。
    
    - **左列 (偶数体/低体)**：存地址 0, 2, 4... (连接数据线 
        
        ```
        D0∼D7D0​∼D7​
        ```
        
        )
        
    - **右列 (奇数体/高体)**：存地址 1, 3, 5... (连接数据线 
        
        ```
        D8∼D15D8​∼D15​
        ```
        
        )
        
    
    **情况 A：读写一个字节 (Byte)**
    
    - 不管是在偶地址还是奇地址，CPU 只需要利用 16 根数据线中的一半（高8位或低8位），伸手拿一次就够了。
        
    - **耗时：1 个周期**。
        
    
    **情况 B：读写一个“对准”的字 (Aligned Word)**
    
    - **定义**：字的起始地址是 **偶数** (如 2000H)。
        
    - **过程**：低 8 位在偶地址 (2000H)，高 8 位在奇地址 (2001H)。
        
    - **动作**：CPU 两只手同时伸出去，左手拿偶体，右手拿奇体。一次正好拿完 16 位。
        
    - **耗时：1 个周期**。
        
    
    **情况 C：读写一个“未对准”的字 (Unaligned Word)**
    
    - **定义**：字的起始地址是 **奇数** (如 2001H)。
        
    - **过程**：
        
        - 低 8 位在奇地址 (2001H) 
            
            ```
            →→
            ```
            
             属于右列。
            
        - 高 8 位在偶地址 (2002H) 
            
            ```
            →→
            ```
            
             属于**下一行**的左列。
            
    - **动作**：CPU 这一记“擒拿手”够不着了（因为数据跨行了）。
        
        - **第 1 次**：先读 2001H (拿低8位)。
            
        - **第 2 次**：再读 2002H (拿高8位)。
            
    - **耗时：2 个周期**。
        
- **结论**：这就是为什么在编程（尤其是 C 语言结构体定义）时，我们强调**“内存对齐”**。如果不对齐，CPU 读写速度会慢一倍！
    

---

到这里，第五章的所有“坑”都被填平了。  
**RESET 是高电平、4周期。**  
**MN/MX 接地是最大模式。**  
**字未对准要读 2 次。**



### **第 5.31 题：I/O 读时序图（简答/画图）**

**题目**：在 8086 最小方式下，说明引脚 CLK, ... RD, WR 的含义？画出它们在没有等待状态的 **I/O 读** 总线周期中的时序图。

**【解析】**  
前半部分问含义（我们在前面几组已经复习过了），重点是后半部分：**I/O 读时序**。

**应试核心（画图/判断要点）：**  
如果考试让你画图或判断波形，死死盯住这三条线：

1. **M/IO**：因为是 **I/O** 操作，所以在整个周期内，这条线必须是 **【低电平 (0)】**。
    
2. **RD**：因为是 **读** 操作，所以在 **T2** 时刻，这条线要变 **【低电平 (0)】**，直到 T4 变回高。
    
3. **WR**：因为没写，所以一直保持 **【高电平 (1)】**。
    

(辅助信号：ALE 在 T1 变高；DT/R 因为是读，全程为低)

---

### **第 5.32 题：对准字读时序**

**题目**：设指令 MOV AX, DATA 已被取到指令队列，假定 DATA 为**偶地址**。画出总线时序图。

**【解析】**  
这题翻译成人话就是：**读一个“对准”的字，需要几个周期？**

- **指令分析**：MOV AX, [偶地址]。
    
    - 源操作数是内存（Memory）。
        
    - 目的寄存器是 AX（16位，字）。
        
    - 地址是偶数（对准了）。
        
- **结论**：8086 的 16根数据线火力全开，**1 个总线周期** 就能把数据搬完。
    
- **时序特征**：
    
    - M/IO：**高电平**（因为是访问内存 DATA）。
        
    - BHE 和 A0：**同时为 0**（表示同时启用高8位和低8位数据线）。
        

---

### **第 5.33 题：未对准字 vs 字节（⭐超级易错点）**

**题目**：上题中，如果指令分别为：  
(1) MOV DATA+1, AX  
(2) MOV DATA+1, AL  
(上题说 DATA 是偶地址，那么 DATA+1 就是 **奇地址**)

**【解析】**

**情况 (1)：MOV [奇地址], AX**

- **动作**：要把 16位的数据 (AX)，写到一个奇数起始的地址里去。
    
- **对准了吗？** 没有！这是典型的 **未对准 (Unaligned)**。
    
- **后果**：CPU 被迫分两次干活。
    
    - 第一次：写高8位。
        
    - 第二次：写低8位。
        
- **答案**：需要 **【2】** 个总线周期。
    

**情况 (2)：MOV [奇地址], AL**

- **动作**：要把 8位的数据 (AL)，写到一个奇数地址里去。
    
- **需要分两次吗？** **不需要！**
    
    - 读写**字节 (Byte)** 永远只需要 1 次操作。8086 只需要使用高8位数据线 (
        
        ```
        D8∼D15D8​∼D15​
        ```
        
        ) 就能把数据写进那个奇数地址。
        
- **答案**：需要 **【1】** 个总线周期。
    

---

### **总结：看到“周期数”题目的秒杀口诀**

在 8086 系统中，问你执行一条 MOV 指令需要几个总线周期：

1. **看数据类型**：
    
    - 如果是 **字节 (AL/BL/DB)**：永远 **1** 个周期。（无论奇偶地址）。
        
2. **如果是 字 (AX/BX/DW)**：
    
    - 看地址是 **偶 (Even)**：**1** 个周期。（对准了，爽）。
        
    - 看地址是 **奇 (Odd)**：**2** 个周期。（没对准，累）。
        

---

### **关于 5.34 (推测)**

图片里没拍全，但根据位置推测，可能是关于 **“总线带宽计算”** 或者 **“Ready 信号插入 Tw”** 的题。

- 如果是考 **Ready**：记住 **READY=0 时插入等待周期 Tw**。
    
- 如果是考 **带宽**：带宽 = 频率 × 2 (字节/秒)。

# ch6
### **一、 基础扫盲：存储器计算的“三把斧”**

做这种题，你只需要掌握三个核心工具：

#### **1. 这里的 KB 怎么转成 Hex（十六进制）？**
这是最关键的一步。你要对 **2 的幂次方** 非常敏感。
*   **$1KB = 2^{10} = 400H$** （这个如果不熟，往下看）
    *   $1K = 1024$。
    *   $400H = 4 \times 16^2 = 4 \times 256 = 1024$。
*   **常用速查表（死记硬背，考场救命）：**
    *   **1KB** = **400H**
    *   **2KB** = **800H**
    *   **4KB** = **1000H**
    *   **8KB** = **2000H**
    *   **16KB** = **4000H**
    *   **32KB** = **8000H**
    *   **64KB** = **10000H**

#### **2. 地址计算黄金公式**
这跟你书上手写的笔记一模一样，逻辑完全正确：
> **末地址 = 首地址 + 容量 - 1**

*   **为什么要减 1？**
    *   这就好比：你是第 1 号，后面还有 2 个人（容量是3）。队伍最后一个人是几号？
    *   是 $1 + 3 - 1 = 3$ 号。
    *   如果不减 1，算出来的就是“下一个区域的起始地址”。

#### **3. 十六进制加减法**
*   **满 16 进 1**：$1+F = 10H$。
*   **借 1 当 16**：$10H - 1 = F$。

---

### **二、 习题 6.8 详解**

**题目**：一台 8 位微机系统，地址总线 16 位（说明寻址空间 $2^{16}=64KB$）。RAM 容量为 **32KB**，**首地址为 4000H**，且地址是连续的。问：**最高地址（末地址）是多少？**

#### **Step 1：把容量 32KB 转换成十六进制**
*   方法 A（查表法）：刚才背了，32KB 对应 **`8000H`**。
*   方法 B（推导法）：
    *   $32KB = 32 \times 1KB = 2^5 \times 2^{10} = 2^{15}$。
    *   二进制就是：`1000 0000 0000 0000` （第15位是1，后面15个0）。
    *   转十六进制：**`8000H`**。

#### **Step 2：套用公式**
*   **末地址 = 首地址 + 容量 - 1**
*   **首地址** = `4000H`
*   **容量** = `8000H`
*   **容量 - 1** = `8000H - 1H` = **`7FFFH`**
    *(想一下：8000 减 1，借位变成 7，后面全是 F)*

#### **Step 3：计算结果**
$$4000H + 7FFFH$$

列竖式：
```text
    4 0 0 0
+   7 F F F
-----------
    B F F F
```
*   $0+F=F$
*   $4+7=11$，在十六进制里 $11$ 就是 **B**。

**答案：最高地址是 BFFFH。**

---

### **三、 顺便看一眼 6.9 (反向操作)**

**题目**：某微机系统中内存 **首地址 4000H**，**末地址 7FFFH**，求**存储容量**。

这题是 6.8 的逆运算，也是考试常考题型。

#### **Step 1：套用逆公式**
> **容量 = 末地址 - 首地址 + 1**

#### **Step 2：计算差值**
$$7FFFH - 4000H$$

列竖式：
```text
    7 F F F
-   4 0 0 0
-----------
    3 F F F
```
差值是 `3FFFH`。

#### **Step 3：加 1 得到容量**
$$3FFFH + 1H = 4000H$$
*(F+1=10，一路进位)*

#### **Step 4：把 4000H 翻译回 KB**
*   我们知道 $1KB = 400H$。
*   $4000H \div 400H = 10H$ (十六进制的10) = **16 (十进制)**。
*   或者查表：**4000H 对应 16KB**。

**答案：16KB。**

---

### **总结：计算题通关秘籍**

1.  **看到 KB 先转 Hex**：
    *   记住 **1K=400H**，剩下的乘这就行。
2.  **看到“求末地址”**：
    *   `首 + (容量-1)`。也就是首地址加上全是 `F` 的那个数。
3.  **看到“求容量”**：
    *   `(末 - 首) + 1`。

这部分是不是很简单？如果 6.8 和 6.9 没问题了，我们就准备进入 **6.12**，那是一道**画图连线大题**，涉及到**译码器 74LS138** 的使用，是这一章的终极 Boss。
