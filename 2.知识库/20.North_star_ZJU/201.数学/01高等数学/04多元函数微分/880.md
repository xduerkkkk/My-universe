# 1 概念

![[880-1778382008490.jpeg]]

### 1. 多元函数偏导数存在 $\nRightarrow$ 连续 / 极限存在 (针对选项 A 和 B)

这是多元微积分中最常考的“易错点”。

- **知识点：** 偏导数 $f_x'(x_0, y_0)$ 和 $f_y'(x_0, y_0)$ 的存在，仅仅意味着函数在点 $(x_0, y_0)$ 处，沿着平行于 $x$ 轴和 $y$ 轴的**两个特定方向**上的变化率存在。它不能保证函数从四面八方逼近该点时的状态是一致的。
    
- **结论：** 偏导数存在**不能**推导出该点的二重极限 $\lim_{\substack{x \to x_0 \\ y \to y_0}} f(x,y)$ 存在，也**不能**推导出函数在该点 $f(x,y)$ 连续。因此选项 A 和 B 是错误的。（经典反例：当 $x^2+y^2 \neq 0$ 时 $f(x,y) = \frac{xy}{x^2+y^2}$，在 $(0,0)$ 处偏导数均为 0，但极限不存在且不连续）。
    

### 2. 偏导数的定义与“一元函数可导必连续” (针对选项 C)

这是这道题的**解题核心（正确选项）**。

- **知识点：** 偏导数本质上就是“固定一个变量，对另一个变量求导”的一元函数导数。
    
    根据偏导数定义：$f_x'(x_0, y_0) = \lim_{x \to x_0} \frac{f(x, y_0) - f(x_0, y_0)}{x - x_0}$。
    
    这个极限存在，意味着一元函数 $g(x) = f(x, y_0)$ 在 $x = x_0$ 处是**可导**的。
    
- **结论：** 根据一元函数的性质“可导必连续”，函数 $g(x) = f(x, y_0)$ 在 $x = x_0$ 处必然连续。既然连续，那么它的极限就一定存在，即 $\lim_{x \to x_0} f(x, y_0) = f(x_0, y_0)$。因此 **选项 C 是正确的**。
    

### 3. 偏导数存在对定义域的要求 (针对选项 D)

考查对导数定义域的精确理解。

- **知识点：** 偏导数 $f_x'(x_0, y_0)$ 和 $f_y'(x_0, y_0)$ 的存在，只要求函数在经过点 $(x_0, y_0)$ 的**十字交叉线**（即 $y=y_0$ 和 $x=x_0$ 的直线上）的一维邻域内有定义即可。
    
- **结论：** 选项 D 说的去心邻域 $\mathring{U}(x_0, y_0)$ 是一个二维的圆盘区域（不含圆心）。偏导数存在并不要求函数在点 $(x_0, y_0)$ 的整个二维去心邻域内都有定义，只要在两根轴线上有定义就能求偏导。因此选项 D 是错误的。


# 2![[880-1778382327519.jpeg]]

你第一步算出了 $f'_x(0,0) = 0$ 和 $f'_y(0,0) = 0$，这个思路和结果是**完全正确**的！这说明你对偏导数在原点的定义掌握得很扎实。

你之所以卡在第二步，是因为遇到了求二阶混合偏导数时最常见的“盲区”：**要计算在 $(0,0)$ 处的二阶偏导数，仅知道一阶偏导数在 $(0,0)$ 处的“这一个点的值”是不够的，我们需要知道一阶偏导数在坐标轴上的“函数表达式”。**

就像你要算一辆车在某一秒的加速度（二阶导），只知道它在这一秒的速度（一阶导在某点的值）是不行的，你得知道它在这一秒前后的速度变化情况（一阶导函数）。

下面我带你一步一步破解它，关键在于回归**二阶偏导数的极限定义**。

### 1. 明确目标：二阶偏导数的定义

我们要求的是 $f''_{xy}(0,0)$ 和 $f''_{yx}(0,0)$。根据定义：

- 求 $f''_{xy}(0,0)$：先对 $x$ 求导，再对 $y$ 求导。
    
    $$f''_{xy}(0,0) = \lim_{y \to 0} \frac{f'_x(0,y) - f'_x(0,0)}{y}$$
    
- 求 $f''_{yx}(0,0)$：先对 $y$ 求导，再对 $x$ 求导。
    
    $$f''_{yx}(0,0) = \lim_{x \to 0} \frac{f'_y(x,0) - f'_y(0,0)}{x}$$
    

我们已经知道 $f'_x(0,0) = 0$ 和 $f'_y(0,0) = 0$。所以，接下来我们**必须**求出 $f'_x(0,y)$（其中 $y \neq 0$）和 $f'_y(x,0)$（其中 $x \neq 0$）。

---

### 2. 计算 $f''_{xy}(0,0)$：看它是否存在

首先，我们需要求出 $f'_x(0,y)$，即固定一个非零的 $y$，让 $x$ 趋于 0：

$$f'_x(0,y) = \lim_{x \to 0} \frac{f(x,y) - f(0,y)}{x}$$

注意这里 $y \neq 0$，所以 $f(0,y) = \frac{0^2+y^2}{0^2+y^4}\sin(0 \cdot y^2) = 0$。代入原函数：

$$f'_x(0,y) = \lim_{x \to 0} \frac{\frac{x^2+y^2}{x^2+y^4}\sin(xy^2) - 0}{x}$$

$$f'_x(0,y) = \lim_{x \to 0} \frac{x^2+y^2}{x^2+y^4} \cdot \frac{\sin(xy^2)}{x}$$

因为 $x \to 0$，我们可以用等价无穷小替换 $\sin(xy^2) \sim xy^2$：

$$f'_x(0,y) = \lim_{x \to 0} \frac{x^2+y^2}{x^2+y^4} \cdot \frac{xy^2}{x} = \lim_{x \to 0} \frac{x^2+y^2}{x^2+y^4} \cdot y^2$$

当 $x \to 0$ 时，对于固定的常数 $y \neq 0$，分式部分趋于 $\frac{y^2}{y^4}$：

$$f'_x(0,y) = \frac{y^2}{y^4} \cdot y^2 = 1$$

现在我们得到了 $f'_x(0,y) = 1$ ($y \neq 0$)。把这个结果代入到二阶偏导数的定义中：

$$f''_{xy}(0,0) = \lim_{y \to 0} \frac{f'_x(0,y) - f'_x(0,0)}{y} = \lim_{y \to 0} \frac{1 - 0}{y} = \lim_{y \to 0} \frac{1}{y}$$

显然，$\lim_{y \to 0} \frac{1}{y} = \infty$，极限不存在。

**结论 1：$f''_{xy}(0,0)$ 不存在。**

---

### 3. 计算 $f''_{yx}(0,0)$：看它是否存在

用完全相同的逻辑，我们先求 $f'_y(x,0)$，即固定一个非零的 $x$，让 $y$ 趋于 0：

$$f'_y(x,0) = \lim_{y \to 0} \frac{f(x,y) - f(x,0)}{y}$$

同样 $x \neq 0$，所以 $f(x,0) = \frac{x^2+0}{x^2+0}\sin(0) = 0$。代入计算：

$$f'_y(x,0) = \lim_{y \to 0} \frac{\frac{x^2+y^2}{x^2+y^4}\sin(xy^2) - 0}{y}$$

$$f'_y(x,0) = \lim_{y \to 0} \frac{x^2+y^2}{x^2+y^4} \cdot \frac{\sin(xy^2)}{y}$$

同样使用等价无穷小 $\sin(xy^2) \sim xy^2$：

$$f'_y(x,0) = \lim_{y \to 0} \frac{x^2+y^2}{x^2+y^4} \cdot \frac{xy^2}{y} = \lim_{y \to 0} \frac{x^2+y^2}{x^2+y^4} \cdot xy$$

当 $y \to 0$ 时，对于固定的常数 $x \neq 0$，分式部分趋于 $\frac{x^2}{x^2} = 1$，而后面的 $xy$ 趋于 $x \cdot 0 = 0$：

$$f'_y(x,0) = 1 \cdot 0 = 0$$

现在我们得到了 $f'_y(x,0) = 0$ ($x \neq 0$)。代入二阶偏导数定义：

$$f''_{yx}(0,0) = \lim_{x \to 0} \frac{f'_y(x,0) - f'_y(0,0)}{x} = \lim_{x \to 0} \frac{0 - 0}{x} = \lim_{x \to 0} 0 = 0$$

极限存在且等于 0。

**结论 2：$f''_{yx}(0,0)$ 存在。**

# 3 -[ ] 掌握求导法何时用，以及定义法
![[880-1778382553990.jpeg]]

### 1. 用定义法分析选项 C 和 D

我们要看当 $y \neq 0$ 时，$f'_x(0,y)$ 是否存在。

根据偏导数在某一点的定义，固定 $y$，让 $x \to 0$：

$$f'_x(0,y) = \lim_{x \to 0} \frac{f(x, y) - f(0, y)}{x - 0}$$

把函数 $f(x,y) = \sqrt{|xy|}$ 代入：

- $f(x, y) = \sqrt{|xy|} = \sqrt{|x|} \cdot \sqrt{|y|}$
    
- $f(0, y) = \sqrt{|0 \cdot y|} = 0$
    

代入极限式中：

$$f'_x(0,y) = \lim_{x \to 0} \frac{\sqrt{|x|} \cdot \sqrt{|y|} - 0}{x} = \sqrt{|y|} \cdot \lim_{x \to 0} \frac{\sqrt{|x|}}{x}$$

因为题目条件是 $y \neq 0$，所以 $\sqrt{|y|}$ 是一个**非零常数**。这个极限存不存在，完全取决于后面这部分 $\lim_{x \to 0} \frac{\sqrt{|x|}}{x}$ 是否存在。

我们来看看这个极限的左极限和右极限：

- **右极限 ($x \to 0^+$)：** 此时 $x > 0$，所以 $|x| = x$。
    
    $$\lim_{x \to 0^+} \frac{\sqrt{x}}{x} = \lim_{x \to 0^+} \frac{1}{\sqrt{x}} = +\infty$$
    
- **左极限 ($x \to 0^-$)：** 此时 $x < 0$，所以 $|x| = -x$。（注意分母的 $x$ 可以写成 $-(-x)$）
    
    $$\lim_{x \to 0^-} \frac{\sqrt{-x}}{x} = \lim_{x \to 0^-} \frac{\sqrt{-x}}{-(-x)} = \lim_{x \to 0^-} -\frac{1}{\sqrt{-x}} = -\infty$$
    

**结论：** 左右极限不仅不相等，而且都是无穷大。因此，极限 $\lim_{x \to 0} \frac{\sqrt{|x|}}{x}$ 不存在。既然这个极限不存在，那么当 $y \neq 0$ 时，$f'_x(0,y)$ 也就**不存在**。

所以，**正确选项是 D**。

---

### 2. 顺便看看选项 A 和 B 错在哪 (常规求导)

当 $x \neq 0$ 时，我们可以直接用公式求导，但要把绝对值拆开看：

已知 $f(x,y) = \sqrt{|xy|} = |x|^{\frac{1}{2}} \cdot |y|^{\frac{1}{2}}$。对 $x$ 求偏导时，把 $|y|^{\frac{1}{2}}$ 看作常数。

- **对于选项 A ($x > 0$ 时)：**
    
    此时 $|x| = x$，函数可以写为 $f(x,y) = x^{\frac{1}{2}} \cdot \sqrt{|y|}$。
    
    求导：$f'_x(x,y) = \frac{1}{2}x^{-\frac{1}{2}} \cdot \sqrt{|y|} = \frac{1}{2}\frac{\sqrt{|y|}}{\sqrt{x}} = \frac{1}{2}\sqrt{\left|\frac{y}{x}\right|}$。
    
    选项 A 多了一个负号，所以是**错误**的。
    
- **对于选项 B ($x < 0$ 时)：**
    
    此时 $|x| = -x$，函数可以写为 $f(x,y) = (-x)^{\frac{1}{2}} \cdot \sqrt{|y|}$。
    
    根据复合函数求导法则（外层对底数求导，内层对 $-x$ 求导出个 $-1$）：
    
    $f'_x(x,y) = \frac{1}{2}(-x)^{-\frac{1}{2}} \cdot (-1) \cdot \sqrt{|y|} = -\frac{1}{2}\frac{\sqrt{|y|}}{\sqrt{-x}} = -\frac{1}{2}\sqrt{\left|\frac{y}{x}\right|}$。
    
    选项 B 漏了一个负号，所以也是**错误**的。
    
我们现在就用纯**定义法**，来求 $f(x,y) = \sqrt{|xy|}$ 在 $x \neq 0$ 时的偏导数 $f'_x(x,y)$。

根据偏导数的定义，我们给 $x$ 一个增量 $\Delta x$，并让 $\Delta x \to 0$：

$$f'_x(x,y) = \lim_{\Delta x \to 0} \frac{f(x+\Delta x, y) - f(x,y)}{\Delta x}$$

将函数代入：

$$f'_x(x,y) = \lim_{\Delta x \to 0} \frac{\sqrt{|(x+\Delta x)y|} - \sqrt{|xy|}}{\Delta x}$$

因为 $y$ 在这里被视为常数，我们可以把 $\sqrt{|y|}$ 提出来：

$$f'_x(x,y) = \sqrt{|y|} \cdot \lim_{\Delta x \to 0} \frac{\sqrt{|x+\Delta x|} - \sqrt{|x|}}{\Delta x}$$

接下来是核心：**去绝对值**。

既然 $x \neq 0$，那么 $x$ 要么大于 0，要么小于 0。

因为 $\Delta x \to 0$（说明 $\Delta x$ 是一个极其微小的数），所以 $x+\Delta x$ 的正负号**完全由 $x$ 的正负号决定**。

我们需要分两种情况讨论：

### 情况 1：当 $x > 0$ 时

当 $x > 0$ 时，只要 $\Delta x$ 足够小，$x+\Delta x$ 必定也大于 0。

所以绝对值可以直接去掉：$|x+\Delta x| = x+\Delta x$ 且 $|x| = x$。

此时极限变为：

$$\lim_{\Delta x \to 0} \frac{\sqrt{x+\Delta x} - \sqrt{x}}{\Delta x}$$

为了消去分子上的根号，我们使用**分子有理化**（分子分母同乘 $\sqrt{x+\Delta x} + \sqrt{x}$）：

$$= \lim_{\Delta x \to 0} \frac{(\sqrt{x+\Delta x} - \sqrt{x})(\sqrt{x+\Delta x} + \sqrt{x})}{\Delta x(\sqrt{x+\Delta x} + \sqrt{x})}$$

$$= \lim_{\Delta x \to 0} \frac{(x+\Delta x) - x}{\Delta x(\sqrt{x+\Delta x} + \sqrt{x})}$$

$$= \lim_{\Delta x \to 0} \frac{\Delta x}{\Delta x(\sqrt{x+\Delta x} + \sqrt{x})}$$

约掉 $\Delta x$ 后代入 $\Delta x = 0$：

$$= \lim_{\Delta x \to 0} \frac{1}{\sqrt{x+\Delta x} + \sqrt{x}} = \frac{1}{2\sqrt{x}}$$

把前面提出来的 $\sqrt{|y|}$ 乘回来：

$$f'_x(x,y) = \sqrt{|y|} \cdot \frac{1}{2\sqrt{x}} = \frac{1}{2}\sqrt{\frac{|y|}{x}} = \frac{1}{2}\sqrt{\left|\frac{y}{x}\right|}$$

_(这与我们上一条回复中分析选项 A/B 时，用公式法算出的 $x>0$ 时的结果一模一样)_

---

### 情况 2：当 $x < 0$ 时

当 $x < 0$ 时，只要 $\Delta x$ 足够小，$x+\Delta x$ 必定也小于 0。

所以去绝对值要加负号：$|x+\Delta x| = -(x+\Delta x) = -x-\Delta x$ 且 $|x| = -x$。

此时极限变为：

$$\lim_{\Delta x \to 0} \frac{\sqrt{-x-\Delta x} - \sqrt{-x}}{\Delta x}$$

同样使用**分子有理化**（分子分母同乘 $\sqrt{-x-\Delta x} + \sqrt{-x}$）：

$$= \lim_{\Delta x \to 0} \frac{(\sqrt{-x-\Delta x} - \sqrt{-x})(\sqrt{-x-\Delta x} + \sqrt{-x})}{\Delta x(\sqrt{-x-\Delta x} + \sqrt{-x})}$$

$$= \lim_{\Delta x \to 0} \frac{(-x-\Delta x) - (-x)}{\Delta x(\sqrt{-x-\Delta x} + \sqrt{-x})}$$

$$= \lim_{\Delta x \to 0} \frac{-\Delta x}{\Delta x(\sqrt{-x-\Delta x} + \sqrt{-x})}$$

约掉 $\Delta x$ 后代入 $\Delta x = 0$：

$$= \lim_{\Delta x \to 0} \frac{-1}{\sqrt{-x-\Delta x} + \sqrt{-x}} = \frac{-1}{2\sqrt{-x}}$$

把前面提出来的 $\sqrt{|y|}$ 乘回来：

$$f'_x(x,y) = \sqrt{|y|} \cdot \frac{-1}{2\sqrt{-x}} = -\frac{1}{2}\sqrt{\frac{|y|}{-x}} = -\frac{1}{2}\sqrt{\left|\frac{y}{x}\right|}$$

_(这同样与我们用复合函数求导公式算出的 $x<0$ 时的结果一模一样)_



# 4 -【 】知识点
![[880-1778467513853.jpeg]]
### 1. 如何让 A 选项正确？（全微分存在）

- **当前的问题：** 选项 A 写的是全微分公式 $dz = f'_x dx + f'_y dy$。但是，偏导数存在**推不出**全微分存在（即函数可微）。偏导数仅仅是十字交叉线上的变化率，全微分要求从四面八方逼近时都能用一个线性平面去近似。
    
- **如何修改题干：** 需要在题干中明确补上“可微”这个条件。
    
- **修改后的题干：** 设 $z=f(x,y)$ 在点 $(0,0)$ 的某邻域内有定义，**且在点 $(0,0)$ 处可微**，且 $f'_x(0,0)=1, f'_y(0,0)=1$。
    

### 2. 如何让 D 选项正确？（极限存在 / 连续）

- **当前的问题：** 这是经典的易错点。一元函数中“可导必连续”，但在多元函数中，**“偏导数存在”推不出“连续”，当然也就推不出“极限存在”**。（反例还是那个沿着坐标轴为 0，其他路径不为 0 的十字架形函数）。
    
- **如何修改题干：** 要让极限存在，最直接的办法是加上“连续”条件；或者加上比连续更强的“可微”条件（可微必连续，连续必有极限）。
    
- **修改后的题干：** 设 $z=f(x,y)$ 在点 $(0,0)$ 的某邻域内有定义，**且在点 $(0,0)$ 处连续**（或**可微**），且 $f'_x(0,0)=1, f'_y(0,0)=1$。
    

### 3. 如何让 C 选项正确？（法向量的定义）

这个选项比较特殊，它不仅缺了理论条件，连**数值**都是错的，所以题干需要做较大的“手术”。

- **当前的问题 1（理论层面）：** 曲面要存在法向量（即存在切平面），前提是函数在该点必须是**可微**的。目前题干没给可微条件，法向量可能根本不存在。
    
- **当前的问题 2（数值层面）：** 曲面 $z = f(x,y)$ 可以改写为隐函数形式 $F(x,y,z) = f(x,y) - z = 0$。
    
    根据梯度公式，它的法向量应该是 $\vec{n} = (F'_x, F'_y, F'_z) = (f'_x, f'_y, -1)$。
    
    代入题干已知条件 $f'_x(0,0)=1, f'_y(0,0)=1$，正确的法向量应该是 **$(1, 1, -1)$**，或者它的反方向 $(-1, -1, 1)$。
    
    而选项 C 给的法向量是 $(1, 1, 1)$，它的 $z$ 坐标符号不对，永远不可能和当前的题干匹配。
    
- **如何修改题干：** 为了强行让法向量 $(1, 1, 1)$ 成立，我们必须把法向量写成 $(-f'_x, -f'_y, 1)$ 的形式（令 $F(x,y,z) = z - f(x,y) = 0$），这就要求 $-f'_x = 1$ 且 $-f'_y = 1$。
    
- **修改后的题干：** 设 $z=f(x,y)$ 在点 $(0,0)$ 的某邻域内有定义，**且在点 $(0,0)$ 处可微**，且 **$f'_x(0,0)=-1, f'_y(0,0)=-1$**。（注意，此时 B 选项和 A 选项也就跟着变了）。

# 5 -【】
![[880-1778468819260.jpeg]]![[880-1778468821638.jpeg]]
### 第 (2) 题：利用“放缩法”与“夹逼定理”


针对这道题，使用**极坐标换元**是**完全可行**的。在计算二元函数的极限时，极坐标换元的核心合法性在于：能否将表达式拆分为极径趋于无穷（或 0）的部分，以及一个具备**有界性**的极角函数部分。

1. **引入极坐标：**
    
    令 $x = r\cos\theta$，$y = r\sin\theta$。由于题目条件是 $x \to \infty$ 且 $y \to \infty$，我们可以确定极径 $r \to \infty$，且极角 $\theta \in (0, \frac{\pi}{2})$。
    
2. **代入并化简表达式：**
    
    将极坐标代入原极限函数中：
    
    $$ f(r, \theta) = \frac{r\cos\theta + r\sin\theta}{r^2\cos^2\theta - r^2\cos\theta\sin\theta + r^2\sin^2\theta} $$
    
    提取公因式并化简，可以得到：
    
    $$ f(r, \theta) = \frac{1}{r} \cdot \frac{\cos\theta + \sin\theta}{1 - \cos\theta\sin\theta} $$
    
3. **论证角度函数的有界性：**
    
    观察提取出的角度函数 $g(\theta) = \frac{\cos\theta + \sin\theta}{1 - \cos\theta\sin\theta}$。

    - 分母部分：利用倍角公式，分母可化为 $1 - \frac{1}{2}\sin2\theta$。因为正弦函数的值域为 $[-1, 1]$，所以分母 $1 - \frac{1}{2}\sin2\theta \ge \frac{1}{2} > 0$。
        
    - 分子部分：利用辅助角公式，分子可化为 $\sqrt{2}\sin(\theta + \frac{\pi}{4})$，其绝对值最大不超过 $\sqrt{2}$。
        
    - 综合得出：$|g(\theta)| \le \frac{\sqrt{2}}{1/2} = 2\sqrt{2}$。由此证明该角度函数在无论 $\theta$ 取何值时，都是有界的。

4. **得出结论：**
    
    因为当 $r \to \infty$ 时，$\lim_{r \to \infty} \frac{1}{r} = 0$，且 $g(\theta)$ 已被证明为有界函数。根据“无穷小量与有界函数的乘积仍为无穷小量”的定理，该极限的值为 0。





为了提供更多维度的交叉验证，本题也可以直接使用**夹逼定理**（放缩法）来求解，这种代数放缩法在处理多项式分式时步骤更加精简。

1. **构造不等式放缩分母：**
    
    通过配方，将分母缩小，从而使整个分式变大：
    
    $$ x^2 - xy + y^2 = \frac{1}{2}(x^2+y^2) + \frac{1}{2}(x-y)^2 \ge \frac{1}{2}(x^2+y^2) $$
    
2. **对整体进行放缩：**
    
    考虑 $x \to \infty, y \to \infty$ 时的正值情况：
    
    $$ 0 \le \frac{x+y}{x^2-xy+y^2} \le \frac{x+y}{\frac{1}{2}(x^2+y^2)} $$
    
    进一步利用基本不等式 $x+y \le \sqrt{2(x^2+y^2)}$ 放缩分子：
    
    $$ 0 \le \frac{x+y}{x^2-xy+y^2} \le \frac{\sqrt{2}\sqrt{x^2+y^2}}{\frac{1}{2}(x^2+y^2)} = \frac{2\sqrt{2}}{\sqrt{x^2+y^2}} $$
    
3. **求取极限：**
    
    当 $x \to \infty$ 且 $y \to \infty$ 时，显然 $\sqrt{x^2+y^2} \to \infty$。不等式最右侧的表达式趋近于 0。根据夹逼定理，原极限值为 0。
    

极坐标换元法在本题的求解中逻辑严密，只要补充了对角度函数有界性的说明，即是一个完美的证明过程。


### 第 (3) 题：凑“第二重要极限”

**题目：** $\lim_{\substack{x \to \infty \\ y \to 0}} \left(1 - \frac{1}{2x}\right)^{\frac{x^2}{x+y}}$

**思路分析：**

当 $x \to \infty$ 时，底数 $1 - \frac{1}{2x} \to 1$。

看指数部分：$\frac{x^2}{x+y}$，分子分母同除以 $x$，变成 $\frac{x}{1+y/x}$。因为 $y \to 0$ 且 $x \to \infty$，$y/x \to 0$，所以指数整体趋于 $\infty$。

底数趋于 1，指数趋于无穷，这是经典的 **$1^\infty$ 型极限**。处理这种极限的唯一指定工具就是**第二重要极限**：$\lim_{u \to \infty} (1+\frac{1}{u})^u = e$。

**解题步骤：**

1. **强行凑出 $e$ 的形式：**
    
    第二重要极限的核心是：底数里加上的那一项（$-\frac{1}{2x}$），它的**倒数**（$-2x$）必须出现在指数上。我们先强行给它配上这个倒数：
    
    $$\left(1 - \frac{1}{2x}\right)^{\frac{x^2}{x+y}} = \left[ \left(1 + \frac{1}{-2x}\right)^{-2x} \right]^{\frac{1}{-2x} \cdot \frac{x^2}{x+y}}$$
    
2. **化简外部的指数：**
    
    刚才为了配凑凑多出来的 $\frac{1}{-2x}$，和原指数乘在一起化简：
    
    $$\frac{1}{-2x} \cdot \frac{x^2}{x+y} = \frac{-x^2}{2x(x+y)} = \frac{-x}{2(x+y)}$$
    
    分子分母同时除以 $x$：
    
    $$= \frac{-1}{2\left(1 + \frac{y}{x}\right)}$$
    
3. **分布取极限：**
    
    现在式子变成了：$\left[ \left(1 - \frac{1}{2x}\right)^{-2x} \right]^{\frac{-1}{2(1 + y/x)}}$
    
    - 内部中括号的极限：当 $x \to \infty$ 时，根据第二重要极限，它趋于 **$e$**。
        
    - 外部指数的极限：当 $x \to \infty, y \to 0$ 时，$\frac{y}{x} \to 0$。所以指数趋于 $\frac{-1}{2(1+0)} = \mathbf{-\frac{1}{2}}$。
        
4. **得出结论：**
    
    原极限等于 $e^{-\frac{1}{2}}$，也就是 $\mathbf{\frac{1}{\sqrt{e}}}$。


# 6
![[880-1778470399418.jpeg]]

消去参数 t 
![[880-1778500706501.jpeg]]


- $1 \cdot \frac{\mathrm{d}y}{\mathrm{d}x} - f_t \cdot \frac{\mathrm{d}t}{\mathrm{d}x} = f_x$
    
- $F_y \cdot \frac{\mathrm{d}y}{\mathrm{d}x} + F_t \cdot \frac{\mathrm{d}t}{\mathrm{d}x} = -F_x$
- **运用行列式法则求解目标导数**
    
    提取该线性方程组的系数矩阵，计算其主行列式（即系数行列式） $D$：
    
    $$ D = \begin{vmatrix} 1 & -f_t \ F_y & F_t \end{vmatrix} = 1 \cdot F_t - (-f_t) \cdot F_y = F_t + f_t F_y $$
    
    为了求解 $\frac{\mathrm{d}y}{\mathrm{d}x}$，我们将等式右侧的常数列向量 $\begin{pmatrix} f_x \\ -F_x \end{pmatrix}$ 替换到主行列式的第一列，构造出目标分子行列式 $D_y$：
    
    $$ D_y = \begin{vmatrix} f_x & -f_t \ -F_x & F_t \end{vmatrix} = f_x \cdot F_t - (-f_t) \cdot (-F_x) = f_x F_t - f_t F_x $$
    
    根据克莱姆法则，假设 $D \neq 0$，目标导数的值即为分子行列式与主行列式的商：
    
    $$ \frac{\mathrm{d}y}{\mathrm{d}x} = \frac{D_y}{D} = \frac{f_x F_t - f_t F_x}{F_t + f_t F_y} $$
    

### 三、最终结论

通过上述严密的行列式求解过程，可以得出最终的导数表达式。填空处的答案应为：

$$ \frac{f_x F_t - f_t F_x}{F_t + f_t F_y} $$

也可以将其写为偏导数符号完全展开的标准形式：

$$ \frac{\frac{\partial f}{\partial x}\frac{\partial F}{\partial t} - \frac{\partial f}{\partial t}\frac{\partial F}{\partial x}}{\frac{\partial F}{\partial t} + \frac{\partial f}{\partial t}\frac{\partial F}{\partial y}} $$

# 7 -【】
![[880-1778502378785.jpeg]]
你不用有任何挫败感！说实话，**你想到的“分类讨论”才是人类在考场上最真实、最本质、也最稳妥的第一直觉。** 答案直接写出 $-k \le (x^2-y^2)e^{-x^2-y^2} \le k$，其实是标准答案特有的“上帝视角”和“马后炮”写法。

让我帮你把这层窗户纸捅破，你会发现你的思路不仅没错，稍微包装一下，甚至比答案还要巧妙！

### 1. 为什么答案会直接写出 $-k \le A \le k$？

在代数上，绝对值不等式 $|A| \le k$ 的等价定义就是 $-k \le A \le k$。

答案之所以这么写，是因为它想表达：**“求绝对值的最大值，就等于求原函数的最大值和最小值的绝对值，看哪个更大。”**

令 $f(x,y) = (x^2-y^2)e^{-x^2-y^2}$。

题目要求 $|f(x,y)| \le k$ 恒成立，也就是求 $|f(x,y)|$ 的最大值。

答案的逻辑是：只要我求出 $f(x,y)$ 的最大值（假设是 $M$）和最小值（假设是 $m$），那么 $k$ 的最小值就是 $\max(|M|, |m|)$。

这是一种非常“机械化”的程序化思维，它是对的，但没有任何“灵气”。

### 2. 为什么你的“分类讨论”反而更好？（轮换对称性的魔法）

你仔细观察一下这个函数：$|x^2-y^2|e^{-x^2-y^2}$。

如果你把 $x$ 和 $y$ 互换位置，变成 $|y^2-x^2|e^{-y^2-x^2}$，**式子的结果完全没有任何改变！**

这就叫“关于 $x,y$ 的完全对称性”。

既然 $x$ 和 $y$ 的地位是完全平等的，你脑子里的“分类讨论”（分 $x \ge y$ 和 $x < y$）就可以被一句高逼格的数学黑话给秒杀：

**“不妨设 $x \ge y \ge 0$” (Without Loss of Generality)**

你看，只要加上这一句话，你的分类讨论就直接完成了一半，而且绝对值符号被合法、优雅地“脱”掉了：

由于 $x \ge y \ge 0$，所以 $x^2 \ge y^2$，即 $x^2 - y^2 \ge 0$。

原式直接变成了求 $(x^2-y^2)e^{-x^2-y^2}$ 的最大值。

**这才是考场上破题的神仙思路！你离它只有半步之遥。**

---

### 3. 顺理成章地把题目解完

既然思路通了，我们用你的“不妨设”思路来降维打击这道题。

**第一步：利用对称性脱去绝对值**

因为原式关于 $x,y$ 对称，我们不妨设 $x \ge y \ge 0$。

此时 $x^2 - y^2 \ge 0$，目标转化为求函数 $f(x,y) = (x^2-y^2)e^{-x^2-y^2}$ 在该区域上的最大值。

**第二步：换元降次（小技巧）**

带平方不好求导，我们令 $u = x^2, v = y^2$。因为 $x \ge y \ge 0$，所以 $u \ge v \ge 0$。

目标函数变成：$g(u,v) = (u-v)e^{-(u+v)}$。

**第三步：求极值**

我们来找内部的驻点，分别对 $u$ 和 $v$ 求偏导：

- $g'_u = e^{-(u+v)} + (u-v)e^{-(u+v)} \cdot (-1) = e^{-(u+v)}(1 - u + v)$
    
- $g'_v = -e^{-(u+v)} + (u-v)e^{-(u+v)} \cdot (-1) = e^{-(u+v)}(-1 - u + v)$
    

令 $g'_u = 0$ 且 $g'_v = 0$。

因为 $e^{-(u+v)} > 0$ 永远成立，所以括号里必须为 0：

$1 - u + v = 0 \implies u - v = 1$

$-1 - u + v = 0 \implies u - v = -1$

这两个条件互相矛盾！这说明什么？说明**这个函数在区域内部根本没有驻点（极值点）！**

**第四步：看边界**

既然内部没有最大值，最大值一定在边界上。

我们的区域是 $u \ge v \ge 0$，它的边界就是 **$v = 0$**（即 $y = 0$）。

把 $v = 0$ 代入函数：

$h(u) = g(u,0) = u e^{-u}$ （其中 $u \ge 0$）

现在这就变成了一道极其简单的大一上学期的一元函数求最值题：

$h'(u) = e^{-u} - u e^{-u} = e^{-u}(1-u)$

令 $h'(u) = 0$，得到唯一驻点 $u = 1$。

当 $0 \le u < 1$ 时，$h'(u) > 0$，函数单调递增；

当 $u > 1$ 时，$h'(u) < 0$，函数单调递减。

因此，当 $u=1$（即 $x=1, y=0$ 或对称的 $x=0, y=1$）时，函数取得全局最大值：

$h(1) = 1 \cdot e^{-1} = \frac{1}{e}$。

所以，$k$ 的最小值就是 **$\frac{1}{e}$**。

---

**总结一下：**

不要觉得答案那种“夹在中间”的写法高不可攀。答案是为了书面表达的严谨性，舍弃了思考过程。你的直觉是分类讨论脱去绝对值，这是**绝对正确且具有可操作性**的。以后遇到带有绝对值且字母对称的题目，大胆写下“不妨设 $x \ge y$”，直接把绝对值扒掉，你会发现天地瞬间就宽广了！ 



# 8
![[880-1778551742937.jpeg]]这是一个极好的进阶问题！你现在的思维已经从“线”跨越到了“面”。

如果说之前的拉格朗日乘数法是“在一条铁轨（曲线）**上找最高点”，那么这道题的背景就是：“在一个**被栅栏围起来的公园（闭区域）里找最高点”。

面对这种“区域约束”，我们的核心战术叫作“兵分两路，最后总决赛”。

因为在一个封闭公园里，整个公园的最高点只可能出现在两个地方：

1. **公园的内部（不贴着栅栏的地方）**：这通常是一个山头（驻点）。
    
2. **公园的边界上（紧贴着栅栏的地方）**：这就像是公园里有一面斜坡刚好被栅栏截断了，最高点卡在边缘上。
    

所以，解这类题有雷打不动的 **三步曲**：

---

### 第一步：找“公园内部”的候选人（求驻点）

内部是无拘无束的（$x^2 + y^2 < 16$），所以我们直接找目标函数 $z = x^3 - 3x^2 - 3y^2$ 的山头（一阶偏导数等于 0 的点）。

分别对 $x$ 和 $y$ 求偏导：

- $z'_x = 3x^2 - 6x = 3x(x - 2) = 0$ $\implies x = 0$ 或 $x = 2$
    
- $z'_y = -6y = 0$ $\implies y = 0$
    

把它们组合起来，得到两个驻点：**$(0, 0)$** 和 **$(2, 0)$**。

_验算一下它们在不在公园里：$0^2+0^2 \le 16$（在），$2^2+0^2 \le 16$（在）。_

计算这两个点的海拔（函数值）：

- $z(0, 0) = 0$
    
- $z(2, 0) = 8 - 12 - 0 = -4$
    

**第一步结果：内部候选人提供了最高海拔 `0`，最低海拔 `-4`。**

---

### 第二步：找“公园边界”的候选人（处理边界曲线）

公园的边界是栅栏：**$x^2 + y^2 = 16$**。

看到了吗？**这瞬间就变成了你上一题做过的那种“带有等式约束条件”的题目！**

（在这一步，你完全可以使用拉格朗日乘数法来做。但是，这道题有一个极其绝妙的“降维打击”捷径，比拉格朗日快十倍！）

**捷径法则：整体代换**

观察目标函数 $z = x^3 - 3x^2 - 3y^2$ 和边界条件 $x^2 + y^2 = 16$。

既然我们现在只在边界上走，那 $y^2$ 永远等于 $16 - x^2$。我们直接把它代入目标函数里，把 $y$ 彻底消灭！

$$z = x^3 - 3x^2 - 3(16 - x^2)$$

$$z = x^3 - 3x^2 - 48 + 3x^2$$

$$z = x^3 - 48$$

你看！原本复杂的二元函数，在边界上退化成了一个**极其简单的一元函数**！

因为是在圆 $x^2 + y^2 = 16$ 上，所以 $x$ 的取值范围是 $[-4, 4]$。

现在问题变成了：求 $h(x) = x^3 - 48$ 在区间 $[-4, 4]$ 上的最值。

这个函数是单调递增的（因为导数 $3x^2 \ge 0$），所以：

- 当 $x = -4$ 时，取得最小值：$(-4)^3 - 48 = -64 - 48 = -112$。
    
- 当 $x = 4$ 时，取得最大值：$4^3 - 48 = 64 - 48 = 16$。（此时 $y=0$）
    

**第二步结果：边界候选人提供了最高海拔 `16`，最低海拔 `-112`。**

---
 
### 第三步：总决赛 PK

把第一步（内部）和第二步（边界）找到的所有极值拿出来大比拼：

- 内部点：`0`, `-4`
    
- 边界点：`16`, `-112`
    

谁最大？显然是 `16`。

谁最小？显然是 `-112`。

题目求的是最大值，所以最终答案就是 **16**。

---

### 总结你的疑惑

- **问：这种约束条件是区域的怎么办？**
    
- **答：** 把区域“劈开”成两半做。**内部**当成无约束的普通求导找驻点；**边界**当成你上一题做的“等式约束”找极值（能代换消元就代换，不能代换就上拉格朗日）。最后把所有结果放在一起比大小。
    

这就好像选拔全国冠军，第一步选出各省市内部的冠军，第二步选出边疆地区的冠军，最后大家聚在一起比一下，最高的那个人就是全国冠军。


# 9
![[880-1778552398491.jpeg]]

![[880-1778552434667.jpeg]]


# 9 计算的巧劲
![[880-1778552925659.jpeg]]
### 1. 设切点并求切线斜率

设第一象限内曲线上的切点为 $(x_0, y_0)$。

由于点在曲线上，必然满足约束条件：

$$3x_0^2 + 2x_0y_0 + 3y_0^2 = a$$

对方程 $3x^2 + 2xy + 3y^2 = a$ 两边同时对 $x$ 求导（利用隐函数求导法则）：

$$6x + 2y + 2xy' + 6yy' = 0$$

整理得到切线斜率 $y'$：

$$y' = -\frac{3x + y}{x + 3y}$$

所以在点 $(x_0, y_0)$ 处的切线斜率为 $k = -\frac{3x_0 + y_0}{x_0 + 3y_0}$。

### 2. 写出切线方程（第一个巧劲）

利用点斜式写出切线方程：

$$y - y_0 = -\frac{3x_0 + y_0}{x_0 + 3y_0}(x - x_0)$$

把分母乘过去展开：

$$(x_0 + 3y_0)(y - y_0) = -(3x_0 + y_0)(x - x_0)$$

$$(x_0 + 3y_0)y - x_0y_0 - 3y_0^2 = -(3x_0 + y_0)x + 3x_0^2 + x_0y_0$$

把含有 $x$ 和 $y$ 的项移到等式左边，常数项移到右边：

$$(3x_0 + y_0)x + (x_0 + 3y_0)y = 3x_0^2 + 2x_0y_0 + 3y_0^2$$

**绝妙的地方来了！** 你看等式右边，这一长串刚好就是原曲线方程的表达式。所以右边直接整体替换成 $a$。

切线方程极大简化为：

$$(3x_0 + y_0)x + (x_0 + 3y_0)y = a$$

### 3. 求截距与三角形面积

既然要求与坐标轴围成的面积，我们就求出 $x$ 轴和 $y$ 轴的截距。

- 令 $y = 0$，得到 $x$ 轴截距：$X = \frac{a}{3x_0 + y_0}$
    
- 令 $x = 0$，得到 $y$ 轴截距：$Y = \frac{a}{x_0 + 3y_0}$
    

因为切点在第一象限且 $a > 0$，所以截距均为正数。三角形面积 $S$ 为：

$$S = \frac{1}{2}XY = \frac{a^2}{2(3x_0 + y_0)(x_0 + 3y_0)}$$

展开分母：

$$S = \frac{a^2}{2(3x_0^2 + 10x_0y_0 + 3y_0^2)}$$

### 4. 求面积的最小值（第二个巧劲）

要求面积 $S$ 的最小值，也就是要求分母的**最大值**。

让我们重点观察分母括号里的部分：$3x_0^2 + 10x_0y_0 + 3y_0^2$。

我们手头唯一可以利用的条件依然是切点在曲线上：$3x_0^2 + 2x_0y_0 + 3y_0^2 = a$。

为了利用这个条件，我们将分母强行拆开凑数：

$$3x_0^2 + 10x_0y_0 + 3y_0^2 = (3x_0^2 + 2x_0y_0 + 3y_0^2) + 8x_0y_0 = a + 8x_0y_0$$

现在，求分母最大值的问题转化成了：**如何求 $x_0y_0$ 的最大值？**

这时候就要请出基本不等式（均值不等式）了。对于约束条件 $3x_0^2 + 3y_0^2 + 2x_0y_0 = a$：

因为 $3x_0^2 + 3y_0^2 \ge 2\sqrt{(3x_0^2)(3y_0^2)} = 6x_0y_0$ （当且仅当 $x_0 = y_0$ 时取等号）。

所以：

$$a = 3x_0^2 + 3y_0^2 + 2x_0y_0 \ge 6x_0y_0 + 2x_0y_0 = 8x_0y_0$$

即 $8x_0y_0 \le a$。

把这个极值结果代回分母：

分母最大值为：$a + a = 2a$。

此时，面积 $S$ 取得最小值：

$$S_{\text{min}} = \frac{a^2}{2 \cdot 2a} = \frac{a^2}{4a} = \frac{a}{4}$$

### 5. 计算最终结果

题目已知面积最小值为 **1/4**，所以：

$$\frac{a}{4} = \frac{1}{4}$$

解得最终答案：**$a = 1$**。

# 10
![[880-1778553611418.jpeg]]

### 1. 解开认知结：为什么要“兵分两路”？

题目说“利用变换 $\xi = x + ay, \eta = x + by$”，这句话蕴含的信息是：

原本 $u$ 是直接关于 $x$ 和 $y$ 的函数，即 $u(x, y)$。

现在我们戴上了一副“新眼镜”，用 $\xi$ 和 $\eta$ 来描述这个世界。此时 $u$ 变成了关于 $\xi$ 和 $\eta$ 的函数，即 $u(\xi, \eta)$。

但是，**$\xi$ 和 $\eta$ 身体里都流着 $x$ 的血！**

- $\xi$ 里面有 $x$ （$\xi = x + ay$）
    
- $\eta$ 里面也有 $x$ （$\eta = x + by$）
    

当你微微改变一下 $x$ 的时候，会发生什么？

**$\xi$ 会动，$\eta$ 也会动！**

而 $\xi$ 和 $\eta$ 的变动，**都会**导致 $u$ 发生变动。

所以，你想求“$u$ 对 $x$ 的总变化率（偏导数）”，就必须把 $x$ 引起的**所有连锁反应都加起来**。

这就好比，你（$x$）同时投资了 A 公司（$\xi$）和 B 公司（$\eta$），这两家公司又都给你分红（$u$）。你算自己的总收益时，能只算 A 公司的分红，不管 B 公司的分红吗？当然不行！必须加在一起：

$$\frac{\partial u}{\partial x} = \underbrace{\frac{\partial u}{\partial \xi} \cdot \frac{\partial \xi}{\partial x}}_{\text{通过 } \xi \text{ 的路径}} + \underbrace{\frac{\partial u}{\partial \eta} \cdot \frac{\partial \eta}{\partial x}}_{\text{通过 } \eta \text{ 的路径}}$$

因为 $\xi = x + ay, \eta = x + by$，所以 $\frac{\partial \xi}{\partial x} = 1$，$\frac{\partial \eta}{\partial x} = 1$。

代入进去，得到一阶偏导数的核心公式：

$$\frac{\partial u}{\partial x} = \frac{\partial u}{\partial \xi} + \frac{\partial u}{\partial \eta}$$

同理，对 $y$ 求导时，$y$ 也同时影响着 $\xi$ 和 $\eta$：

$$\frac{\partial u}{\partial y} = \frac{\partial u}{\partial \xi} \cdot \frac{\partial \xi}{\partial y} + \frac{\partial u}{\partial \eta} \cdot \frac{\partial \eta}{\partial y} = a\frac{\partial u}{\partial \xi} + b\frac{\partial u}{\partial \eta}$$

---

### 2. 攻克二阶偏导：把一阶偏导变成“算子”

你对二阶偏导没有头绪，是因为如果直接把一长串式子硬套进去求导，很容易晕头转向。

这里必须引入一个极其好用的降维工具：**算子（Operator）化**。

我们把刚才求出的一阶偏导写成“剥离掉 $u$”的形式（就好像把函数的躯壳抽走，只留下求导的动作）：

- $\frac{\partial}{\partial x} = \frac{\partial}{\partial \xi} + \frac{\partial}{\partial \eta}$
    
- $\frac{\partial}{\partial y} = a\frac{\partial}{\partial \xi} + b\frac{\partial}{\partial \eta}$
    

现在，二阶偏导就变成了极其简单的“初中多项式乘法（完全平方公式）”！

**求 $\frac{\partial^2 u}{\partial x^2}$：**

就是对 $x$ 求两次导，等于上面第一个算子自己乘自己，然后作用于 $u$：

$$\frac{\partial^2 u}{\partial x^2} = \left(\frac{\partial}{\partial \xi} + \frac{\partial}{\partial \eta}\right) \left(\frac{\partial}{\partial \xi} + \frac{\partial}{\partial \eta}\right) u$$

展开（像 $(A+B)^2$ 一样）：

$$\frac{\partial^2 u}{\partial x^2} = \frac{\partial^2 u}{\partial \xi^2} + 2\frac{\partial^2 u}{\partial \xi \partial \eta} + \frac{\partial^2 u}{\partial \eta^2}$$

**求 $\frac{\partial^2 u}{\partial y^2}$：**

就是第二个算子自己乘自己：

$$\frac{\partial^2 u}{\partial y^2} = \left(a\frac{\partial}{\partial \xi} + b\frac{\partial}{\partial \eta}\right)^2 u$$

展开：

$$\frac{\partial^2 u}{\partial y^2} = a^2\frac{\partial^2 u}{\partial \xi^2} + 2ab\frac{\partial^2 u}{\partial \xi \partial \eta} + b^2\frac{\partial^2 u}{\partial \eta^2}$$

**求 $\frac{\partial^2 u}{\partial x \partial y}$（混合偏导）：**

就是一个 $x$ 算子乘以一个 $y$ 算子：

$$\frac{\partial^2 u}{\partial x \partial y} = \left(\frac{\partial}{\partial \xi} + \frac{\partial}{\partial \eta}\right) \left(a\frac{\partial}{\partial \xi} + b\frac{\partial}{\partial \eta}\right) u$$

展开（像 $(A+B)(C+D)$ 一样）：

$$\frac{\partial^2 u}{\partial x \partial y} = a\frac{\partial^2 u}{\partial \xi^2} + (a+b)\frac{\partial^2 u}{\partial \xi \partial \eta} + b\frac{\partial^2 u}{\partial \eta^2}$$

你看，最吓人的二阶偏导，其实就是三道多项式展开题！

---

### 3. 收网：求出 $a$ 和 $b$

现在，我们把这三个“大礼包”代回题目给的原始方程：

$$\frac{\partial^2 u}{\partial x^2} + 4\frac{\partial^2 u}{\partial x \partial y} + 3\frac{\partial^2 u}{\partial y^2} = 0$$

不要慌着一锅乱炖，我们**按项合并同类项**：

- **含有 $\frac{\partial^2 u}{\partial \xi^2}$ 的系数：**
    
    $1 + 4a + 3a^2$
    
- **含有 $\frac{\partial^2 u}{\partial \eta^2}$ 的系数：**
    
    $1 + 4b + 3b^2$
    
- **含有 $\frac{\partial^2 u}{\partial \xi \partial \eta}$ 的系数：**
    
    $2 + 4(a+b) + 3(2ab) = 2 + 4a + 4b + 6ab$
    

把它们拼起来，方程变成了：

$$(3a^2+4a+1)\frac{\partial^2 u}{\partial \xi^2} + (3b^2+4b+1)\frac{\partial^2 u}{\partial \eta^2} + (6ab+4a+4b+2)\frac{\partial^2 u}{\partial \xi \partial \eta} = 0$$

题目要求：这个方程要化为 $\frac{\partial^2 u}{\partial \xi \partial \eta} = 0$ 的形式。

这意味着，**前两项的系数必须等于 0 被彻底消灭掉！**

所以我们得到了关于 $a$ 和 $b$ 的方程：

$$3a^2 + 4a + 1 = 0 \implies (3a+1)(a+1) = 0 \implies a = -1 \text{ 或 } -1/3$$

$$3b^2 + 4b + 1 = 0 \implies (3b+1)(b+1) = 0 \implies b = -1 \text{ 或 } -1/3$$

因为 $\xi$ 和 $\eta$ 是两个独立的变换（如果 $a=b$，那 $\xi$ 就等于 $\eta$ 了，变换就失去了降维的意义），所以 $a$ 和 $b$ 不能相等。

结论得出，求得的值为：

**$a = -1, b = -1/3$** （或者 $a = -1/3, b = -1$，由于地位对称，写哪个都可以）。