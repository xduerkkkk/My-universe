**一个矩阵的迹，等于它所有特征值之和。**


可逆矩阵，


#### 1. 绝对自由的互换（**不需要**可逆）
**转置 $(T)$ 和伴随 $(*)$ 之间的互换是绝对自由的。**
$$ (A^T)^* = (A^*)^T $$
这个公式对**任何** $n$ 阶方阵都成立，不管它可不可逆，哪怕它是零矩阵也成立。因为求转置和求代数余子式这两个动作互不干扰。

#### 2. 有条件的互换（**必须**可逆）
只要互换的帽子里包含了**逆矩阵 $(-1)$**，那就**必须**要求矩阵可逆。
*   $(A^T)^{-1} = (A^{-1})^T$
*   $(A^*)^{-1} = (A^{-1})^*$
道理很简单：如果矩阵不可逆，连 $(-1)$ 这个符号都没有资格写出来，更别提互换了。