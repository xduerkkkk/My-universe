# 1
![[_attachments/image/03大观题-1786065836471.jpeg]]


![[_attachments/image/03大观题-1786065847982.jpeg]]


# 2

![[_attachments/image/03大观题-1786066191899.jpeg]]


这是一个在考研数一中**极其经典且能“秒杀”计算量**的高级技巧。

如果你在考场上把这三个平方项硬生生展开、合并同类项，然后再去凑实对称矩阵 $A$，你至少要花 5 分钟，而且极容易算错系数。这张图上的手写笔记，就是为了教你如何绕过这片“雷区”。

我们分三步来吃透这个解法：

### 1. 结构理解：矩阵 $B$ 是哪来的？
观察题目给的二次型，它是一个很特殊的结构：**纯粹的几个平方项相加**。
我们可以在脑子里做一次“换元”（线性变换）：
*   令 $y_1 = x_1 - x_2 + x_3$
*   令 $y_2 = 0x_1 + x_2 + x_3$
*   令 $y_3 = x_1 + 0x_2 + ax_3$

把这三个方程的系数直接拎出来排好，就是图上手写的那个矩阵 $B$。
用矩阵语言表达就是：$Y = BX$。

### 2. 数学推理：为什么 $A = B^T B$？
原二次型现在变成了非常清爽的：$f = y_1^2 + y_2^2 + y_3^2$。
把这个“平方和”写成矩阵相乘的形式，就是列向量自己的内积：$f = Y^T Y$。

接下来，见证奇迹：
我们把 $Y = BX$ 代进去：
$f = (BX)^T (BX)$
利用转置穿脱原则：
$f = X^T B^T B X$

对比标准二次型的定义式 $f = X^T A X$，结论呼之欲出：
这个二次型对应的矩阵 **$A = B^T B$**！

### 3. 应试方法（这道题怎么算？）

到了这一步，我们需要动用考研线代中一个必须“焊死在脑子里”的定理：
**对于任何实矩阵 $B$，都有 $r(B^T B) = r(B)$**。
（也就是说，一个矩阵乘上自己的转置，秩绝对不会改变）。

现在我们回到这道题的求解流程，只需三步，纯“秒杀”：

1.  **翻译条件**：题目说二次型的矩阵 $A$ 秩为 2，也就是 $r(A) = r(B^T B) = r(B) = 2$。
2.  **找突破口**：矩阵 $B$ 是一个 $3 \times 3$ 的方阵，最高秩应该是 3。现在它的秩是 2，说明它**不满秩**！
3.  **计算**：方阵不满秩的绝对等价条件是**它的行列式等于 0**。
    所以，直接算 $|B| = 0$ 即可：
    $$ |B| = \begin{vmatrix} 1 & -1 & 1 \\ 0 & 1 & 1 \\ 1 & 0 & a \end{vmatrix} $$
    按第一列展开：$1 \cdot (a - 0) + 1 \cdot (-1 - 1) = a - 2$
    令 $a - 2 = 0$，解得 **$a = 2$**。

**总结一下考场肌肉记忆：**
以后只要看到二次型是**“几个完全平方项的和”**，别犹豫：
直接把括号里的系数抄下来当成矩阵 $B$，后面所有关于秩、正定性、甚至特征值的问题，全都可以通过分析 $B$ 或 $B^TB$ 来解决，绝对不要去展开多项式！


# 3
![[_attachments/image/03大观题-1786067765872.jpeg]]


![[_attachments/image/03大观题-1786067754848.jpeg]]

求合同规范型.  
标准型是二次型没有交叉项的情况，规范型是进一步，令标准型的系数为+-1，求规范型，是将实对称矩阵 A  
化为没有交叉项且系数为+-1 的情况

由惯性定理知，规范型正负系数与 A 的特征值正负个数完全相关  
但首先，这个 A 不是标准的二次型矩阵，应先化为实对称矩阵  
使用公式 A+A^T/2  
然后试图找出这个标准矩阵的特征值正负

接下来才用题目条件：一堆单位正交向量，标准矩阵式子还有转置，那就尝试乘各个向量

构造出特征值一种构造是通过加减式子得到 Bx=λx 一种是，构造矩阵。

# 4
![[_attachments/image/03大观题-1786073600504.jpeg]]

![[_attachments/image/03大观题-1786073611305.jpeg]]


# 5
![[_attachments/image/03大观题-1786075008805.jpeg]]


![[_attachments/image/03大观题-1786075387600.jpeg]]
# 6
![[_attachments/image/03大观题-1786075369706.jpeg]]


![[_attachments/image/03大观题-1786075394561.jpeg]]



![[_attachments/image/03大观题-1786075399202.jpeg]]



# 7
![[_attachments/image/03大观题-1786153835379.jpeg]]


# 8
![[_attachments/image/03大观题-1786154139378.jpeg]]


针对你的两个疑惑，我先进行概念上的纠正和空间上的解释，然后再秒杀这道题。

### 一、概念解答（直击实对称矩阵的核心）

**1. “一提到 3 阶实对称矩阵，是一定有三个特征值吗？”**
**是的，但不仅仅是实对称矩阵。** **任何** $3$ 阶方阵（不管对不对称）都有 $3$ 个特征值（因为特征方程是个 $3$ 次多项式，代数基本定理保证必有 $3$ 个根，包含重根）。
实对称矩阵真正强大的地方在于：它的这 $3$ 个特征值**绝对都是实数**（不会出现虚数）。

**2. “特征值一定有不共线的特征向量？”**
**你的记忆非常准确！这是实对称矩阵最强悍的性质！**
一般的矩阵如果遇到“重根”（比如 $\lambda_1 = \lambda_2$），可能会出现特征向量缺失的情况（无法对角化）。
但**实对称矩阵绝对不会缺失特征向量**！哪怕它有 $2$ 重甚至 $3$ 重特征值，它也**必定能找到对应数量的、线性无关（不共线）的特征向量，而且这些向量还能做到两两垂直（正交）**。
*   **空间理解**：一般的矩阵变换可能包含“扭曲（剪切）”空间的动作。但**实对称矩阵**在空间上代表一种**“纯粹的拉伸或压缩”**。既然是纯拉伸，必定存在 $3$ 个互相垂直的拉伸轴（这就是正交特征向量），沿着这些轴只改变长度，不改变方向。

---

### 二、回到题目：如何秒杀这道题？

这道题是考研的超高频标准题型，考察的是**“通过特征值来确定规范型”**（也就是我们最开始说的**惯性定理**：几个正特征值就有几个正号，几个负特征值就有几个负号）。

**第一步：利用矩阵多项式，圈定特征值的“候选人”**
已知 $A^2 + A = 2E$。
设 $A$ 的特征值为 $\lambda$，那么 $\lambda$ 必须满足同样的方程：
$\lambda^2 + \lambda = 2 \implies \lambda^2 + \lambda - 2 = 0 \implies (\lambda - 1)(\lambda + 2) = 0$
解得：**$\lambda$ 只能取 $1$ 或 $-2$**。

**第二步：利用行列式，锁定具体的特征值**
已知 $|A| = 4$。
矩阵的行列式 = 特征值的乘积。设 $3$ 个特征值为 $\lambda_1, \lambda_2, \lambda_3$。
我们要从候选人 $\{1, -2\}$ 中挑出 $3$ 个数字（可以重复），让它们的乘积等于 $4$。
试一下：
*   全选 $1$：$1 \times 1 \times 1 = 1$ （错）
*   两个 $1$ 一个 $-2$：$1 \times 1 \times (-2) = -2$ （错）
*   **一个 $1$ 两个 $-2$：$1 \times (-2) \times (-2) = 4$ （对！）**
所以，$A$ 的三个特征值必然是：**$1, -2, -2$**。

**第三步：用惯性定理写出规范型**
特征值为 $1, -2, -2$。
*   正特征值有 $1$ 个 $\implies$ 正惯性指数为 $1$。
*   负特征值有 $2$ 个 $\implies$ 负惯性指数为 $2$。
因此，规范型一定是**“一个正号，两个负号”**。

直接看选项：
(A) 三个正（错）
(B) 两正一负（错）
**(C) 一正两负（正确！）**
(D) 三个负（错）

**答案选 C。**

---

### 三、考研应试方法总结

在考场上看到**抽象实对称矩阵**（没给具体数字）求规范型或标准型，**千万别去想特征向量怎么求，根本不需要！**
直接走以下三步流水线：
1.  **给方程（如 $A^2+A=2E$）** $\rightarrow$ 解出特征值 $\lambda$ 的可能取值。
2.  **给条件（行列式 $|A|$、迹 $tr(A)$、秩 $r(A)$）** $\rightarrow$ 列式子组合出确切的 $3$ 个特征值。（乘积= $|A|$，相加=迹，非零个数=秩）。
3.  **看正负** $\rightarrow$ 数出正负特征值个数，直接在选项里找对应正负号个数的规范型。
# 9 （多题）
![[_attachments/image/03大观题-1786158108184.jpeg]]

![[_attachments/image/03大观题-1786158120876.jpeg]] 

' ![[_attachments/image/03大观题-1786158611660.jpeg]] '


![[_attachments/image/03大观题-1786158730797.jpeg]]


![[_attachments/image/03大观题-1786160722649.jpeg]]



![[_attachments/image/03大观题-1786174114748.jpeg]]


![[_attachments/image/03大观题-1786174176070.jpeg]]


# 9
![[_attachments/image/03大观题-1786178554293.jpeg]]


# 10
![[_attachments/image/03大观题-1786179055416.jpeg]]



---

### 一、 核心铁律：左乘行，右乘列

矩阵乘法的本质规则必须刻在脑子里：
*   **左乘一个矩阵 $\Longleftrightarrow$ 对目标矩阵做“行向量”的线性组合（行操作）。**
*   **右乘一个矩阵 $\Longleftrightarrow$ 对目标矩阵做“列向量”的线性组合（列操作）。**

---

### 二、 为什么本题只能右乘？（$Q = PM$）

看题目给出的已知条件：
1.  $P = (e_1, e_2, e_3)$：这里把 $e_1, e_2, e_3$ 明确写成了 **$P$ 的三列（列向量）**。
2.  $Q = (e_1, -e_3, e_1 + e_2)$：题目给出的 $Q$，它的第一列、第二列、第三列，**全部是由 $P$ 的列向量组合出来的**！

*   $Q$ 的第 1 列 = $1 \cdot e_1 + 0 \cdot e_2 + 0 \cdot e_3$
*   $Q$ 的第 2 列 = $0 \cdot e_1 + 0 \cdot e_2 - 1 \cdot e_3$
*   $Q$ 的第 3 列 = $1 \cdot e_1 + 1 \cdot e_2 + 0 \cdot e_3$

既然是**把 $P$ 的“列向量”重新组合拼成 $Q$ 的“列向量”**，根据“右乘列”的铁律，就**必须且只能在 $P$ 的右边乘上系数矩阵 $M$**：

$$Q = P M = (e_1, e_2, e_3) \begin{pmatrix} 1 & 0 & 1 \\ 0 & 0 & 1 \\ 0 & -1 & 0 \end{pmatrix}$$

#### 假如你强行用左乘（$Q = N P$），会发生什么？
如果你写 $Q = N P$，这意味着你在用 $N$ 去对 $P$ 的**行向量**做组合。
但题目根本没告诉你 $P$ 的“行”长什么样，你手头只有 $P$ 的“列” $e_1, e_2, e_3$。你根本找不到一个固定的常数矩阵 $N$ 来满足左乘。所以**左乘在这里完全行不通**。

---

### 三、 这道题的极速完整求解（体会右乘的精妙）

既然确定了 $Q = P M$，其中 $M = \begin{pmatrix} 1 & 0 & 1 \\ 0 & 0 & 1 \\ 0 & -1 & 0 \end{pmatrix}$。

**1. 题目已知：**
$x = Py$ 可以将 $f$ 化为标准型 $2y_1^2 + 2y_2^2 + 3y_3^2$。
这意味着：$$P^T A P = \Lambda = \begin{pmatrix} 2 & 0 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 3 \end{pmatrix}$$

**2. 现在令 $x = Qy$：**
二次型变成了 $y^T (Q^T A Q) y$。我们只需要求中间的矩阵 **$Q^T A Q$**：

把 $Q = PM$ 代入：
$$Q^T A Q = (PM)^T A (PM) = M^T \mathbf{(P^T A P)} M = M^T \mathbf{\Lambda} M$$

**3. 代入纯数字矩阵计算 $M^T \Lambda M$：**
$$\Lambda M = \begin{pmatrix} 2 & 0 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 3 \end{pmatrix} \begin{pmatrix} 1 & 0 & 1 \\ 0 & 0 & 1 \\ 0 & -1 & 0 \end{pmatrix} = \begin{pmatrix} 2 & 0 & 2 \\ 0 & 0 & 2 \\ 0 & -3 & 0 \end{pmatrix}$$

$$M^T (\Lambda M) = \begin{pmatrix} 1 & 0 & 0 \\ 0 & 0 & -1 \\ 1 & 1 & 0 \end{pmatrix} \begin{pmatrix} 2 & 0 & 2 \\ 0 & 0 & 2 \\ 0 & -3 & 0 \end{pmatrix} = \begin{pmatrix} 2 & 0 & 2 \\ 0 & 3 & 0 \\ 2 & 0 & 4 \end{pmatrix}$$

**4. 还原为二次型：**
由 $\begin{pmatrix} 2 & 0 & 2 \\ 0 & 3 & 0 \\ 2 & 0 & 4 \end{pmatrix}$ 还原出二次型：
$$2y_1^2 + 3y_2^2 + 4y_3^2 + 2 \times 2 y_1 y_3 = \mathbf{2y_1^2 + 3y_2^2 + 4y_3^2 + 4y_1 y_3}$$

**秒选 D 选项！**

---

### 四、 考场肌肉记忆口诀

* **按列拼接/列组合**（如 $Q = (e_1, e_2+e_3, \dots)$） $\implies$ **果断右乘 $Q = PM$**，利用 $Q^T A Q = M^T \Lambda M$ 纯数字计算秒杀。
* **按行拼接/行组合**（极罕见） $\implies$ **左乘 $Q = NP$**。考研 99% 的题给的都是列向量，认准**右乘**即可！


# 11
![[_attachments/image/03大观题-1786191416926.jpeg]]


![[_attachments/image/03大观题-1786191379630.jpeg]]

你看问题的直觉非常敏锐！两道题在形式上确实非常像，都是给你两个二次型 $f(x)$ 和 $g(y)$，让你求变换矩阵。

但是，**这两道题的解法有着天壤之别！如果这道题你照搬上一题（求特征向量 $Q = Q_1 Q_2^T$）的方法，直接爆零！**

为什么？我用最清晰的对比带你看清其中的**巨坑**。

---

### 一、核心区别：相似 vs 合同

| 题目对比 | 关系 | 特征值是否相同？ | 能否用特征向量拼矩阵？ | 桥梁（中介）是什么？ |
| :--- | :--- | :--- | :--- | :--- |
| **上一题（正交变换 $Q$）** | $Q^T A Q = B$（正交等价） | **完全相同** ($\Lambda_A = \Lambda_B$) | **能！** ($Q = Q_1 Q_2^T$) | **标准型（对角阵 $\Lambda$）** |
| **本题（可逆变换 $P$）** | $P^T A P = B$（仅仅合同） | **不相同！** ($\Lambda_A \neq \Lambda_B$) | **绝对不能！** | **规范型（$z_1^2 + z_2^2$）** |

**【关键暴击】**：
上一题是**正交变换**，正交变换保证了 $A$ 和 $B$ **相似**，所以它们特征值一样，可以用对角阵 $\Lambda$ 做中介。
而这道题是**可逆变换**，只能保证 $A$ 和 $B$ **合同**，**它们的特征值根本不一样！**
*（我们可以验证一下：$A$ 的特征值是 $\frac{3}{2}, \frac{3}{2}, 0$；而 $B$ 的特征值是 $2, 4, 0$。特征值都不一样，你上哪去搞 $Q_1 Q_2^T$ ？）*

---

### 二、这道题第二问的真正解法：用“配方法/规范型”做桥梁

既然特征值不同，无法用对角阵 $\Lambda$ 连接，我们就用**合同的不变量——规范型**来做连接！

**【解题思路】**：
1. 用配方法把 $f(x)$ 化为规范型，得到 $x$ 和 $z$ 的关系：$z = C_1 x$。
2. 用配方法把 $g(y)$ 化为规范型，得到 $y$ 和 $z$ 的关系：$z = C_2 y$。
3. 联立消去 $z$：$C_1 x = C_2 y \implies x = C_1^{-1} C_2 y$。
4. 从而求出目标矩阵 **$P = C_1^{-1} C_2$**。

---

### 三、第二问标准解题全程演示

已求得 $a = -\frac{1}{2}$。

#### **第一步：对 $f(x)$ 配方（找到 $x$ 与规范型 $z$ 的关系）**
$$f(x_1, x_2, x_3) = x_1^2 + x_2^2 + x_3^2 - x_1 x_2 - x_1 x_3 - x_2 x_3$$

按死磕 $x_1$ 的顺序配方：
$$f = \left(x_1 - \frac{1}{2}x_2 - \frac{1}{2}x_3\right)^2 + \frac{3}{4}x_2^2 + \frac{3}{4}x_3^2 - \frac{3}{2}x_2 x_3$$
$$f = \left(x_1 - \frac{1}{2}x_2 - \frac{1}{2}x_3\right)^2 + \frac{3}{4}(x_2 - x_3)^2$$

令新变量 $z$ 为：
$$\begin{cases} z_1 = x_1 - \frac{1}{2}x_2 - \frac{1}{2}x_3 \\ z_2 = \frac{\sqrt{3}}{2}x_2 - \frac{\sqrt{3}}{2}x_3 \\ z_3 = x_3 \quad (\text{凑数的充要变量}) \end{cases}$$

写成矩阵形式 $z = C_1 x$：
$$C_1 = \begin{pmatrix} 1 & -\frac{1}{2} & -\frac{1}{2} \\ 0 & \frac{\sqrt{3}}{2} & -\frac{\sqrt{3}}{2} \\ 0 & 0 & 1 \end{pmatrix}$$
此时 $f$ 化为了规范型 $z_1^2 + z_2^2$。

---

#### **第二步：对 $g(y)$ 配方（找到 $y$ 与规范型 $z$ 的关系）**
$$g(y_1, y_2, y_3) = y_1^2 + y_2^2 + 4y_3^2 + 2y_1 y_2 = (y_1 + y_2)^2 + 4y_3^2$$

令新变量 $z$ 为：
$$\begin{cases} z_1 = y_1 + y_2 \\ z_2 = 2y_3 \\ z_3 = y_2 \quad (\text{凑数的充要变量}) \end{cases}$$

写成矩阵形式 $z = C_2 y$：
$$C_2 = \begin{pmatrix} 1 & 1 & 0 \\ 0 & 0 & 2 \\ 0 & 1 & 0 \end{pmatrix}$$
此时 $g$ 也化为了同一个规范型 $z_1^2 + z_2^2$。

---

#### **第三步：求解目标矩阵 $P$**

由 $z = C_1 x$ 且 $z = C_2 y$，得：
$$C_1 x = C_2 y \implies x = C_1^{-1} C_2 y$$

对比 $x = Py$，可知：**$P = C_1^{-1} C_2$**。

1. **求 $C_1^{-1}$**（初等行变换或公式法）：
   $$C_1^{-1} = \begin{pmatrix} 1 & \frac{1}{\sqrt{3}} & 1 \\ 0 & \frac{2}{\sqrt{3}} & 1 \\ 0 & 0 & 1 \end{pmatrix}$$

2. **计算矩阵乘法 $P = C_1^{-1} C_2$**：
   $$P = \begin{pmatrix} 1 & \frac{1}{\sqrt{3}} & 1 \\ 0 & \frac{2}{\sqrt{3}} & 1 \\ 0 & 0 & 1 \end{pmatrix} \begin{pmatrix} 1 & 1 & 0 \\ 0 & 0 & 2 \\ 0 & 1 & 0 \end{pmatrix} = \begin{pmatrix} 1 & 2 & \frac{2}{\sqrt{3}} \\ 0 & 1 & \frac{4}{\sqrt{3}} \\ 0 & 1 & 0 \end{pmatrix}$$

*(注意：配方凑变量时选择不唯一，所以 $P$ 的答案不唯一，考研只要写出一个行列式不为 0 且满足条件的 $P$ 即给满分！)*

---

### 四、考场辨析大总结

遇到“求变换矩阵”的大题，先看题目要求：

1. 如果题目写 **“求正交矩阵 $Q$”**：
   * 结论：$A$ 和 $B$ 必定**相似**（特征值相同）。
   * 做法：老老实实求特征值、单位正交特征向量，拼出 $Q = Q_1 Q_2^T$。
2. 如果题目写 **“求可逆矩阵 $P$”**：
   * 结论：$A$ 和 $B$ 仅仅**合同**（特征值通常不同，但正负惯性指数相同）。
   * 做法：**千万别求特征向量！** 分别对 $f(x)$ 和 $g(y)$ **配方**，写出 $z = C_1 x$ 和 $z = C_2 y$，最后求 $P = C_1^{-1} C_2$。
![[_attachments/image/03大观题-1786192225107.jpeg]]

这种方法看似是“黑科技”，**本质上就是我们刚才讲的“规范型中介法”的终极简化版（偷懒速算法）！**

它之所以绝对正确，背后的数学原理非常漂亮。我用三句话带你彻底秒懂：

---

### 一、核心原理：等式右边的“中介”被隐形了

1. **第一步：把系数吸收进括号**
   对 $f$ 和 $g$ 配方后，把前面的常数项（比如 $\frac{3}{4}$ 和 $4$）开平方吸进括号里：
   * $f = \left(x_1 - \frac{1}{2}x_2 - \frac{1}{2}x_3\right)^2 + \left[\frac{\sqrt{3}}{2}(x_2 - x_3)\right]^2$
   * $g = (y_1 + y_2)^2 + (2y_3)^2$

2. **第二步：看清它们的本质（规范型）**
   如果我令规范型变量为 $z_1, z_2$，那么：
   * 对 $f$ 来说：只要令 $\begin{cases} z_1 = x_1 - \frac{1}{2}x_2 - \frac{1}{2}x_3 \\ z_2 = \frac{\sqrt{3}}{2}(x_2 - x_3) \end{cases}$，那么 $f$ 就变成了规范型 **$z_1^2 + z_2^2$**。
   * 对 $g$ 来说：只要令 $\begin{cases} z_1 = y_1 + y_2 \\ z_2 = 2y_3 \end{cases}$，那么 $g$ 也变成了同一个规范型 **$z_1^2 + z_2^2$**。

3. **第三步：令“中介 $z$”相等！**
   既然 $f$ 和 $g$ 最终都要等于同一个规范型 $z_1^2 + z_2^2$，那**直接让它们的 $z_1$ 等于 $z_1$，$z_2$ 等于 $z_2$ 不就行了吗？！**
   这就得到了图里的两个等式：
   $$\begin{cases} x_1 - \frac{1}{2}x_2 - \frac{1}{2}x_3 = y_1 + y_2 \\ \frac{\sqrt{3}}{2}(x_2 - x_3) = 2y_3 \end{cases}$$

**只要这两个等式成立，代入进去后，$f(x)$ 必然恒等于 $g(y)$！**

---

### 二、为什么必须补第三个方程？

因为我们是在 **三维空间** 探讨问题。
* $x$ 有三个未知数（$x_1, x_2, x_3$）。
* 上面只有两个方程，属于“欠定方程组”，是解不出确定的 $x_1, x_2, x_3$ 的（有无穷多组解）。

**为什么可以随便补第三个方程？**
因为规范型是 $z_1^2 + z_2^2 + \mathbf{0 \cdot z_3^2}$，第三个变量 $z_3$ 前面的系数是 **$0$**！
这意味着 $z_3$ 无论怎么取，平方乘以 $0$ 之后对二次型的值没有任何影响。

所以，我们只需要**随便补充第三个线性无关的方程**（比如补一个最简单的 $x_3 = y_2$ 或 $x_3 = y_3$），**凑满 3 个方程，确保求出来的矩阵 $P$ 行列式不为 0（可逆）**，就能直接解出 $x_1, x_2, x_3$，从而直接写出 $x = Py$！

---

### 三、这种秒算法为什么比求矩阵逆快 10 倍？

对比一下两种写法：

* **正统方法（我上一个回答）**：
  先写出 $z = C_1 x$ 的矩阵，再求逆矩阵 $C_1^{-1}$（3×3 矩阵求逆极易算错），再写出 $z = C_2 y$ 的矩阵，最后做矩阵乘法 $P = C_1^{-1} C_2$。
* **图中的秒算法**：
  直接让括号等于括号，随便补一个最简单的第三方程（如 $x_3 = y_3$），然后像初中解方程组一样，用代入消元法把 $x_1, x_2, x_3$ 用 $y_1, y_2, y_3$ 表示出来，写成矩阵 $x = Py$。

**考场结论：** 
只要考题是 **“求可逆矩阵 $P$ 使 $f(x)$ 变 $g(y)$”**，强烈推荐用图中这种**“把系数吸进括号，直接令括号等于括号”**的秒算法！既不需要求逆矩阵，也不需要做矩阵乘法，解个三元一次方程组就能拿满分！


![[_attachments/image/03大观题-1786193233564.jpeg]]



# 12
![[_attachments/image/03大观题-1786322036314.jpeg]]




![[_attachments/image/03大观题-1786322313967.jpeg]] 答案的逻辑其实非常直观，它的核心是在验证**“这个线性变换到底可不可逆”**。

我们可以把答案的思考路径拆解如下：

---

### 一、 解读答案的逻辑

#### 1. 当 $a \neq 2$ 时：为什么能直接得出 $y_1^2 + y_2^2 + y_3^2$？
* 二次型做变换的前提是：**必须是“可逆线性变换”**（也就是不能丢失维数）。
* 答案设三个括号为 $y_1, y_2, y_3$，写成矩阵形式就是 $y = Ax$。
* 答案算了一下系数矩阵的行列式 $|A| = a - 2 \neq 0$，说明这个变换是**可逆的**！
* 既然是合法的可逆变换，且变换后的表达式 $y_1^2 + y_2^2 + y_3^2$ 的系数全为 $+1$，那它**天然就是规范形**，直接得出结论。

#### 2. 当 $a = 2$ 时：为什么答案突然去展开并重新配方？
* 当 $a = 2$ 时，$|A| = 0$，这意味着 **$y = Ax$ 不是可逆变换**（三个括号线性相关，丢失了一个自由度）。
* 答案因为不能直接用 $y_1, y_2, y_3$ 了，所以退而求其次，使用了最传统的**拉格朗日配方法**：
  1. 把 $a = 2$ 代回原式，硬展开成一般的二次型多项式；
  2. 对 $x_1, x_2, x_3$ 重新配方，配成了两个平方式相加：$2(\dots)^2 + \frac{3}{2}(\dots)^2$；
  3. 看到只有 2 个正平方项（正惯性指数 $p=2$），于是得出规范形为 $z_1^2 + z_2^2$。

---

### 二、 答案方法的优缺点评估

* **优点**：极其严谨，完全按照教科书中“利用拉格朗日配方法求规范形”的定义步骤走。
* **缺点**：当 $a = 2$ 时，**展开再配方的计算量太大**，在考场上极易算错分式（比如答案里出现的 $\frac{3}{2}, \frac{1}{2}$）。

---

### 三、 考场提速建议（如何写既严谨又快速）

答案在 $a=2$ 时的硬展开配方，在考场上属于**“多干了苦力”**。你可以把答案的严谨性与我们上一问讲的**惯性定理**结合，写出既满分又最快的步骤：

1. **当 $a \neq 2$ 时**：按答案这么写！
   > 设 $\begin{cases} y_1 = x_1 - x_2 + x_3 \\ y_2 = x_2 + x_3 \\ y_3 = x_1 + ax_3 \end{cases}$，系数矩阵行列式 $|A| = a - 2 \neq 0$，该变换为可逆线性变换，故规范形为 $y_1^2 + y_2^2 + y_3^2$。

2. **当 $a = 2$ 时**：**跳过展开配方**，直接写：
   > 当 $a = 2$ 时，系数矩阵秩 $r(A) = 2$。又因为 $f \ge 0$（负惯性指数 $q = 0$），故正惯性指数 $p = r(A) = 2$。
   > 由惯性定理，二次型的规范形为 $z_1^2 + z_2^2$。

这样写完全符合考研阅卷标准，且能帮你省下至少 3 分钟展开配方的时间！


# 13
![[_attachments/image/03大观题-1786323634607.jpeg]]

![[_attachments/image/03大观题-1786326714196.jpeg]]





![[_attachments/image/03大观题-1786326723019.jpeg]]



# 14

![[_attachments/image/03大观题-1786326679753.jpeg]]

![[_attachments/image/03大观题-1786326821545.jpeg]]





# 15
![[_attachments/image/03大观题-1786328056726.jpeg]]


![[_attachments/image/03大观题-1786328095166.jpeg]]

![[_attachments/image/03大观题-1786328118051.jpeg]]


# 16 正定证明题 
![[_attachments/image/03大观题-1786329373134.jpeg]]


![[_attachments/image/03大观题-1786329471878.jpeg]]



![[_attachments/image/03大观题-1786329527562.jpeg]]


![[_attachments/image/03大观题-1786329648169.jpeg]] 先给你吃一颗定心丸：图中红字写的“难度略大”是真的，因为它用的证明方法是**“凑矩阵分解 + 凑相似”**（设 $A = P_1^T P_1, B = P_2^T P_2$），这种构造技巧在紧张的考场上**确实很难硬想出来**。

但实际上，**考场上有两种更正统、逻辑更自然的思考路径**！下面助教带你拆解考场上该怎么顺理成章地想出来。

---

### 一、 考场思考路径 1：利用“同时对角化”（最直观）

在考场上，当你看到两个关键词：
1. $A, B$ 是**实对称矩阵**
2. $AB = BA$（**可交换**）

你的脑海里应该立刻联想到线性代数的一个**核心定理**：
> **可交换的实对称矩阵，一定可以“同时正交对角化”！**

**推导步骤**：
1. 因为 $A, B$ 实对称且 $AB=BA$，所以**存在同一个正交矩阵 $Q$**，使得：
   $$Q^T A Q = \Lambda_1 = \text{diag}(\lambda_1, \dots, \lambda_n)$$
   $$Q^T B Q = \Lambda_2 = \text{diag}(\mu_1, \dots, \mu_n)$$
2. 因为 $A, B$ 都正定，所以特征值全大于 0，即 $\lambda_i > 0, \mu_i > 0$。
3. 把它们乘起来：
   $$Q^T (AB) Q = (Q^T A Q)(Q^T B Q) = \Lambda_1 \Lambda_2 = \text{diag}(\lambda_1 \mu_1, \dots, \lambda_n \mu_n)$$
4. 对角矩阵 $\Lambda_1 \Lambda_2$ 的对角线元素全为正数（$\lambda_i \mu_i > 0$），且 $(AB)^T = BA = AB$ 是实对称矩阵。
5. 既然 $AB$ 正交相似于一个全正的对角矩阵，那么 **$AB$ 必为正定矩阵**！

*这种写法逻辑一气呵成，完全不需要去凑 $P_1, P_2$！*

---

### 二、 考场思考路径 2：利用“正定矩阵算术平方根” + 定义法（最简洁）

如果你想用二次型定义 $x^T (AB) x > 0$ 来证，考场上可以这样突破：

1. **对称性**：由 $AB=BA$，可得 $(AB)^T = B^T A^T = BA = AB$，说明 $AB$ 是实对称矩阵。
2. **正定性**：因为 $A$ 正定，我们可以给 $A$ “开平方根”，即存在正定矩阵 $A^{1/2}$ 满足 $A = A^{1/2} A^{1/2}$（且 $A^{1/2}$ 与 $B$ 也可交换）。
3. 代入二次型定义，对任意非零向量 $x \neq 0$：
   $$x^T (AB) x = x^T A^{1/2} A^{1/2} B x = x^T A^{1/2} B A^{1/2} x = (A^{1/2} x)^T B (A^{1/2} x)$$
4. 令 $y = A^{1/2} x$，因为 $A^{1/2}$ 可逆且 $x \neq 0$，所以 $y \neq 0$。
5. 因为 $B$ 正定，所以 $y^T B y > 0 \implies x^T (AB) x > 0$。
6. 满足正定定义，证毕！

---

### 三、 考场应试策略总结

如果在真题中碰到这个题：

1. **必要性（正推：$AB$ 正定 $\implies AB = BA$）**：
   * **超简单，属于送分步**！
   * 牢记上一问讲的“**正定矩阵必为实对称矩阵**”。
   * $AB$ 正定 $\implies (AB)^T = AB$。
   * 又因为 $(AB)^T = B^T A^T = BA$，所以 $AB = BA$。两行搞定！

2. **充分性（反推：$AB = BA \implies AB$ 正定）**：
   * 优先用上面教你的 **路径 1（同时对角化）** 或 **路径 2（平方根法）**，放弃书中那种“硬凑分解”的恶心方法。

3. **如果是选择题**：
   * **特例法秒杀**：直接令 $A = E, B = E$（或对角矩阵 $\text{diag}(1,2)$），满足正定且 $AB=BA$，乘积 $AB = E$ 显然正定。直接选答案



![[_attachments/image/03大观题-1786331266726.jpeg]]

这张图里手写过程和老师字幕在争论的，本质上是**“推理过程的书写严谨性”**。

用一句话帮你理清：**结论和解题方向完全正确，但手写推导里的逻辑符号写错了，导致老师说它“不严谨”。**

---

### 一、这道题的核心逻辑是什么？（极其简单）

1. **观察形式**：二次型 $f(x_1, \dots, x_n)$ 是 $n$ 个**实数平方项**相加。
   因为实数的平方非负，所以 $f(x) \ge 0$ **恒成立**（即 $f$ 已经天然是“半正定”的了）。

2. **正定条件**：正定要求除原点外 $f(x) > 0$。
   由于 $f(x)$ 已经不可能小于 0，要想让它绝对 $>0$，**唯一的可能就是防止它等于 0**！
   也就是：**$f(x) = 0$ 当且仅当 $x = 0$（只有零解）**。

3. **转化为方程组**：
   平方和等于 0 $\iff$ 每一个括号同时等于 0：
   $$\begin{cases} 
   x_1 + a_1 x_2 = 0 \\ 
   x_2 + a_2 x_3 = 0 \\ 
   \dots \\ 
   x_n + a_n x_1 = 0 
   \end{cases}$$

4. **方程组只有零解** $\iff$ 系数行列式 $|C| \neq 0$。

---

### 二、为什么老师在视频里说“不严谨”？

老师强调“不严谨”，不是说这个方法不能用，而是**手写版犯了两个逻辑漏洞**：

1. **漏洞一（混淆概念）**：手写版红字写了“否命题”，但实际上它用的是**逆否命题**（“否命题”和原命题不不等价，只有“逆否命题”才等价）。
2. **漏洞二（漏掉前提）**：只有在**已知 $f(x) \ge 0$ 恒成立**的前提下，才能说“正定 $\iff f(x)=0$ 只有零解”。如果是一个一般的二次型（可能取负值），这个等价关系是不成立的。手写版没写“因为 $f(x) \ge 0$”，直接推，在考研大题中会被扣逻辑分。

---

### 三、考场满分书写范本（直接抄这个）

在考场上遇到这道题，按以下 4 步写，既快又得满分：

**【解】**
1. **写出前提**：
   因为 $f(x_1, \dots, x_n) = \sum_{i=1}^n (\dots)^2 \ge 0$ 恒成立，
   故二次型 $f$ 正定 $\iff f(x) = 0$ 当且仅当 $x = (0,0,\dots,0)^T$（只有零解）。

2. **列出线性方程组**：
   $f(x) = 0 \iff$ 齐次线性方程组 $Cx = 0$ 只有零解：
   $$\begin{pmatrix} 
   1 & a_1 & 0 & \dots & 0 \\ 
   0 & 1 & a_2 & \dots & 0 \\ 
   \vdots & & \ddots & \ddots & \vdots \\ 
   0 & 0 & \dots & 1 & a_{n-1} \\ 
   a_n & 0 & \dots & 0 & 1 
   \end{pmatrix} 
   \begin{pmatrix} x_1 \\ x_2 \\ \vdots \\ x_{n-1} \\ x_n \end{pmatrix} = \begin{pmatrix} 0 \\ 0 \\ \vdots \\ 0 \\ 0 \end{pmatrix}$$

3. **计算行列式**：
   齐次线性方程组只有零解 $\iff$ 系数行列式 $|C| \neq 0$。
   按第一列展开行列式：
   $$|C| = 1 \cdot 1^{n-1} + (-1)^{n+1} a_n \cdot (a_1 a_2 \dots a_{n-1}) = 1 + (-1)^{n+1} \prod_{i=1}^n a_i$$

4. **得出结论**：
   令 $|C| \neq 0 \implies 1 + (-1)^{n+1} \prod_{i=1}^n a_i \neq 0 \implies \prod_{i=1}^n a_i \neq (-1)^n$。
   即当 $a_1 a_2 \dots a_n \neq (-1)^n$ 时，二次型 $f$ 为正定二次型。


我们把手写答案的推导过程像“做手术”一样逐行拆解，看看它到底漏洞在哪里，以及能给我们带来什么考场启示：

---

### 一、详细拆解：手写答案的三大不严谨漏洞

手写答案试图用“数理逻辑（逆否命题）”去推导，结果掉进了三个逻辑坑里：

#### 漏洞 1：脱离前提讲等价（最致命的逻辑漏洞）
手写答案最后写出了一个“结论”：
$$\text{正定} \iff f(x) = 0 \text{ 只有零解}$$

**这个等价关系在一般意义下是完全错误的！**
* **反例**：取二次型 $g(x_1, x_2) = -x_1^2 - x_2^2$（这是一个**负定二次型**）。
  我们来解方程 $g(x_1, x_2) = 0 \implies -x_1^2 - x_2^2 = 0 \implies x_1 = 0, x_2 = 0$。
  你看！$g(x) = 0$ **也只有零解**，但 $g(x)$ 是正定的吗？显然不是，它是负定的！

**为什么手写版在这道题里“凑巧”对了？**
因为本题的 $f(x)$ 是“平方和”，天然满足 $f(x) \ge 0$（排除了负定的可能）。
手写答案**没有写明“因为 $f(x) \ge 0$”** 这个必不可少的前提，就直接断言“$f(x)=0$ 只有零解 $\iff$ 正定”，这在严谨的数学阅卷中会被扣光逻辑分。

---

#### 漏洞 2：混淆“否命题”与“逆否命题”
手写第一行红字写着：“由 $\dots$ **否命题**得 $\dots$”。
* **原命题**：若 $x \neq 0$，则 $f(x) > 0$。
* **手写版写的式子**：若 $f(x) \le 0$，则 $x = 0$。

在形式逻辑中，“若 $f(x) \le 0$ 则 $x=0$” 是原命题的**逆否命题**，而不是“否命题”！
（原命题的“否命题”是“若 $x = 0$，则 $f(x) \le 0$”，否命题和原命题真假性根本无关）。手写版连逻辑术语都用错了。

---

#### 漏洞 3：对“空真”命题绕圈子解释
手写第二行写道：“又 $f(x) < 0$ 则 $x = 0$ 显然不成立”。
这说明写答案的人自己也绕晕了：因为 $f(x)$ 是平方和，$f(x) < 0$ 本来就是**不可能发生的事件**（前提为假）。在逻辑学中，前提为假的条件句属于“空真”，手写版非要强行解释一通“显然不成立”，纯属画蛇添足，反而暴露了逻辑混乱。

---

### 二、这道题给我们的 3 大考研启发

#### 启发 1：做大题时，隐含前提必须“显性化（写出来）”
在考研高数和线代的大题中，任何依托于题目特殊结构的推论，**第一句话必须把前提写出来**。
* **错误写法**：直接写 $f \text{ 正定} \iff f(x)=0 \text{ 只有零解}$。（扣逻辑分）
* **满分写法**：`因为 f(x) 是实数平方和，故 f(x) >= 0 恒成立。因此 f 正定等价于方程 f(x) = 0 只有零解。`（满分）

---

#### 启发 2：能用“代数/矩阵”说话，就不要玩“文字逻辑”
考研线性代数的本质是**矩阵与秩**。这道题如果用矩阵的视角来看，根本不需要绕任何文字逻辑，最正统、最漂亮解法如下：

**【降维打击解法（矩阵法）】**
1. 令 $y_1 = x_1 + a_1 x_2, \dots, y_n = x_n + a_n x_1$，写成矩阵形式 $y = C x$。
2. 则二次型可写为：
   $$f(x) = y_1^2 + \dots + y_n^2 = y^T y = (Cx)^T (Cx) = x^T (C^T C) x$$
3. 二次型 $f$ 的矩阵是 $C^T C$。
4. **利用线性代数核心定理**：
   $$f \text{ 正定} \iff C^T C \text{ 正定} \iff r(C^T C) = n \iff r(C) = n \iff |C| \neq 0$$
5. 直接计算 $|C| \neq 0$，三行拿下！

*这种解法利用了实矩阵重要性质 $r(C^T C) = r(C)$，没有任何逻辑文字漏洞，阅卷老师最喜欢这种干净利落的卷面。*

---

#### 启发 3：识别考研题的“结构套路”
以后在考研真题里只要看到：
$$f(x) = (\text{线性式}_1)^2 + (\text{线性式}_2)^2 + \dots + (\text{线性式}_n)^2$$
要立刻反应出两点：
1. **结构本质**：它就是 $x^T (C^T C) x$。
2. **正定条件**：直接等价于**线性变换矩阵 $C$ 满秩（即 $|C| \neq 0$）**。

掌握了这个本质，你以后做这类题连“$f(x)=0$”都不用解，直接提取系数写出矩阵 $C$，算 $|C| \neq 0$ 就能秒杀。


# 17
![[_attachments/image/03大观题-1786332022709.jpeg]] 这道题是考研线性代数中非常经典的一道**矩阵秩与方程组解的综合逻辑推理题**。

它的核心思路可以总结为一句话：**先化简已知条件，再用反证法推出矩阵满秩，最后判断方程组解的情况。**

下面助教带你按逻辑一步步拆解：

---

### 第一步：化简条件（利用 $1 \times 1$ 矩阵转置不变）

已知对任意非零向量 $\alpha \neq 0$，都有：
$$\alpha^T (AB + B^T A) \alpha = \alpha^T AB \alpha + \alpha^T B^T A \alpha > 0$$

* **关键技巧**：$\alpha^T B^T A \alpha$ 计算出来的结果是一个**实数（即 $1 \times 1$ 的矩阵）**，而实数的转置等于它本身。
* 我们对第二项求转置：
  $$(\alpha^T B^T A \alpha)^T = \alpha^T A^T (B^T)^T (\alpha^T)^T = \alpha^T A^T B \alpha$$
* 因为题目已知 **$A$ 是实对称矩阵**（即 $A^T = A$），所以：
  $$\alpha^T B^T A \alpha = \alpha^T AB \alpha$$
* 代回原条件，两项合并化简为：
  $$2 \alpha^T AB \alpha > 0 \implies \mathbf{\alpha^T AB \alpha > 0 \quad (\forall \alpha \neq 0)}$$

---

### 第二步：推导 $AB$ 满秩（反证法）

思考：$AB$ 有可能是非满秩（降秩）矩阵吗？
* **反证**：假设 $AB$ 不满秩，那么齐次方程组 $AB x = 0$ 一定存在**非零解**，记为 $\alpha_0 \neq 0$。
* 我们把这个非零解 $\alpha_0$ 代入刚才化简的条件中：
  $$\alpha_0^T (AB \alpha_0) = \alpha_0^T \cdot 0 = 0$$
* 这与第一步结论“对**任何非零向量**代入都必须 **$> 0$**”矛盾！
* 结论：假设不成立，$AB x = 0$ 只能有零解，因此 **$AB$ 必须满秩**，即：
  $$r(AB) = n$$

---

### 第三步：推导 $A$ 的秩并判断选项

根据矩阵乘积的秩的不等式定理：
$$r(AB) \le r(A) \le n$$

* 因为已经推出 $r(AB) = n$，被两端夹逼，所以 **$r(A) = n$**（$A$ 是可逆/满秩矩阵）。
* 同样，也能推出 $r(B) = n$。

**对照选项**：
1. 因为 $r(A) = n$，系数矩阵满秩，所以非齐次方程组 $A X = \alpha$ 必有**唯一解**（$X = A^{-1} \alpha$）。$\implies$ **C 选项正确，D 错**。
2. 同理，$r(AB) = n$，方程组 $AB X = \alpha$ 也应该有**唯一解**。$\implies$ **A、B 选项错误**。

---

### 四、 考研应试思维提炼（实战秒杀点）

做这道题时，你在草稿纸上只需要反应出两个**高频考研二级结论**：

1. **结论一**：若对任意非零向量 $\alpha \neq 0$，都有 $\alpha^T M \alpha > 0$，则方程组 $M x = 0$ 绝不可能有非零解 $\implies$ **$M$ 必满秩**。
2. **结论二**：若两个矩阵乘积 $AB$ 满秩 $\implies$ **$A$ 和 $B$ 都必须满秩**（乘积满秩，因子必满秩）。

看到题目条件 $\implies$ 秒出 $AB$ 满秩 $\implies$ 秒出 $A$ 满秩 $\implies$ 秒选 C，全过程不超过 1 分钟！

# 18
![[_attachments/image/03大观题-1786332738143.jpeg]]

![[_attachments/image/03大观题-1786333233208.jpeg]]