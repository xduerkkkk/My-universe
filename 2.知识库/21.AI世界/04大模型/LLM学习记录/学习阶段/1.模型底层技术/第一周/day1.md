视频看到第四个，时间分配较少，白天事太多了！需要继续把不重要的课业学习比例下调

算法上学习优先队列，逆向读栈的应用，还有 python 的 heapq

# 预训练
这个定义起源于视觉模型（cnn）
所有模型的浅层，识别的东西都是类似的
当我们眼前是小数据，未训练模型时
一些大参数大的精准的模型训练出来的底层 layer，就能拿来给我们用
我们可以把自己的数据在这些好模型的 layer 的浅层先跑一跑。
### 后面的训练
- 冻结：固定借来的浅层参数
- 微调：不固定，稍微变变

# 统计语言模型
传统的，就是概率预测，一个一个累乘起来。带着前面所有的信息，预测下一个字是什么
优化一下，就是n 元语言模型。不跟前面所有字关联概率了，就只跟前面 n 个字关联一下。
<font color="#e36c09">为什么这就优化了呢？</font>

# 独热编码
假如 5 个字，那第一个就用一个【1 0 0 0 0】的向量取表示

# 神经语言模型
