图文对齐图文对齐
文是传统nlp的transformer
图是上次了解到的vit transformer

输入有两组，
一组是文本，自然语言，如小猫躺在沙发上 小狗在地上打滚 然后转化为词向量
一组是图片，经过vit转化为图向量

两组向量，一模一样的维度。 

下面我们进行两组向量的矩阵相乘

那么，矩阵的00，11，22，这种对角线位置，就是正确的配对，
我们把正确配对的地方让他们点积大，其余地方让点积小，使用交叉熵损失函数。

这样训练，模型就知道文本与图像的对齐了 