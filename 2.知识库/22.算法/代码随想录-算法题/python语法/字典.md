| 类型            | 访问不存在的键时行为     | 示例                          |
| ------------- | -------------- | --------------------------- |
| `dict`        | 抛出 `KeyError`  | `d['missing']` → `KeyError` |
| `defaultdict` | 返回默认值，**不会报错** | `d['missing']` → 返回默认值      |