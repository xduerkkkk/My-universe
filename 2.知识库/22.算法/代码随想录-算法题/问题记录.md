counter 的用法
python 的 else 缩进

忽视 break 对变量“j”的影响：
```python
while i < len(nums):

    cnt=1

    for j in range(i+1,len(nums)):

        if nums[j]-1==nums[j-1]:

            cnt+=1

        else:

            break

    if cnt>ans2:

        ans2=cnt

        ans1=nums[i]

    i=j
```
应改为 i+=cnt

读取数据何时用 while true何时用 for range