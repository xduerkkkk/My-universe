

---
# 今天想说的话

## 困惑一
<font color="#f79646">整个项目 用户是如何调用的。 我看到是pip install ftllm，为什么？ 然后我看pip后用户在命令行输入参数就能部署并运行模型。我好奇的是，这个项目是怎么做到让用户调用的？ 也就是怎么封装给外界的？我是小白，看很多项目都能这样。 到底是哪些文件起了作用</font>
用户输入命令 -> 触发执行 cli.py 里的 main 函数 -> main 函数再调用其他Python代码 -> 这些Python代码最终调用到由 pybind11 “翻译”过来的C++函数 -> C++代码执行高性能的大模型推理。
pip install ftllm 是直接下载**别人已经编译好的、打包成wheel的“成品”**

## 困惑二
回顾进程、异步，各个进程之间的通信
每个进程都是同步阻塞的，但通过多进程实现并发。 这句话什么意思？？？？？ 多进程难道不就是异步吗？？？？
## 困惑三
floyd算法，为啥把k放到最外层？ 我明白这符合直觉，但我想象不到放内层会怎样。
## 今日目标
第一个为主线任务
1. [ ] 
2. [ ] 
3. [ ] 

## 主线任务拆解
最终目标：
里程碑：



## 今日完成
- [ ] 
- [ ] 
- [ ] 

## 今日总结

## 明日计划
- [ ] 
- [ ] 
- [ ] 




