# 强化 D 62/ 120

## 一、回滚台账

### 今日必须回滚

（早上从 [[1.工作流/回滚台账]] 抄：排期日 ≤ 今天的）

- [[2.知识库/20.North_star_ZJU/201.数学/01高等数学/08三重积分，曲线面积分/02习题]] 16--20 

### 今日未清空 → 明天

（没清完的写这里，同时去台账把状态改「欠」。真清完了写「无」）

- 18，19，20

### 新排期

（今天二刷觉得还不行的题，写进台账，这里只留一行摘要）

- 

---

## 二、今日卡点

> 三科都要填。**真没卡点就写「无」，不许留空** —— 空白和「无」不是一回事，空白让周报没法判断。

### 数学
- 秩一矩阵、秩一矩阵同时是实对称矩阵（aa^T）
- 秩与特征值，特征向量的关系，实对称-->秩一-->对角化....
- 3 倍根号下（1-x 方-y 方）对 x 求偏导，居然把 3 跟着根号放到分母了
- 椭圆区域，用“变形极坐标”，本质是先雅可比换元再正常极坐标，详细见今日回滚 17 题，另外，一定不要把参数方程和极坐标混着，曲线积分一般用参数方程 t，曲面的话就是极坐标换元
- **标准椭球总体积**：$V_{\text{椭球}} = \frac{4}{3} \pi a b c$
### 408
- 关键活动路径不是非得拓扑路径，拓扑只是说在个人计算时候的隐形工具
- DFS 对有权且权值不等的无法做单源路径最短
- 以为“**阻塞进程**”包括切换上下文换到另一个进程，涉及用户态内核态转换
“一个进程因为缺页而被阻塞，直到另一个进程运行”是一个包含了多个微观动作的链条：  

| 触发类型 | 常见名称（408 标准分类） | 具体机制与实例 |
| :--- | :--- | :--- |
| **1. 程序请求** | **自陷 / 访管中断 (Trap)** | **系统调用（System Call）**。<br>用户程序主动执行特权指令请求（如 x86 的 `int 0x80` 或 `syscall`）。 |
| **2. 内部异常** | **异常 / 故障 (Fault / Exception)** | 程序执行过程中发生了**意料之外的错误或特殊事件**。<br>例如：**缺页中断（Page Fault）**、除零错误（Divide-by-zero）、段错误/非法内存访问（Segmentation Fault）、非法指令。 |
| **3. 外部中断** | **外中断 / 硬件中断 (Interrupt)** | **来自 CPU 芯片外部设备的异步信号**。<br>例如：**时钟中断**（时间片用完，强行剥夺 CPU）、磁盘 I/O 完成中断、键盘/鼠标输入中断。 |

> **关键判定：** 只要 CPU 接收到了上述任意一种信号，硬件就会立即保存当前的 PC 和 PSW，**强制将 CPU 特权级降为内核态（Ring 0）**，并跳转到内核的对应处理程序执行。

### 英语

> 英语不在日记里记。卡点、抽查、真题、回滚全在当周单文件：`200.英语积累/04每周训练/`
> 这里只在英语**影响了当天整体安排**时写一行（如超时挤占数学），否则写「见周文件」。

复盘昨天的 TEXT1 就用了快 2h，结果还没总结出新流程，我及时止损，明天下午吃完饭，再优化

---

## 三、今日产物

> 图 / 表 / 卡片 / 模板，至少一个。只是"理解了"不算，下次还会重新混。

-  [[2.知识库/20.North_star_ZJU/202.计算机知识/操作系统/01进程与线程/卡片/用户态，内核态]]

---

## 四、明日最小推进

- 数学：
- 408：
- 英语：

---

## 五、时间漏洞

- 英语阅读大超时，但感觉只要不是无效的，就还好
